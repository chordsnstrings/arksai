# ArksAI — Feature List

A self-hosted, Claude-Code-style web coding agent for **non-technical teams**: describe
something once → get one finished thing that looks perfect, works perfectly, and is
actually live/usable. Powered by MiniMax (the LLM brain + image/vision/speech/video), with Suno for music.

> **Maintenance:** keep this file current — every shipped change that adds, removes, or
> meaningfully alters a feature should update this list in the same commit.
> _Last updated: 2026-07-08._

---

## Robots — the agentic surface
- A **separate, full-page workspace for standing agents**, reached from a **"🤖 Robots"** link in the sidebar and linkable at **`/robots`** — its own destination. The classic **Chat stays exactly as it is**; Robots is an additive second surface.
- **Role-branded agents** (Finance Agent, Marketing Agent, Sales Agent… one per department, with the department's accent + icon) instead of one-shot chats — **standing teammates** that work and check in with you when they need a decision.
- **Triage-first email console (per robot type).** Robots are now organised by **type** (a registry: **Email** is live; Scheduled report / Ads manager / Monitor are declared and plug in as their own console — the host routes by `robot.type`). Open an **email robot** and you get a **triage inbox**, not a settings page:
  - **Needs you, first.** Email robots default to **auto + escalate** (a quiet inbox), so the top of the screen shows only what needs a decision — **escalations first**, then any pending approvals — as cards; when there's nothing, an **"All clear · N handled today"** proof-of-work state. A "**checked Xm ago**" heartbeat + **"Check now"** (with a ping summary, "2 new · 1 replied · 1 for you").
  - **Tap-to-respond by intent.** Tapping an item opens a responder with the **full email**, the escalation reason, **quick chips** (Accept / Propose another time / Decline / Ask / Thank), and a one-line **"how should I respond?"** box — type *"say yes, propose Thursday 2pm"* and the robot **writes the full reply** for you to glance at and **Send** (recipient stays **locked** to the sender). Plus **Snooze / Archive / Forward** (forwarding only to an **admin-allowlisted** teammate).
  - **It learns (fewer escalations over time).** Tick **"handle emails like this automatically"** when you resolve one and it becomes a **standing rule** — grounded into the reply engine so the robot handles that kind of mail itself next time instead of escalating. Learned rules are visible + one-tap-removable in **⚙ Settings** (which also holds mandate / autonomy / triggers / mailbox / the forward allowlist).
  - An ambient **Timeline** (All / Replied / Flagged) shows everything it did. Escalated items now correctly reach **"Needs You"** + the sidebar badge (they were previously only in the activity feed).
- A guided **Hire wizard**: (1) pick the kind — **Customer assistant / Personal assistant / Department specialist** (this sets the robot's real reply persona, and Specialist reveals the department grid for its expertise) → (2) teach it (name, mandate, knowledge, escalation rules, signature) → (3) **connect its own mailbox** inline (Test connection; skippable — it stays paused until connected) → (4) autonomy + triggers → Activate. The roster and office show each robot's mailbox status ("connected ✓ / needs a mailbox").
- **Backed by a real, durable per-org engine** (`/api/orgs/:id/robots`): robots persist, run on the server, and their pending work fills the "Needs You" inbox. The **first working robot type is the email robot** (below) — connect a mailbox and a robot drafts real replies you approve & send. (Department-specific automations beyond email are the next build.)
- **Multichannel auto-responders — Telegram, WhatsApp, and SMS (SMSALA)** alongside email: connect a channel in the robot's ⚙ Settings and the SAME brain answers there — same knowledge grounding, same escalate-don't-guess rule, same ask/auto autonomy, and the reply is always **locked to the person who wrote in** (a prompt-injected "forward this elsewhere" is structurally impossible on every channel). **Telegram** needs only a @BotFather token (the server long-polls the bot — no webhook setup); **WhatsApp** uses the official Cloud API (webhook verify handshake + HMAC-signed inbound when an app secret is stored); **SMS** uses SMSALA (send + two-way inbound via a secret per-channel hook URL; Unicode/Arabic auto-detected). Channel credentials are AES-256-GCM encrypted at rest and write-only over the API; every channel has a one-tap **Test connection**. Replies are channel-aware — conversational and short in chats, ≤450 characters by SMS, never an email signature block outside email.
- **Personas — reusable saved voices** (org-level): create "Concierge", "Legal counsel", "Sales rep" once — voice/tone text, preferred language (e.g. always Arabic), optional signature — and assign one to any robot from a picker. The persona folds into the reply engine under the robot's own mandate.
- **Knowledge base per robot** — upload files (txt/md/csv/pdf/docx, text-extracted server-side) or paste text; on every inbound message a deterministic retriever picks ONLY the slices relevant to that question into the reply context (data-minimized — an unrelated corpus contributes nothing), and anything the knowledge doesn't cover still escalates to a human instead of being guessed.
- **Text the robot → it builds → it delivers ("build commanders")**: list YOUR OWN addresses (your Telegram chat id, email, WhatsApp number) as commanders, then message the robot *"make me a landing page for X and send it to sales@acme.com"* — it recognizes the build command (only from listed commanders; customers can never trigger builds or redirect output), replies "On it", spawns a full unattended ArksAI build session, and when the run finishes **delivers the result on the channel**: files as real attachments (email) / documents (Telegram) / linked documents (WhatsApp, via short-lived token URLs) / a link by SMS — plus the live `/apps/<slug>/` URL when the build published. Explicitly named destinations in your message are honored; one build at a time per robot, a hard time cap, honest failure messages back to you, and every task is audited in a "Builds on command" feed in Settings. **You stay in control by message**: ask "status" for progress, "cancel" to stop a running build (both deterministic — they work even with no model), and after a delivery just say *"make the header blue"* — the robot recognizes a revision, re-enters the SAME build session, applies a targeted change, and delivers only what changed.
- **Conversation memory** — the robot remembers each sender's own thread: earlier messages and what it actually replied are folded into the next answer ("as I mentioned, delivery is 2–3 days"), scoped strictly to that ONE sender per channel — no other customer's conversation can ever leak in, and unsent drafts are never quoted as if they were said.
- **It pings YOU where you live** — when something needs a human (an escalation, or everything awaiting approval — your choice), the robot messages your own Telegram/WhatsApp/email with the customer's message and its proposed draft. Reply **APPROVE** to send it, **IGNORE** to drop it, or just type how to respond — it re-drafts to your direction and sends, then confirms. Recipient stays locked to the original sender throughout; only your listed addresses are pinged or listened to; each item pings once.
- **It reads what people send it — and now LISTENS** — photos are looked at (vision: visible text, order numbers, what's shown), PDFs/Word/CSV attachments are read, and **voice notes are transcribed verbatim in their original language** (Arabic stays Arabic) via an audio-capable model on the existing BytePlus key — no extra service. **Voice commands work end-to-end**: a spoken "build me a landing page for the new offer" from a commander flows through the same build lane as a typed one, and a spoken "approve" resolves a pending ping. Honest fallbacks remain — silence/noise gets "no clear speech", and if transcription is unavailable it asks the sender to type. Works on Telegram, WhatsApp and email; capped and bounded; what it saw/heard is stored with the conversation so you see it in the console too.
- **It TALKS BACK — two-way voice on chat channels**: robot replies can go out as real voice notes (Telegram voice bubbles; WhatsApp audio messages) alongside the text, synthesized with MiniMax speech. Per-robot policy in Settings: **match the sender** (default — speak only when they sent a voice note), always speak, or text-only; email/SMS never get audio (wrong medium). Speech is cleaned for the ear (no URLs/markdown/code read aloud, bounded length), fails soft (the text reply always stands alone), and works on both auto-replies and approved drafts.
- **Meeting invites handled properly (personal assistant)** — a real calendar invite (text/calendar) is parsed, decided per your stated preferences, and answered with a genuine iCal accept/decline (RFC 5546 METHOD:REPLY with the right PARTSTAT and event UID) so the organizer's calendar updates — not just a polite email. Cancellations are acknowledged; ambiguous invites escalate.
- **Routines — the robot acts on a schedule**: a **daily/weekly digest** to your own channel (what it handled, per channel, plus anything that's waited on you too long — quiet days send nothing), and **recurring builds** ("every Monday 09:00: build the weekly sales PDF and send it here") that run through the same build-and-deliver machinery. Double-fire-proof scheduling, timezone-aware, paused robots stay silent.
- **Gated actions — real lookups mid-reply**: define HTTPS calls (an order-status API, a stock check) with `{{param}}` slots; the robot requests one when a question needs live data, gets the result, and answers from it. Admin-defined URLs only (a customer message can never change the host or headers), keys encrypted and write-only, SSRF-guarded, responses treated as data, "ask first" mode escalates instead of running, rate-capped, and every execution is audit-logged.
- **Per-robot performance** — a 30-day strip in each robot's office: messages handled, % handled solo (deflection), median reply time, flags to you, builds delivered, lookups run, a 14-day volume bar and per-channel counts. Metadata only — message content never feeds analytics.

## Core product & agent
- **Excel pattern engine + model scaffold library (2026-07-06, operator-directed; research: Anthropic's own xlsx/dcf/3-statement skills + FAST practice)**: spreadsheet models are built from a PATTERN dialect — each row is ONE formula stamped across all periods, referencing other rows BY LABEL ({ROW:Assumptions!growth rate}, {PREV}, [-k] lags, {FIRST:}/{LAST:}/{RANGE:} refs) and expanded deterministically server-side — ~10× fewer model tokens and the wrong-row-reference failure class is impossible by construction. TWENTY-EIGHT parameterized scaffolds ship as templates (2026-07-06 expansion: "a scaffold for everything that's needed for anyone, including BI") — FINANCE: revenue-forecast · three-statement (IF-gated tax, cash roll-forward) · dcf-valuation (recursive discount factors — no POWER — terminal value, per-share) · loan-amortization (PMT via a compounding-factor row) · saas-mrr (waterfall + NRR) · headcount-plan (loaded payroll) · budget-vs-actual · cash-runway (burn + months of runway) · break-even (+ volume sensitivity) · unit-economics (CAC/LTV/payback) · npv-project (appraisal + profitability index) · capex-depreciation (multi-asset, start-month gated) · working-capital (DSO/DIO/DPO → NWC + cash conversion cycle); PREDICTION: forecast-trend (least-squares + exponential smoothing) · scenario-forecast (bear/base/bull); SALES & MARKETING: sales-pipeline (funnel + conversion) · sales-commission (tiered accelerators) · marketing-funnel (spend→customers, CAC/ROAS); BI & ANALYTICS: kpi-dashboard (actual vs target, MoM %, on-target flags) · cohort-retention (decay curve + LTV/user) · ab-test (pooled SE, z-score, 95% significance flag — sqrt via ^0.5); OPERATIONS: inventory-planning (EOQ — the ordering-cost==holding-cost property IS its check row — reorder point) · project-budget (planned vs actual, % consumed); HR: attrition-headcount (workforce roll-forward); PERSONAL & PROPERTY: personal-budget (inflating expense lines, savings rate) · savings-goal (compounding — closed-form-FV-verified) · rental-property (PMT mortgage, NOI, cap rate, cash-on-cash, DSCR); E-COMMERCE: ecommerce-pnl (GMV→returns→fees→contribution). Every scaffold carries CHECK rows (tie-outs that must equal 0 every period) and the tool REJECTS a model that does not tie; a label-sanity audit also catches wrong-row passthroughs ("Rent" pointing at the bean-cost cell) with a did-you-mean. Spreadsheet-deliverable briefs route to the Swift lane (bake-off-verified equal accuracy at ~16s vs ~8min). **Premium look baked in at BUILD time — no cosmetic pass (2026-07-06, operator-directed)**: every workbook ships with Helvetica typography, hidden gridlines, an accent-TINTED zebra band (branded, not grey), right-aligned numeric headers, finance conventions (single/double rules under totals and bottom lines, blue input font on assumption sheets, italic-muted tie-out check rows), coloured tabs, frozen label column on wide models, and per-ROW number formats in the pattern dialect (`fmt: currency|percent|number|explicit` — money rows read "AED 43,494.21", ratio rows keep precision; all 8 scaffolds fully annotated). Spreadsheets are EXCLUDED from the vision design-review — the deterministic audits (formulas, refs, recalc, check rows) are the gate, so the slow cosmetic re-author pass is gone.
- **Ending gate — every video lands, never stops (2026-07-06, operator-directed)**: research-backed closing-beat enforcement (MrBeast production doc, Kurzgesagt reframing, kicker craft — distilled into SCRIPT_CRAFT.md THE ENDING). A proper ending is a SCRIPT event: before any audio is synthesized the gate now hard-rejects a final sentence over 12 words, a script whose very last word is connective tissue instead of the payoff word, an ending that never calls back to the hook (no shared key word/number), and outro filler ("thanks for watching", "to wrap up"). Advisories catch a 9–12-word landing, an overlong closing beat, and a NEW number introduced in the final beat (the ending synthesizes, never adds). The word-budget rejection now protects the ending explicitly (cut MIDDLE beats — the hook and punch-out are untouchable), the vision QC judges the video's closing frame as an ENDING (payoff at hero scale, able to hold the 2–3s music outro), and MOTION.md gained THE ENDING doctrine: write the last line first, per-length minimum endings (15s = callback punch line + held frame; 60s+ = thesis restated + button line), no end-cards or CTAs on shorts. Same beats at every length — a 15-second short gets a real landing, not a stop.
- **Google Workspace tools — the agent works with YOUR Google account (2026-07-06, operator-directed)**: connect Google once (Settings → Connections; per-user OAuth, tokens AES-encrypted at rest, auto-refreshed, never exposed to the model or the client) and seven tools light up in chat/build (read-only ones in report mode too): **read_gmail** (search + read your mail, bounded digests), **send_gmail** (send AS YOU — lands in your Sent mail; the agent always confirms recipient + text first), **read_calendar** / **create_calendar_event** (attendees get real invitations — confirmed first), **search_drive** + **read_drive_file** (Docs/Slides export as text, PDFs/Office files land in the workspace with text extracted), and **read_gsheet** (values from a PRIVATE Google Sheet the public fetch_data tool can't reach — feeds dashboards/reports with live private numbers). Every tool resolves the SESSION USER's own connection server-side; unconnected users get an honest "connect Google in Settings" pointer, and reads are data-minimized (what was asked, never a mailbox dump).
- **Design studio artifact kinds — wireframes, prototypes, animations (2026-07-06, operator-directed)**: the studio starts from NINE visual template cards (sketch-thumbnail picker): Website · Landing page · **Prototype** · **Wireframe** · Pitch deck · One-pager · Social creative · Document · **Animation**. (1) **Wireframe engine** — a deliberate LO-FI language (`ui-kit/wireframe.css` + a vendored Caveat handwriting face): dashed sketch screens with phone/tablet/desktop frames, hatch-fill image placeholders, grey text bars, red handwritten **annotations** with leader lines, labelled **flow arrows** between screens, and named **concept-variant** columns each carrying a one-line thesis. Wireframe builds bypass the editorial design bar — a dedicated lo-fi review rubric judges flow completeness, annotation quality and flags **hi-fi creep as a defect**; a **deterministic annotation-geometry gate** measures every handwritten note against the board and hard-fails any note covering screen text or another note (annotations always land on empty paper) — and the board exports a shareable PNG alongside the HTML. In the studio, picking the Wireframe template parks the Style menu ("Lo-fi by design") since wireframes are deliberately greyscale. (2) **Prototype kind** — hi-fi, clickable, multi-screen mocks at the full design bar: ordinary linked screens + `ui-kit/proto.js` injects a floating screen switcher (arrow keys jump), optional phone device frame, hotspot pulse cues; fake-but-real data, no backend, no publish. (3) **Animation kind** — the studio routes the brief into the motion engine (render_motion_video) with the Style pick mapped to a pack + accent. Classifier knows the new kinds ("wireframe the landing page" is a wireframe, not a landing page); architecture router keeps them backend-free.
- **Robot use-case catalog — seven jobs to hire for (2026-07-06, operator-directed)**: the hire screen presents everything a robot can be with today's capabilities, grouped by how it works. **Answer people**: Customer support desk, **Sales assistant** (answers from your knowledge and MAKES & sends brochures/price sheets mid-chat, flags hot leads), **Order & booking desk** (answers with LIVE data via connected lookups), Executive assistant (real iCal replies), Department specialist. **Make things on command**: **Studio & builder** — your production line in a chat: images/creatives/docs/sheets/charts on the spot, full builds (websites, videos, decks, songs) delivered anywhere. **Run on a schedule**: **Reporting robot** — recurring reports + digests. Each card carries capability chips and pre-configures the robot (mandate, escalation defaults, autonomy, studio-tools policy); commander-centric jobs add your own addresses right in the hire flow. Catalog in `client/src/lib/robotUseCases.ts` — pure presets over existing engine capabilities.
- **Robots connect to the system's production tools + tools land in plain chat (2026-07-06, operator-directed)**: two lanes now cover the WHOLE tool surface from a robot conversation. (1) **Build lane widened**: a commander message asking for a video/explainer/ad creative/image/logo/song/chart/infographic now classifies as a BUILD (the prefilter + classifier cover all media) and runs in a full agent session that carries every system tool — delivered back on the channel as before. (2) **Studio tools in the reply lane** (`robots/tools.ts`): quick production runs RIGHT IN the conversation — `make_image`, `make_creative` (finished ad with composited text), `make_document` (designed .docx from markdown), `make_spreadsheet` (styled .xlsx from CSV), `make_chart`, `find_photos` — flat-param adapters over the real agent tools, executed in a per-robot studio workspace under `data/`, and the produced files ride out with the reply on the same channel (email attachment, Telegram document, WhatsApp/SMS link). Gated per robot (**Studio tools** select in the robot's settings): the owner's commander addresses only (default), everyone the robot talks to, or off; shares the gated-action hourly rate cap and audit log. (3) **Chat space**: `generate_doc`, `generate_spreadsheet`, `generate_pptx`, `render_chart` and `convert_document` are now first-class in plain CHAT — a one-off document/sheet/deck/chart is produced directly in the conversation, no mode switch. (4) **Robots UI catches up with multichannel**: the hire flow connects channels (mailbox AND Telegram/WhatsApp/SMS) at step 3, role copy speaks about messages not just email, and the "needs a mailbox" warning is now "needs a channel" — a Telegram-only robot counts as connected.
- **Animated-illustrated explainers — the "generative look", two layers (2026-07-08, operator-directed)**: a new video kind (`render_animated_explainer`, and an **Illustrated** card in the Video studio) that produces flowing, AI-animated illustrated explainers in the "generative-video look" — WITHOUT giving up crisp text. Every scene is **two independent layers**: (1) a text-free, non-photoreal generative CLIP (Seedance, style-locked across scenes by one style-key image; the prompt bans on-screen text + realism and reserves calm negative space), and (2) our **pixel-crisp HTML/CSS text composited on top** — titles, callouts, stat cards and captions are never baked into the generated pixels, so they stay sharp, legible and on-brand (the exact thing generative explainers elsewhere can't do — they ban on-screen text and post-burn subtitles). Pick an illustrated LOOK (flat-vector · painterly · ink-wash · paper-collage · silhouette · isometric · storybook · cel-anime). Each text overlay carries a built-in **scrim** so it stays WCAG-AA over any illustration underneath (correct-by-construction, like the hero-fx layer). It reuses the motion engine's script/hook/ending gates, one-narrator TTS, ducked music bed and ending fade, composites the two layers, vision-QCs the result (illustration stays wordless + on-style; overlay stays legible), and retakes one scene at a time. Complements `render_motion_video` (graphic/kinetic-typography, crisp + instant + free); the illustrated kind is for the painterly/animated-film aesthetic and costs real generation time+money per clip.
- **Video outputs named for their subject (2026-07-06, operator-directed)**: a finished explainer downloads as e.g. `why-airplane-windows-are-round-final.mp4` (from the new `title` param on render_motion_video, falling back to the hook scene's label), and story videos as `<first-words-of-the-story>-final.mp4` — never a generic `explainer-final.mp4`/`story-final.mp4`. Old manifests keep their original names for retakes. Also fixed: the in-chat video card's path detector never matched the nested `videos/motion-<id>/…` paths (missing `/` in the regex class) — corrected so produced videos reliably get a playable card.
- **Motion SCRIPT QUALITY engine — storytelling craft, enforced before a word is voiced (2026-07-06, operator-directed)**: "improve our storytelling techniques… ensure the actual script that gets generated is high quality". From storytelling research (But/Therefore writers-room rule, Pixar story spine, retention-curve science, broadcast write-for-the-ear guides, the documented LLM-prose lexical fingerprint — distilled in `SCRIPT_CRAFT.md`): (1) a **deterministic script gate** (`scriptProblems` in motion QC) runs over the whole narration BEFORE any TTS is paid — **hard failures** (essay scaffolding like "firstly/moreover/in conclusion/let's dive in", engagement CTAs, hype promises like "mind-blowing", and the But/Therefore test: a ≥3-beat script with zero contrastive/causal connectives is a reorderable list, not a story) block synthesis with a named fix; **advisories** (intensifier pileups, hedging, AI-lexicon tells like delve/tapestry/unlock/seamless, three same-length sentences in a row, >24-word sentences, a script that never says "you") ship but come back in the tool result as SCRIPT NOTES so the agent fixes the narration on its next pass. (2) **SCRIPT DOCTRINE rewritten** in MOTION.md: story spine + beat templates by length (15s/30s/60s/3min word budgets), open-loop rules, language craft (concrete nouns, "you" as the stakes-carrier, translate every number into a comparison the ear can hold, deliberate sentence-rhythm variety), and write-for-the-ear TTS rules (contractions, no abbreviations/symbols, end-focus, a ≤8-word landing-strip last line that calls back to the hook). The Explainer studio brief steers the same doctrine.
- **Motion DESIGN LIBRARY — ~900 intent-indexed presets the AI picks from (2026-07-05, operator-directed)**: "close to a thousand different typography, call outs, SVG background movement and other little extra animations with indexed but with intent". New `agent/motion/library.ts` — parametric generators over the kit's base systems produce ~900 curated entries {id, kind, name, INTENT line, mood tags, pack affinity, paste-ready snippet}: **type** (133: every bundled family in hero/headline/uppercase/label/italic/mixed-scale/ghost-echo/display-numeral/pull-quote/small-caps/tight-giant treatments + 20 curated display×label×data pairings with register intent), **callouts** (92: pills, print stamps, flag tags, ribbons, crawling underlines, keycaps, corner badges ×4 positions, drawn brackets, shout boxes, highlighter/ink labels — toned accent/danger/money/ink × compact/standard/large), **animated backgrounds** (506: dot grids, ring dots, diagonal/vertical hairlines, blueprint grids, crosshatch, plus marks, contour waves, soft checker — each × density × motion (drift/rain/rise/slide/spin/breathe/still, direction carries MEANING) × ink/accent × whisper/standard/present opacity; plus morphing accent blobs, orbit rings with travelling dots, light sweeps), **micro** (162: entrances by role×timing incl. blur-in/flip-in/drop-bounce/swing/diagonal-wipe/zoom-settle/skew, phase-offset idles, emphasis effects). Pure token-overlap search with mood synonyms (`searchMotionDesign`), surfaced as the **`search_motion_design` tool** (query by intent → exact snippets), and every scaffold takes a `bg:<library-id>` slot that injects the animated layer under the content (validated, unknown ids error with guidance). Kit CSS gained the parametric bases (.mg-bgx scroll/spin/breathe, .mg-blob, .mg-orbits, .mg-bgsweep, 9 new callout components, 8 new micro keyframes). Locked by catalog-integrity tests (unique ids, every snippet class exists in motion.css, intent lines present).
- **Motion CRAFT engine — animated dataviz, drawn icons, kinetic emphasis, film-grammar scenes (2026-07-05, operator-directed)**: from deep practitioner research (Vox/Kurzgesagt/Economist/Harris workflows, distilled in `MOTION_CRAFT.md`) — the answer to "no animated charts, animated icons, texts given emphasis… doesn't look templated / AI". (1) **Animated charts BUILT from data slots** (no more pasted static SVGs): `bar-chart` (staggered growth on the chart easing curve, counting tabular-num value labels, exactly ONE hero bar in accent — the rest muted, optional `source` credibility line fading in last), `line-chart` (SVG path draws on via pathLength=1000, faint gridlines, end-dot pop + counting end value, area fill after the stroke), `donut-stat` (sweep synced to its counting center number). (2) **Animated icons**: stroke icons (lucide/tabler/ph) DRAW themselves on (`drawIcon` wrapper, .mg-draw stroke-dashoffset) then settle into idles; semantic motion — `tone:danger` callout subjects get a damped shake (each cycle 60% of the previous — constant amplitude reads template). (3) **Kinetic word emphasis**: mark a phrase with *asterisks* in ANY scaffold text slot → the pack's emphasis treatment (vox yellow highlighter, nordic thin red rule, broadcast amber sweep, accent elsewhere) fires proportionally while the word is spoken; selection doctrine (≤1 phrase/sentence, numbers/entities/pivots/negations only) in MOTION.md. (4) **Film-grammar scene types**: `chapter-card` (ghost-numeral act break), `timeline` (progress rail draws, dated events pop alternating above/below; vertical rail in 9:16; endpoint labels clamped inside frame), `breath` (the near-empty quiet beat that keeps dense videos from feeling relentless). (5) **Anti-template sequencing**: the tool flags same-scaffold neighbours (shot-grammar advisory); MOTION.md gains ANIMATED DATA / EMPHASIS / SHOT GRAMMAR doctrine (alternate wide/medium/close, entrance direction follows meaning, one breath per ~30s). 17 scaffold archetypes total. 948 tests.
- **Motion STUDIO LOOK — real photos, cutouts, editorial typography (2026-07-05, operator-directed)**: "hit that studio look… real photos, cut outs, typography and other scaffolding ready for any kind of media". (1) **Cutout pipeline**: `search_photos {cutout:true}` also produces `<photo>-cutout.png` — the subject lifted off its background via the product-ad engine's rembg step (same graceful degrades: not installed / low contrast / too small each reported plainly, plate fallback suggested); vision gate tightened to demand ONE separable subject when a cutout is requested. (2) **Studio material kit** (motion.css): `.mg-cutout` (+`.sticker` die-cut white outline, `.ink` print outline + hard offset shadow), `.mg-duotone` (accent-toned plates — each pack tones photography in its own color), `.mg-archival`, `.mg-halftone`, `.mg-tape`, `.mg-torn`, `.mg-polaroid`, `.mg-crop-circle`/`.mg-crop-arch`, `.mg-photo-grain`. (3) **Photo-first scaffolds**: `photo-hero` (full-bleed treated plate + giant mixed-size headline + drifting echo), `cutout-stat` (cutout subject + hero counter + accent bar; sticker edges on nutshell/broadcast, ink on nordic), `collage-compare` (two cutouts face off, pack-labeled) — all packs × all formats, living-frame ambients baked in; `annotated-plate` gains `treatment: duotone|archival`. (4) **Real display typography, guaranteed**: each pack speaks in a bundled face (clean→Fraunces, nutshell→Bricolage Grotesque, broadcast→Space Grotesk uppercase; vox/nordic editorial serif) and `ensureMotionKit` now installs the self-hosted font set into every motion workspace (previously scenes silently fell back to system fonts unless the agent called add_fonts). (5) QC gradesheet flags raw untreated photo rectangles and steers fixes toward targeted scaffold retakes over bespoke rebuilds. Validated by a 45-frame studio matrix (5 packs × 3 sizes × 3 scaffolds) through the real engine.
- **Motion design engine v3 — per-pack design DNA, any-size formats, living frames (2026-07-05, operator-directed)**: the answer to "focus on the design part for each, fix it for any size, ensure there's dynamic movement within the frames". (1) **FORMAT system**: scaffolds derive a real format from the output pixels — landscape (16:9), square (1:1 AND 4:5, tightened row compositions via `.mg-fmt-sq`), tall (9:16, full-height stacked) — replacing the binary portrait flag that gave square canvases squeezed widescreen layouts; display sizes are aspect-bounded everywhere (`min(Xvh, Yvw)`). (2) **Pack identity in the ENGINE**: every scene carries `.mg-style-<pack>` — pack accents (nutshell neon magenta/cyan, broadcast red/amber, vox mustard→full yellow only on dark grounds, nordic red, clean green), display-type voice (nutshell heavy sans, broadcast shouting uppercase, vox/nordic/clean editorial serif) and per-pack card DNA (broadcast comic-outlined, nordic ruled print, nutshell soft glass) apply automatically; an agent-passed accent still wins (written inline on the scene element). Previously all five packs rendered with the same default green unless the agent passed an accent. (3) **Living frames**: perpetual phase-offset ambient motion baked into every scaffold — drifting background echoes (`.mg-drift` wrapper), bobbing icons, shimmering labels, a `.mg-runline` travelling-accent hairline filling empty lower bands — and QC gained a weak-motion advisory (camera-only mid-scene stretches get flagged with concrete fixes; pixel-static stays a hard fail). (4) **Composition upgrades**: split-compare cards claim the frame on real pack-skinned surfaces, the callout's big number is a hero stat above the label (killing the number/text concat bug), hero-stat gains an accent rule + baseline band. Validated by a 100-frame matrix (5 packs × 4 sizes × 5 scaffolds) through the real engine, eyeballed; mid-scene motion floor lifted ~2× and all packs render visibly distinct at every size. (5) **Music outro** (operator follow-up): with a bed, the final scene holds ~2.6s past the last spoken word so the sidechain duck releases — the music rises to full and carries the ending, then music+picture fade over ~1.8s; the narration is never faded or clipped (loudness-curve verified through the real ffmpeg chain). Without music the short breath + 0.9s fade stays.
- **Motion polish engine — media-house craft, guaranteed by the system (2026-07-05, operator-directed)**: the answer to "student project, not a polished media house". (1) **Scene scaffolds** — 11 slot-driven archetypes (`hook-question`, `hero-stat`, `split-compare`, `process-steps`, `annotated-plate`, `callout`, `character-beat`, `chart-insight`, `quote-punch`, `list-recap`, `end-punch`) with entrance choreography (≤1.2s, 40-80ms staggers), EXIT choreography in the scene tail, an eased camera move per scene (direction auto-rotated), ambient idles, composition (grounds/depth/vignette/safe areas) and per-style-pack skin all baked in — the AI passes `{scaffold:{id,slots}}` and focuses on script + content + asset picks; slot word ceilings make walls-of-text mechanically impossible. (2) **Retention-science script doctrine**: scene 1 must be a HOOK (question/bold claim/stake — greetings and "in this video" openings are REJECTED before any TTS is paid), BUT/THEREFORE beat chaining, `target_seconds` word-budget check (~2.4 words/s) that blocks narration overshoot pre-synthesis, punch-out endings, pattern-interrupt pacing (MOTION.md carries the full hook templates + beat sheets + Mayer multimedia rules). (3) **Designed transitions**: choreographed cuts by default (exit → entrance), plus per-boundary `transition: dip|dissolve|wipe|slide|circle` rendered via segmented voice-safe xfade (the fade window fits inside the silent holds — narration never crossfades). (4) **Kinetic typography kit v2**: masked line rises, word-by-word reveals (`.mg-words`, split deterministically at load), keyword pops, accent sweeps, follow-through lag, squash-and-settle, stress pulses, eased cameras + parallax depth layers, environments (floor grounds, vignette, contact shadows, edge-bled hero props). (5) **QC that can see the problems**: a deterministic pixel-diff STILLNESS audit hard-fails any scene that renders frozen (the failure mode the delivered LDL videos shipped with), a 3×3 frame-fill audit flags mostly-empty frames, spot frames moved to early/mid/late with a student-vs-pro vision gradesheet (incl. icon↔label semantic match), and the variety check now judges cut quality with two stills per scene. (6) **Real photography**: `search_photos` — Pexels photos+footage when keyed (admin providers endpoint, encrypted at rest) with keyless CC fallbacks (Openverse, Wikimedia Commons), vision-gated downloads, automatic ATTRIBUTIONS.md, and an explicit fallback chain to generate_image/generate_video when no quality asset exists; powers the vox `annotated-plate` scaffold's evidence plates. (7) **A fifth style pack: `nordic`** — Swiss International Typographic Style × Scandinavian editorial, from primary-source research (Müller-Brockmann grids, Form-magazine warmth, Bass/Ferro kinetic type): paper+ink+one-accent palettes, 12-column grid stage, kicker-on-hairline, oversized ghost numerals, vertical rails, and a disciplined kinetic-type vocabulary (`.kt-stamp` speech-beat word stamps, `.kt-swap` hard-cut giant word swaps, `.kt-track-in`, `.kt-rail-wipe`, `.kt-drop`, typewriter caret) — ONE device per scene by doctrine; appears in the Style Picker automatically with a real engine-rendered preview.
- **Visual Style Picker (2026-07-04)**: the Video studio's Explainer selects styles via picture cards — every thumbnail is a REAL frame rendered by that pack's engine (`server/assets/motion-kit/previews/`, served by `GET /api/motion/styles` + per-style preview routes, auth-gated, traversal-safe), so the card can never drift from actual output. Catalog-driven (`MOTION_STYLES` in `agent/motion/styles.ts` — the directions-menu pattern): a new pack entry + preview jpg lights up in the UI with zero client changes; `available:false` ships packs hidden. Selection ring in each pack's accent, 2×2 on mobile, QA'd 1280/390.
- **Motion style packs + micro-animation doctrine (2026-07-04, operator-directed)**: explainer videos now ship in four signature styles — **nutshell** (Kurzgesagt-inspired: neon flat vector on cosmic gradient grounds, seeded starfields (`__mgScatter`, deterministic), parallax hill planes, glow, an `.mg-bird` mascot with a composable acting vocabulary: blink/hop/wing-flap/gaze), **broadcast** (Infographics-Show-inspired: bright stage ground, outlined color props, `.mg-callout` shouting stat labels with danger/money variants, `.mg-tag` arrow pins, `.mg-vs` split-screens — every spoken number appears as a callout), **vox** (Vox-inspired annotated evidence: warm studio-gray ground, Ken-Burns plates, yellow boxed `.mg-label-vox` labels, `.mg-highlight` highlighter sweeps, `.mg-underline`, `.mg-connector` draw-on lines, serif editorial titles), and **clean** (house). Chosen via the `style` param on render_motion_video + a Style picker in the Video studio's Explainer kind; MOTION.md carries per-pack palette/motion doctrine. **NOTHING IS EVER STATIC**: every element enters smoothly then settles into an ambient idle (`.mg-breathe/.mg-sway/.mg-bob/.mg-float/.mg-shimmer`, phase-offset via negative delays) or rides a camera move — locked by tests. The asset library grew to **~17,800** with 1,791 full-color props (fluent-emoji-flat MIT + Streamline Kameleon CC-BY, `kind:'prop'`, never recolored). All three packs proven through the real capture pipeline (sandbox frames eyeballed); style research (Kurzgesagt/Infographics/Vox production analysis) in the session record.
- **Motion studio — autonomous narrated motion-graphics videos, no video model (2026-07-04)**: `render_motion_video` produces real mp4 explainers from VECTOR scenes — the agent writes the script, splits it into scenes, pulls exact icons/logos via the new asset library, authors each scene as a motion-kit HTML page (seekable web animation: reveals, SVG draw-on, counters, typewriter, camera moves — one brand identity via `--mg-*` tokens), and the tool does the rest autonomously: per-scene MiniMax narration FIRST (scene length derives from the real audio — sync by construction), deterministic frame capture in headless Chromium (`__seek()` on paused animations — identical frames on any machine), uniform h264 encodes, vision spot-frame QC (defects come back named; fix the scene file and retake JUST that scene), lossless concat + optional auto-ducked Suno music bed. Any length (30s to many minutes; 30fps short / 24fps long), any dimension (16:9/9:16/1:1/4:5), pennies per video (TTS only — zero video-model cost). Surfaced as the Video studio's new **Explainer** kind and auto-routed in chat ("explainer/animated/how-to video" → motion graphics; Seedance strictly for filmed footage). Sandbox-proven e2e: 475 frames captured deterministically, final duration == narration sum ±0.02s.
- **Vendored asset library — ~16,000 open-licensed vector assets, searchable offline (2026-07-04)**: `search_assets` ranks Lucide + Tabler + Phosphor icons, the Health Icons medical set, and 3,400+ REAL brand logos (Simple Icons, official brand hex) with a deterministic synonym-aware search ("cholesterol" → blood-vessel/heart-organ; "stripe logo" → the real mark), then writes brand-recolored, attribution-headed SVGs into the workspace — the hard rule baked into prompts: never hand-draw an icon or guess a path. `fetch_asset` adds a binary-safe, SSRF-guarded download of any public asset URL (the old text-only fetch corrupted binaries). No API keys; everything ships in the image.
- **Design studio — a chat-first design surface at `/design` (2026-07-04; reskinned to the ArksAI identity 2026-07-06)**: a separate full-page studio (🎨 Design in the sidebar, like Robots/Android/Video) wearing **the product's own editorial identity** — the app's warm paper, ink, house accent, Source Serif greeting and Space Grotesk labels, in both light and dark (it inherits the app tokens; no borrowed skin). Chat-first interaction: a time-of-day greeting, ONE composer with suggestion chips (Website · Landing page · Pitch deck · One-pager · Social creative · Document), optionally pick a **Style** from the composer menu — "Let ArksAI choose", "Use my brand", or any of the 40 tested directions served by the read-only `GET /api/design/directions` (swatch + mood + dark chip, grouped modern/glass/structural/aesthetic; the pick locks the exact `design_direction` recipe) — attach a logo/content files, and send. The conversation stays INSIDE the studio (centered column, the live preview sliding in beside it) and refinement is conversational — you just keep talking. Recent designs (sessions tagged `design.*`) list on the landing and reopen in place. 0-overflow at 390px.
- **Claude design doctrine absorbed into the engine (2026-07-04, `CLAUDE_DESIGN.md`)**: five research-backed upgrades locked by tests — (1) **treatment calibration** ("calibrate the treatment, not whether to design"): the brief compiler now emits `TREATMENT: utilitarian | balanced | editorial` read from the request, and the design-review rubric judges per-treatment — a memo/internal tool is rewarded for composition + legibility and no longer failed for lacking a hero/signature (over-designing a utilitarian page is now a named defect, ending gate churn on plain asks), while landing/brand work keeps the full distinctive bar; (2) **words are design material** — user-side naming, controls that say what happens, errors that say how to fix, empty states that direct the next action (designCore + CRAFT + a rubric COPY bullet); (3) **token-level dual theme** for apps with a theme toggle — tokens on `:root`, media-query redefines tokens only, `[data-theme]` overrides both directions; (4) the **anti-default-look list completed** (adds purple→blue gradient hero, everything-centered, uniform rounded-lg, accent bar/rail on cards, emoji section markers, Inter/Space-Grotesk-as-inherited-default) across the brief compiler, designCore, the rubric and the design_direction defaults; (5) **micro-craft** — `text-wrap:balance` headings, ~65ch measure, tracked uppercase labels, gap-not-margins, `overflow-x:auto` containers, semantic state colour separate from the accent, "boldness in ONE place" + accent-conflict (shift analogous/desaturate, never swap).
- Autonomous agent loop — streaming, tool-calling, stall guard, context truncation, graceful error handling.
- **Shareable per-chat links** — every session has its own URL (`/s/<id>`) reflected in the address bar (deep-link / bookmark / reload reopens it — including older chats past the first page, fetched on demand; back-forward works), plus a **🔗 Link** button in the top bar to copy it. A link to a deleted or inaccessible chat shows a clear "this chat isn't available" panel (the URL is kept so it can be retried) rather than silently dumping you on the home screen.
- One seamless chat — no modes to pick. The user just talks; ArksAI reads the request and moves itself into whatever it needs — chat, plan, build (code), report, or image generation — with the right curated toolset. Under the hood there are still 4 modes (Chat / Plan / Code / Report) + capability tools, but the user never toggles them (a read-only chip in the TopBar shows what it's doing). Power users can still force one with `/mode`.
- **Multi-file spreadsheet COMBINE engine (2026-07-06, operator-directed)** — `combine_spreadsheets` merges many xlsx/xls/csv files (or tabs) into ONE clean, reconciled, premium-themed workbook, fully deterministically: real-header detection under bank-export preamble, meaning-based column auto-mapping across files (synonyms; debit+credit pairs → one signed amount, credits positive), date normalisation (DD/MM vs MM/DD resolved by corpus evidence, Excel serials, month names; rollover dates rejected), amount normalisation (currency symbols/codes, thousands separators, parentheses negatives, CR/DR markers, SAP trailing minus), cleaning (empty rows, repeated in-file headers, footer total rows), cross-file de-duplication for overlapping exports, a Source provenance column and date sorting. ACCURACY BY CONSTRUCTION: the engine reconciles per source (rows in == kept + every counted drop, throws on mismatch) and ships the proof INSIDE the workbook — an Audit sheet with live COUNTIF/SUMIF formulas re-counting/re-summing each source vs engine expecteds (tie checks read OK) plus a Monthly summary of live SUM formulas over the date-sorted contiguous blocks. When auto-mapping isn't confident it writes nothing and returns per-source profiles + a proposed mapping JSON for one correction round — the model only ever decides the mapping, never touches row data. Imported-data sheets are exempted from the model-shaped audits (a "Balance transfer" transaction is data, not a hard-coded roll-up).
- **BI toolkit v2 (2026-07-06, operator goal "complete BI section")** — three deterministic engines join the combine engine, all "the model picks the files/columns; the engine handles every row" with in-workbook proof:
  - **`reconcile_spreadsheets`** — matches two exports that should agree (orders vs payouts, bank vs ledger, GL vs subledger) and buckets EVERY row: matched, amount-mismatched (with delta — the fee/partial-payment gap), probable (same amount, dates within ±N days), only-in-A, only-in-B. Match keys auto-picked (a well-populated unique Reference wins; else date+amount+description with entity-name normalisation — "Acme, Inc." == "ACME INC"); an invariant throws unless every input row lands in exactly one bucket. Themed workbook: Reconciliation summary with live accounted-for checks + a sheet per bucket.
  - **`analyze_variance`** — the "why did revenue drop?" answer: decomposes a metric's move between two files (or one file split by period column) BY DIMENSION; segment deltas sum EXACTLY to the total change (bridge identity, throws otherwise), movers ranked, NEW/disappeared segments flagged, deterministic driver commentary returned ("Revenue fell 270 (-20.8%): driven by EMEA -120 (44.44% of the change)…") + a Variance Bridge workbook (prior → movers → current with an OK tie check, movers sheet per dimension).
  - **`record_metrics` / `metric_history`** — recurring-report memory: org-scoped metric snapshots per series+period so a scheduled weekly/monthly report grounds "vs last period" in the actually-recorded numbers. Like-for-like deltas only (a missing metric is flagged, never compared as zero); re-recording a period is a RESTATEMENT — prior values kept and the before→after diff surfaced in every read.
  - **Combine v2 hardening** — EU number locale (1.234,56) detected per source, two-row (spanning) headers composed, multiple tables per tab split (largest wins, preamble ignored), wide month-column sheets unpivoted to rows, Excel-corrupted long IDs (1.23457E+13) flagged, update-with-new-file mode (a prior combined workbook's Source column is preserved and de-duped against), per-source first/last date on the Audit sheet, and months with NO rows inside the data range reported as "no data" — never zero.
  - **Honesty furniture (doctrine)** — every data deliverable states its ground: data-as-of range (from the data, not today), sources, rows in → kept. Report/chat/scheduled prompts steer to these tools and to surfacing restatements.
- **Ad & motion craft borrows (2026-07-07, from Higgsfield research)** — three production patterns adopted from the leading AI-video platform:
  - **Hook variants (`generate_creative`)** — pass 2-3 alternate headlines in `headline_variants` and each is composited onto the SAME settled background as its own image: an instant A/B/C ad set for one image-generation cost. The tool + marketing steering teach the proven hook archetypes (question / bold claim / before-after / social proof / stat lead); product-URL ad asks crawl the site first so the brief uses the real product language and brand colours.
  - **Camera vocabulary (motion kit)** — 16 named cinematic moves (`mg-cam-crash` crash zoom, `mg-cam-whip-l/r`, survey pans, pedestal `rise`/`fall`, 3D `orbit-l/r`, `mg-cam-fpv` sweep, `mg-cam-handheld`, `mg-cam-dutch` + the original push/pull/drift), all seekable and edge-safe, indexed as a `camera` kind in the motion design library so agents pick a move by intent ("shock stat" → crash zoom); MOTION.md maps each move to its storytelling meaning.
  - **Style anchor (`generate_video_story`)** — a `style_anchor` image is attached as a style-only reference to every plain scene (r2v) so a multi-shot cinematic story holds one palette/grade/lighting across shots; scenes with cast refs, an opening frame, or chained frames keep their own stronger anchor.
- **Copy any response** — every assistant reply carries a quiet Copy button underneath (next to the run timing); one click puts the full response text on the clipboard with a transient "Copied ✓" confirmation. Works on plain-HTTP local dev too (textarea fallback when `navigator.clipboard` is unavailable).
- Tooling: file read/write/edit, glob/grep, bash + persistent background processes, git, web search/fetch.

- **Judgment engine — benchmarked routing + failure discipline (2026-07-02, see QUALITY_PROGRAM.md)**: a live 3-scenario judgment bake-off (provider decline, repeated error, ambiguous ask) across the BytePlus stable re-routed **chat light/standard to Swift (seed-2-0-pro)** — the only model that didn't hallucinate "I can't create images / use Canva" (the exact deployed lie), the best repeat-error diagnosis, and the fastest; heavy chat stays on M3. Every mode now carries a **Judgment & failure handling** block (read/quote the real error; classify my-call vs provider-decline vs truly-absent; NEVER coach evading a safety check; never deny a toolset capability or recommend external tools; fix root causes yourself). The runner adds a **repeat-error circuit-breaker** (an identical tool error twice gets a [SYSTEM] nudge in the result itself — the only intervention every model respects). Complexity routing is **subsystem-aware** (multi-tenant/auth/orgs/invites/isolation/billing stacking + enumerated-requirements briefs score HEAVY — the "moderate task" SaaS mislabel is a locked test). Big builds write **`.arksai/CONTRACT.md`** (exact API shapes, naming, entities, credentials) before step 1 and the checkpoint resume note injects it verbatim, killing cross-window contract drift; and a **BytePlus stall ladder** (retry → Kimi → M3, each hop visible) means a silent stream recovers instead of killing a checkpointed run.

- **The gate uses the app like a person — authenticated every-page audit + CSS cascade lint (2026-07-02)**: the pre-delivery browser check no longer stops at the first screen. When the built app shows a login and demo credentials, the gate **signs in and walks every nav destination at 390px AND desktop**, running three deterministic detectors — *clipped content* (a box cut off by the viewport edge outside any scroll container — catches what scrollWidth can't see under overflow:hidden), *covered headings* (a chip/panel sitting on an h1–h3), and *truncation clusters* (3+ ellipsized fields hiding >30% of their text — names reduced to initials). Validated by re-breaking a real shipped app: the walker caught every planted defect plus two REAL residual ones a manual audit missed (a clipped dashboard card, kanban columns unreachable on mobile), and runs clean with zero false positives on the fixed build — one bounded pass, never a loop. Upstream of the browser, **`auditCssCascade`** (pure, tested) statically flags the root disease that shipped those flaws: a max-width @media rule declared ABOVE the base rule for the same selector is silently overridden at every width (source order) — the whole mobile layer half-applies. The CODE craft block now teaches the prevention (responsive layer at the END of the stylesheet; identity columns get the flexible space; no "overdue" on completed items; self-check inner pages signed-in).

- **scaffold_app — correct-by-construction full-stack apps on any brief (2026-07-02, SCAFFOLD_PLAN.md Phases 1–2)**: any app with a backend now starts from a composable skeleton system instead of hand-rolled plumbing. The **service skeleton** (Express + SQLite + Vite/React client, ONE port) owns every property the platform requires — /api before the SPA fallback, relative paths (path-proxy safe), PORT env, idempotent migrations (`server/migrations/*.sql` run-once) and seeds (seed only when absent — a republish never wipes data), JWT auth with a **seeded demo login**, graceful shutdown, and the responsive layer at the end of the stylesheet. **Capability modules** compose onto it with auto-resolved deps: `crud` (the exemplar entity the model CLONES per real entity), `orgs` (multi-tenant workspaces + invite codes + the mergeParams-correct isolation middleware — tenancy is never hand-rolled again), `dashboard`, `forms` (public intake + in-app outbox, zero credentials needed), `uploads` (data/uploads convention), `realtime` (SSE-first — proxy-safe; the client hook abstracts the transport), `jobs` (dep-free in-process scheduler + run log). The tool generates **`.arksai/CONTRACT.md`** (the binding conventions — response casing, error envelope, demo creds) and **`.arksai/verify.json`** — the scaffold DECLARES its own verification, and the quality gate now reads it: the signed-in walker uses the declared demo account (no page parsing), every declared route is asserted (works for API-only apps), and the SSE endpoint must stream within 5s. A **brownfield guard** refuses to clobber an existing project. Proven end-to-end: the all-modules golden app assembled, seeded, built, booted, and passed the full gate (signed-in walk of 5 pages × 2 viewports + manifest routes + SSE) with zero defects; a 5-test conformance suite (module resolution, generated-guard correctness, placeholder-free assembly, brownfield, manifest) keeps the scaffolds from rotting.

- **Binance Pay: crypto checkout as the sixth rail (2026-07-03)**: the payments module adds **Binance Pay** — buyers pay in crypto from the Binance app, merchants settle in USDT. Same doctrine as every rail: dependency-free (v3 order API with the per-request HMAC-SHA512 signature computed via node:crypto — timestamp/nonce/certificate headers), hosted checkout page, and **server-verified truth** (confirm-on-return queries the order by prepayId; only status PAID marks the order paid). Orders are denominated in the shop's currency — if the merchant account doesn't support that fiat, Binance's own error surfaces verbatim to the owner. The storefront shows a distinct "Pay with crypto (Binance Pay)" button alongside "Pay now" (card rail) and PayPal; the owner pastes the Binance Merchant API key + secret on the same Payments page (write-only, tail-shown). Boot-validated end-to-end: fake keys → the request signed and dispatched to Binance's real endpoint, error surfaced through the actionable 502 path (the sandbox's US egress gets Binance's 451 geo-block — from the UAE droplet the API is reachable).

- **UAE payment rails + wallets: Ziina, Telr, Network International (2026-07-03)**: the payments module grows from two rails to five — **Ziina** (the UAE SME favourite: fast signup, AED-native, minutes to keys), **Telr** (low local-card rates, GCC coverage) and **N-Genius / Network International** (the UAE's bank-backed enterprise gateway, incl. **Samsung Pay**) join Stripe + PayPal, all dependency-free over plain REST with the same server-verified confirm-on-return doctrine. **Apple Pay and Google Pay come free**: they appear automatically on every provider's hosted checkout page — wallet buttons are never hand-built. The owner's Payments page now covers all five (each with test/sandbox ↔ live toggles and write-only keys), plus **checkout preferences**: a currency selector (AED/USD/SAR/EUR/GBP/KWD/BHD/QAR/OMR/EGP/INR — Telr takes major units, Ziina/N-Genius minor units, handled per provider) and a **preferred card rail** — the storefront shows ONE clean "Pay now" button wired to it (plus PayPal), never a wall of gateway buttons. Validated against the REAL provider APIs with fake keys: Telr answered "E04:Invalid store ID", Ziina "Unauthorized" — both surfaced verbatim to the owner as actionable 502s; rail selection, AED currency, tails-only settings and the honest unconfigured 400 all verified on a booted app; visually QA'd at 390/1280.

- **Real payments: Stripe + PayPal in any app, keys pasted in-app (2026-07-03)**: the `payments` scaffold module ends the checkout-stub era. Any shop/booking/service app now ships with **Stripe Checkout (hosted) + PayPal Orders fully wired** — dependency-free (both providers over plain REST), with the flow that makes it safe and seamless: totals always computed server-side; **payment truth is server-verified** (the confirm step retrieves the session / captures the order against the provider's API — a client can never mark itself paid); no webhooks to configure (confirm-on-return); orders move new → paid → confirmed → fulfilled. The owner's only action: open their app's **Payments page and paste their keys** (Stripe secret key; PayPal client ID + secret with a sandbox/live toggle) — online payment turns on instantly, no code, no redeploy; money lands in THEIR account. Secrets are write-only (the API returns booleans + 4-char tails, never keys) and credential endpoints are rate-limited. Until keys exist, checkout gracefully stays the recorded-order flow — never a fake checkout — and a provider error surfaces its actual message ("Invalid API Key provided…") so the fix is obvious. Validated live end-to-end without real keys: unconfigured → honest 400, key save → providers flip on, settings GET leaks nothing, a bad key hits Stripe's real API and returns Stripe's own error; storefront + Payments page visually QA'd at 390/1280. The archetype router now routes "stripe/paypal/take payments" briefs to the module, and the prompt doctrine changed from "payments are stubbed" to "payments are REAL — tell the owner where to paste keys, test mode first."

- **Android scaffold system: modules, a 23-component kit, and a pre-build gate (2026-07-03)**: the web-scaffold doctrine ported to native Android so large builds are better, quicker and first-go-accurate. (1) The **mobile UI kit grows from 9 to 23 components** — Header, ListRow, SearchBar, Chip, SettingRow, SectionHeader, Divider, Avatar, Banner, FAB, ProgressBar, bottom Sheet, and a Toast system (mounted in the template root) — all token-driven, light+dark, pressed/disabled states, so screens are composed, never hand-rolled. (2) **create_expo_app gains capability modules** (deps auto-resolved): `tabs` (themed bottom tab bar + settings screen — the structure for any multi-surface app; replaces the root screen so routes never clash), `auth` (typed API client with token storage + AuthProvider + a complete sign-in/sign-up screen wired to the add_app_backend contract), `crud` (exemplar entity on local SQLite — offline-first list/detail/edit to clone per real entity; the untouched exemplar is a gate-flagged seam like the web scaffold's), `scanner` (camera + QR/barcode with the full permission flow and a result sheet) — each with SDK-52-pinned native deps merged into package.json. The scaffold writes a binding **.arksai/CONTRACT.md** + verify manifest. (3) **build_apk runs a deterministic pre-build gate**: `tsc --noEmit` + a local `expo export` Metro android bundle BEFORE any build droplet is started — a type error or broken import comes back in the tool result for a same-turn fix instead of a ~10-minute failed remote build. Proven end-to-end on the assembled all-modules app (install → tsc → bundle green) — and the gate immediately caught **three real latent template bugs** (literal palette types that rejected every brand accent under tsc, and missing expo-asset/expo-font that broke bundling), now fixed at the root.

- **Developer APIs, live WebSockets, and a nightly pipeline canary (2026-07-03, SCAFFOLD_PLAN Phases 3+4 core)**: (1) `scaffold_app` gains **base:"api-only"** — a pure JSON API skeleton for webhooks/integrations: API-key auth (first key printed exactly once at seed), an exemplar resource to clone, a **self-documenting index page** rendered from the code's own endpoint manifest (the API's truthful front door), and a browserless verify.json (the gate asserts the key gate 401s, the docs page, and health — no walker needed). The archetype router sends developer-API briefs here automatically. (2) **WebSocket upgrade forwarding**: the fetch-based /apps proxy couldn't carry an UPGRADE, so a published app's WS died at the front door — a raw upgrade listener now pipes the handshake to the app's port (prefix stripped, Host rewritten); the Phase-4 test passes (101 + correct RFC 6455 accept key round-trips; unknown slug → clean 404). SSE stays the default transport; chat/multiplayer/live-cursor apps now also work at their published URL. (3) A **nightly scaffold canary** runs on the live scheduler (03:30 Dubai): scaffold → domain-clone → gate → publish daily, ending with a plain-language alarm if any step fails — scaffold rot pages the operator instead of a customer finding it.

- **First-go numbers: spreadsheets audited at creation, analysis figures always computed (2026-07-03)**: the "perfect on the first go" pass for numeric deliverables. (1) **generate_spreadsheet now runs the quality gate's deterministic audits INSIDE the tool call** — a hard-coded financial model (literal totals instead of live formulas), an empty statement sheet, banner rows or error cells come back in the tool result itself with the exact defects, so the model fixes them in the same turn instead of after a gate round-trip (append-mode calls note the audit re-runs each call, so sheet-by-sheet builds aren't falsely flagged); a clean workbook reports "audit-clean". Locked by tests (a hard-coded P&L is flagged in-result; a formula-driven sheet passes). (2) **Analysis correctness is structural**: the report protocol's first rule is now that every figure derived from provided data comes from `query_spreadsheet` (SQL over the real rows) or `compute_financials`, quoted exactly — mental arithmetic over a file is forbidden; the tool description carries the same mandate for every mode. `query_spreadsheet` also gained **.json record support** (API exports analyze like any sheet) and a robustness fix: Python now runs with a safe sys.path (`-P`), so a stray .py file in the workspace can no longer shadow pandas' imports and kill the tool with a bogus "missing deps" (a real reproduced failure). (2b, 2026-07-03) **The formula-accuracy auditor** (`sheetAudit.ts`) deepens the same guarantee: every generate_spreadsheet call is now checked in-turn for DANGLING references (a formula naming a missing sheet — with the intended tab suggested on a near-miss — or a single cell beyond a sheet's real extent, the banner-shift off-by-one), DOT-notation refs (Assumptions.B10), and **cached-value mismatches** — where the model's {f,v} disagree >1%, the formula usually is wrong, and the exact cells are named in the tool result. Validated against live model output the day it shipped: in a 7-model bake-off (EXCEL_BAKEOFF.md) it caught 8+ wrong cached values each from two frontier models in-tool while passing the two clean ones — and the bake-off found **seed-2-0-pro (our fast lane) is the most accurate AND fastest multi-sheet Excel brain on the account** (audit-clean 24-month 4-sheet model in 96–161s at half everyone's tokens; 4/4 on multi-sheet big-data SQL in 16–21s, where 6 of 7 models scored perfect — analysis accuracy is solved, speed is the differentiator). Big-data write speed: cosmetic styling passes cap past 5k rows (120k-row generation 12.3s → 9.1s, no visual change on normal sheets). (3) The **archetype router** (same push): the plan gate now shows the deterministic architecture pick — "Multi-tenant SaaS → scaffold_app + orgs, crud, dashboard" — mapped from the brief (negations like "no accounts needed" respected), so the user sees the architecture before the build and the build starts on the right base.

- **B2C/commerce scaffold modules + data that survives republish (2026-07-03, SCAFFOLD_PLAN Wave A)**: three premium modules extend `scaffold_app` beyond team-workspace SaaS to storefronts, service businesses and content sites — all correct by construction and boot-proven through the full gate. **catalog**: products (public browse/search API + management page), a client cart, and **server-priced checkout** (totals computed from the database — a tampered client price is ignored; validated live), orders with a confirm→fulfill→cancel flow; payment collection is an honest order-request seam (pay on invoice/delivery), with the provider hookup point documented in code — never a fake checkout. **booking**: resources with opening hours + slot lengths, a per-day availability grid, and **conflict-safe reservations** — the pure slots engine (`server/lib/slots.js`, half-open intervals so back-to-back bookings work) plus a transaction-guarded insert (two clients racing for one slot: exactly one wins, verified live with a real 409); cancelling frees the slot. **cms-lite**: markdown posts with a dependency-free **XSS-safe renderer** (HTML escaped, `javascript:` links stay inert — tested), a write/preview/publish editor, and a public read API where drafts 404 until published. Each module declares its own verify.json checks (the golden all-modules app passes 15 route assertions + SSE + the signed-in walker — which caught and forced a fix to a real 390px truncation defect in the catalog admin page before it ever shipped). And the standing publish gap is closed: **a republish now preserves the live app's `data/`** (SQLite db + uploads, plus Prisma db files) — the previous deployment's user records are stashed and restored over the fresh snapshot, so shipping a fix never wipes what users saved (idempotent migrations/seeds make the preserved database boot cleanly under new code).

- **Production-complete, not a demo — the delivery bar (2026-07-02, operator doctrine)**: "we don't want a working demo or MVP — we want the actual complete production-level app," enforced in three layers, all inside the existing single bounded review (no new loops). (1) **The scaffold base is production-grade out of the box**: security headers (nosniff, frame-deny, referrer policy) + a terminal JSON error handler (never an HTML stack trace) in `server.js`; a dep-free fixed-window **rate limiter on the credential endpoints** (429 + Retry-After); email-format + length validation on signup; and a complete **Account page** in every scaffolded app — profile rename (`PATCH /api/auth/me`) and password change (`POST /api/auth/password`, verifies the current password; the shared demo account's password is fixed so the gate's login can never be broken by a user). The verify manifest now also asserts the authed `/api/auth/me` route, so "login works" is proven at the route level on every gate run. (2) **The gate scans for demo-grade seams** (`auditProductionSeams`, wired into the same web-hygiene audit the self-check and verify gate already run): "lorem ipsum", visible "coming soon"/"under construction"/"not implemented" copy (string-literal/JSX-text only — a code comment or a vendored library's internals never false-positive; minified bundles are skipped by an average-line-length heuristic), unreplaced `__APP_*__` tokens, walls of dead `href="#"` links, and — for scaffolded apps — the **untouched scaffold fingerprints**: delivering the generic "Items" exemplar or the scaffold home stub fails the gate with a named defect telling the model to do the domain work. (3) **The prompts state the bar up front**: a PRODUCTION-COMPLETE block in the code prompt + the Definition-of-Done (every briefed feature fully implemented; every control does its real action; loading/empty/error states; real copy; a capability that genuinely can't be production-real here — live payments, real email without SMTP — is built completely up to the seam and stated plainly, never silently faked; scope is controlled at the plan, not by delivering half). Validated live: a fresh all-modules scaffold booted with headers/limiter/account flows all exercised (renamed profile, changed a real user's password, demo change refused, 25 bad logins → 429), passed the full gate structurally, and was flagged by the seam audit on exactly the two scaffold fingerprints — which cleared the moment the domain work was simulated.

## Orchestration & engines
- **ArksAI Video — Seedance-powered video generation with a draft→final ladder (2026-07-02)**: `generate_video` now runs on **ArksAI Video 1.5** (Seedance 1.5 Pro — 4–12 s, up to 4K, **native synchronized audio** by default) with **ArksAI Video 2.0** (Seedance 2.0 — reference/edit/extend, 4–15 s) selectable, behind branded names only. The required flow is **draft-first**: a cheap 480p draft (~2 min) the user watches and approves before the 1080p final ever renders (4K only on explicit ask) — cost control and quality control in one mechanism. A deterministic **video prompt compiler** (`videoBrief.ts`) turns a casual ask into the granular, director-grade shot plan the model responds to best — following the official Seedance guides: **subject + action → exactly ONE camera move → lighting → style → audio → negative constraints**, ordered so the model "locks" the frame before inventing motion. It only *adds* a layer the author didn't already give (never fights an explicit instruction): it picks a fitting single camera move (push-in for people, orbit for product heroes, aerial rise for landscapes — never multiple conflicting moves, the top jitter cause), always names a light (the highest-impact lever), sets a premium cinematic grade, puts spoken dialogue in quotes (the synced-audio signal), and appends the right negatives (anatomy stability — "no warped faces/hands, no bent limbs" — when a person is in frame; "no temporal flicker" on longer clips). So the user only ever writes *what happens* and gets a professional prompt automatically. Durations respect each model's verified range — **up to 12s on Video 1.5, up to 15s on Video 2.0**. New `engines/seedance.ts` async task client (create/poll/cancel/download — the temporary video URL is downloaded immediately because it expires; every capability empirically verified against the live API, see SEEDANCE_PLAN.md). Key-gated on BytePlus: without the key, the legacy Hailuo path is untouched. Two surfaces make it usable end to end: (1) an **in-chat video player card** — whenever a run produces a clip it renders inline as a playable `<video>` (HTTP range/seek support on the file route so it scrubs and plays in Safari), with a **Draft** or **Final** badge; a draft offers one-tap **Render final** + **Tweak**, a final offers **Download** (all prefilled chat messages, no typing). (2) A dedicated **Video studio** surface (`/video`, 🎬 in the sidebar, mirroring the Android page): a brief composer (scene + optional spoken dialogue), **one-tap direction presets, each with an SVG icon showing what it does** — look (11), **camera move** (15: push-in / pull-out / pan / tracking / orbit / aerial / crane up·down / zoom / dolly-zoom / whip pan / FPV / handheld / fixed) and **lighting** (13: golden/blue hour / soft daylight / hard sun / studio / rim / spotlight / neon / backlit / candlelight / overcast / moody), each defaulting to **Auto** (the compiler picks the ideal setup per scene) so a non-expert gets a perfect shot untouched while anyone can direct it themselves — a chosen preset is injected as the exact professional phrasing, and because only one camera move is selectable the steady "single-move" rule is enforced by construction — **a Character feature — real people, actual photo and likeness** (upload a person/mascot photo → they star in the video; add a spoken line and they **lip-sync** it. **Real-person policy, verified live against the API on 2026-07-02 with the exact photo a user had rejected:** the provider declines real-person photos on *identity-reference* (both roles on Video 2.0 — its anti-deepfake line), but **officially supports a real person as the FIRST FRAME on Video 1.5** — so ArksAI animates the person's ACTUAL photo (the clip starts on their pixels = exact likeness, and it talks with lip-sync; a real talking clip of the previously-rejected photo was rendered end-to-end as proof). The `generate_video` tool now **auto-routes**: a "may contain real person" rejection retries once on the supported 1.5 photo-animation path, so the user never sees a refusal; the steering explicitly forbids suggesting face-obscuring or any moderation evasion — if the provider declines an image outright the agent says so plainly. Illustrated characters/mascots/products still use `reference_images` on Video 2.0), **optional image inputs** (a **start frame** the clip opens on, an **end frame** it lands on — start+end animates a controlled transition — and up to 4 **reference images** whose subject/style the model keeps consistent, best on Video 2.0; the studio uploads them and wires them to `generate_video` as the ModelArk `first_frame` / `last_frame` / `reference_image` roles), format controls (aspect ratio · duration · model — Auto / ArksAI Video 1.5 / 2.0 · native-audio toggle), and a cross-session **library** of everything you've generated, newest first, each a playable card that jumps back to its chat. A **Product video mode** (Scene ↔ Product toggle) turns a product photo into a commercial: the product's ACTUAL photo is animated (first-frame fidelity — it stays exactly yours), staged on a chosen commercial backdrop (**20**: studio white/grey, dark luxury, colour pop, gradient, marble, silk & fabric, water splash, ice & frost, smoke & mist, stone & slate, warm wood, botanical, desert sand, floating/clouds, industrial concrete, neon night, festive, lifestyle, tech) with a hero-shot plan (centered, one steady move — orbit by default, studio light, an end beat settling on the hero pose), an optional spoken tagline as voiceover, and premium ambience. A **drag-and-drop shot sequencer** (Scene AND Product modes) chains multiple camera motions into ONE continuous clip — e.g. aerial over the coast → zoom in on the car → pull out — as ordered beat cards (motion + "what we see"), draggable to reorder, each showing its computed time slice; 2+ beats compile into a timed shot-sequence brief (steered to Video 2.0, whose multi-shot rendering is native) and override the single camera move — the "one steady move" rule applies within a beat, not across the sequence.
- **Product-ad engine — the product is found, cut out, and staged; every kind of product gets expert ad language (2026-07-03, operator directive)**: the Product video mode now runs a real commercial pipeline instead of just animating the raw photo. (1) **Background removal that focuses on the product**: `generate_video` gained product parameters (`product_photo`, `product_name`, `product_category`, `ad_template`, `backdrop`, `focus_product`) — the uploaded photo (any background: kitchen counter, shop shelf, anywhere) is segmented with **rembg/U2Net** (`productShot.ts isolateProduct`, EXIF-aware, with honest graceful degrades: too-small photo, low-contrast mask, rembg not installed, model not downloadable — each reported in plain language and the original photo staged as-is), then the cutout is **composited onto a studio backdrop in headless Chromium** (20 CSS scenes shared with the client tiles; floor gradient + drop shadow + a masked reflection only when actually isolated) and the clip **opens on that clean staged frame** (`first_frame_image`), so the product in the ad is pixel-exact yours on a professional set. (2) **A researched per-category ad-template catalog** (`shared/productAds.ts`, pure data shared by server + client so they can never drift): 10 categories — skincare & beauty, food & beverage, fashion, jewellery & watches, electronics, fragrance, furniture & home, fitness, automotive, everyday goods (FMCG) — with **51 director-grade ad styles — 4 bespoke per category plus 11 universal formats every product can wear** (the unboxing, shot-on-a-phone UGC, zero gravity, satisfying ASMR close-up, before/after transformation, **problem→solved DR arc**, **speed-ramp hype cut**, **family line-up**, celebration gift, miniature world, retro film; universal keys resolve with or without a category, bespoke keys win on collision — locked by tests), each a timed shot plan with its own lighting, audio and per-category negatives. **Product RANGES stage as a real line-up**: `generate_video product_photos` (2–4 paths, first = hero) isolates each product and composites them shoulder-to-shoulder on one backdrop — hero center at full size, siblings flanking at 0.82, per-product shadows + reflections (explicit pixel slot widths after a found-by-eyeball flex sizing bug; validated on a real 3-variant raster) — defaulting the ad style to family-lineup. And a **modular art-style catalog** (`shared/videoStyles.ts`, 24 styles in 3 groups: Photoreal camera looks / Animated & illustrated (3D feature, 2D cartoon, anime, claymation, papercraft, watercolour, comic, pixel, low-poly, ink sketch) / Stylized worlds (cyberpunk, synthwave, fantasy, toy world, surreal)) powers the Scene mode Look picker AND described-product ads (grouped chips, click-to-clear); every phrase is written to trip the prompt compiler's has-style guard so a chosen style is never overwritten by the default grade (locked by test). Photo-staged product ads stay photoreal by construction — the clip opens on the product's real pixels: skincare gets texture-and-application, ingredient story, freshness splash and dark-luxury gold sweep; jewellery macro sparkle, worn elegance, gift-box reveal and dark-water reflection; automotive the studio reveal-and-drive, night-city neon, macro detail-craft and off-road grit; electronics keep screens an abstract glow (never fake readable UI), plus lifestyle desk and neon product-drop energy; FMCG pack hero, daily ritual, stop-motion play and colour-pop. `productAdBrief()` compiles category + template + backdrop + tagline into the timed shot plan (duration clamped to the models' 4–15s, beats split across the runtime, "the product must stay exactly as photographed — never redesign it"). (3) **The Video studio's Product flow** exposes it all: photo tile with a **"Focus on the product (remove background)" toggle** (default on), a **kind-of-product grid** + per-kind **ad-style cards**, and backdrop tiles that preview the real gradient scenes; the brief instructs the agent to pass the exact tool parameters. Docker installs rembg + pre-fetches the U2Net weights (non-fatal, LibreOffice pattern; shared `U2NET_HOME` so the non-root server reads root-fetched weights). Isolation + composition + brief compiler validated live in-sandbox (busy-background jar → clean cutout → staged on studio-white/dark-luxury/neon-night at 16:9 + 9:16); catalog integrity, brief timing, and frame HTML locked by tests.
- **Multi-scene STORY videos — plan, generate, stitch (2026-07-03, STORY_PLAN Phases 0–2)**: describe a sequence once ("a man walks the desert → zoom to the phone showing the 'pick up by ecosine' button → a helicopter drops a car → a suited driver opens the door") and `generate_video_story` plans the scene flow (LLM planner + pure guards: ≤8 scenes/≤60s, 4s floor, per-mechanism ceilings, scene 1 never chains, ≤2 consecutive extends, surfaced-never-silent truncation, fail-open heuristic splitter), generates each scene with the right **continuity mechanism** — fresh shot (Video 1.5, drafts), **pixel-exact composited opening frame** for any on-screen text/UI the story requires (Chromium raster → i2v; the probe kept "Pick up by ecosine" legible while the model staged the phone INTO a desert), **frame-chain** hard cuts (previous scene's last ffmpeg-extracted frame opens the next), and **extend** same-take continuations (2.0-fast r2v via a 15-min token-gated public clip URL — `/api/video-src/:token`, path-locked, range-supported, red-team tested) with optional cast/product reference stills — then **stitches ONE video** (lossless concat by default, optional dissolve). Provider's output-copyright filter (hit live in probes) walks a bounded ladder: one LLM reword → mechanism downgrade to a 1.5 cut → the scene ships failed WITH an honest note, never a dead run. Draft-first at the story level with a **manifest**: retake ONE scene (`retake_scene` + note — dependent scenes re-chain automatically, everything else is reused) or promote to a 1080p final; per-scene usage tokens drive real cost lines. Chat steers sequence-shaped asks to the story tool automatically. Mechanism routing is probe-locked (SCENES_RESEARCH §8); full pipeline dry-run validated against real clips/anchors/stitch. **Phase 4 polish**: an optional **continuous music bed** (Suno instrumental generated once per story, looped and mixed under the diegetic scene audio, sidechain-ducked beneath the voiceover), **continuity vision QC** (each frame-chained cut's boundary frames are hstacked and judged — a gross mismatch earns ONE bounded auto-retake per pass), **fade-black** joins the transition menu, **4K finals** behind an explicit ask, **burned captions** of the voiceover on ask, and an **opening_frame** bridge (a staged product frame anchors scene 1 — "make this product ad a story"); the Video studio gained the third **Story** kind (story box, cast strip, look carried across scenes, cuts/dissolves, total-length chips).
- **Tiered quality review — blocking vs cosmetic (operator doctrine)**: the design/publish review now classifies every defect (`isBlockingDefect`, pure + tested). **Functional and accessibility problems** (overflow, unreadable/contrast, dead controls, crashes, failed requests, blank pages) still hard-block and drive a bounded fix round listing ONLY those items; **taste** (palette, "more distinctive", spacing, font choices) is delivered as notes and can never open a fix loop — the cure for a real $15 live run that got stuck restyling an auth page and downloading fonts while the app had passed 8/8 functional tests.
- **One engine: MiniMax.** ArksAI runs entirely on MiniMax — the LLM brain (M3 + the fast M2.7-highspeed model) and all multimodal capabilities (vision, image, speech, video). No model menu for everyday use: the chat picks the model itself.
- **ArksAI Auto (the default)** — picks the concrete model per task: **M3** for quality/visual/agentic/report/legal work, the **fast model (Flash)** for quick/light turns; escalates Flash → M3 when verification needs more muscle. Server-authoritative cost in the footer. Power users can still force **ArksAI Max** (M3) or **ArksAI Flash** (M2.7-highspeed) via `/model` or the advanced New-session dialog.
- **ArksAI Max** — MiniMax M3. Called via M3's **Anthropic-compatible endpoint** (thinking off by default → fast, decisive tool use; the OpenAI surface forces unbounded thinking and stalls), through an adapter that keeps the rest of the agent loop unchanged. **Never hangs, quality kept first:** the rare long-build "hang" was root-caused (raw-API probing) to **server-side starvation when several M3 turns run at once** — the losers get keepalive pings (so a gap-based timer never trips) while producing no output. Fixed with **(a) a global concurrency limiter** (caps simultaneous M3 streams so they can't starve each other; excess turns queue), **(b) a content-based stall detector** (the idle timer resets only on *real* content, not pings, so a starved no-output turn is caught and the run auto-switches **within MiniMax to the fast model** to finish), and **(c) a quality-first policy on big outputs** — large single-shot generations (a full spreadsheet/deck/doc) are now *allowed to finish on M3* rather than bailing early to a weaker model (a head-to-head bake-off showed M3 completes a detailed 3-statement financial model with hundreds of live cross-sheet formulas, where the alternative didn't finish). A turn that's genuinely large surfaces a **"still working — hang tight"** note so a long-but-healthy build never looks frozen. **Quality-first stall recovery is per-turn:** a single slow M3 step finishes on the fast model for *that step only* and the run then **returns to M3** (each staged build step is small, and M3 references cells far more accurately on a financial model) — only after repeated stalls does the whole run drop to the fast model.
- **MiniMax capability tools:** `see_image` (vision), `generate_image`, `text_to_speech`, `generate_video` (Hailuo).
- **Suno** music engine — full toolkit, per-track cost: `generate_music` (AUTO mode from a description, or CUSTOM mode with a rich ~1000-char style field + structured lyrics; controls for vocal gender, style weight, weirdness, and negative tags to exclude styles), `generate_lyrics` (structured words only, no audio — free), `extend_music` (lengthen/continue a prior track), and `cover_audio` (AI-cover a source track in a new style). The agent is steered as a Suno expert (style = comma-separated components, vocal cues in parens, strongest line first) and confirms the brief before the first paid generation. Default model is config-driven (`SUNO_MODEL`).

## One-shot quality system
- **Auto-continue + harness checkpoints — long builds finish by themselves (2026-07-02)**: proven by a live method bake-off (a checkpointed multi-tenant SaaS build survived a manual kill AND a real deploy restart and resumed at the right step both times, while the un-checkpointed control died twice at the budget with nothing durable). Now baked in: (1) **harness-driven auto-checkpoints** — every verified-green gate pass is committed automatically (Replit-style durability that never depends on the model remembering the tool); (2) **checkpointed steps are the default** for standard AND heavy code builds; (3) **auto-continue instead of death** — when a checkpointed build exhausts its token window, the runner COMPACTS the context to the checkpoint ledger + remaining work and continues by itself (up to 2 extra windows, env `AUTO_CONTINUE_WINDOWS`) — the user never types "continue"; (4) the **wrap-up notice has teeth** — past 60% budget no NEW design-review rounds open (deliver-with-notes instead of the $6 revision spiral); (5) the **publish-proxy URL rewrite is idempotent** — a client URL already carrying `/apps/<slug>/` is left alone, killing the double-prefix bug that broke a live app's API calls — and the CODE prompt now forbids client-side prefix-detection and hand-rolled backend servers (use the pre-wired scaffold). All locked by tests. **The plan is visible while it builds:** a slim **Build plan** trail under the progress bar derives the ordered steps from the agent's own "Step N — …" headings / numbered plan and shows them live (✓ done · spinner active · ○ pending) the moment the plan is posted — no longer waiting for the first git commit — while the "N saved — resumable" tally reflects the real committed checkpoints as they land.
- **"One pass if it works" — the build doctrine (operator, 2026-07-02)**: the default shape of every build is exactly one loop — build complete → self-check ONCE → green = deliver. The agent iterates ONLY on a named, concrete defect (a failing check, a review finding, an error), never on a general "another look"; if an automated probe contradicts direct observation, it notes the discrepancy once and moves on instead of chasing the instrument. A genuinely large build applies the same rule per STEP (a short 3–6 step plan, each step one pass → one check → a durable git `checkpoint()`), with the old contradiction between the craft block ("write in one pass") and the checkpoint guidance ("don't do it in one pass") resolved into one doctrine. Supporting fixes: the **UI inspector now detects state flips** (a theme toggle swapping `data-theme`/class, computed background/color changes, `aria-pressed` toggles) so working controls are never reported dead — the false positive that burned ~15 turns of a real $6 run — and its "no effect" message names the probe-blind-spot escape hatch; and runs get **finish-before-stop budgeting**: at 60% of the token budget a one-time "wrap up now — finish the current fix, check once, deliver" notice lands, so the hard cutoff (which used to kill runs mid-fix, shipping a defect the model was about to catch) becomes a rare last resort. All locked by tests (doctrine + inspector + budget notice).
- **Prompt-quality overhaul — ten systemic fixes from a full pipeline audit (2026-07-02, all locked by tests)**: (1) the **design-brief compiler now steers to DISTINCTIVE** (art-directed for the subject, restraint as a choice) instead of mandating the "minimal-muted" look the vision gate fails — the direct cause of revise-loop churn — and its AVOID list names the gate's exact fail patterns; (2) **unattended scheduled runs get a dedicated prompt block** (no user is present → never ask, proceed on stated assumptions, deliver completely) so a recurring task can never stall on a clarifying question nobody will answer; (3) **.docx is a first-class deliverable** with a real protocol (title block, strict heading hierarchy, table standards, PDF-grade substance) — the old "say it won't be as richly designed" bar-lowering line is gone; (4) **creatives are QC'd for subject fidelity** (right people/landmark/product, no garbled anatomy) and baked-in text — with machine verdicts (SHIP / REVISE:SUBJECT / TEXT_IN_IMAGE / LEGIBILITY) where every class triggers a bounded corrective action, plus call-sheet-specific imagery-prompt guidance in the tools; (5) **one canonical page-fill rule** (≥60% floor, 85–100% target) across both document protocols; (6) **one canonical intake rule** (one round, the fewest questions that unblock — usually one, never more than four; scheduled runs never ask) replacing four conflicting budgets; (7) **number-format craft for charts + spreadsheets** — abbreviate big values (155K/2.4M), unit/currency stated once, and a new `currency` option on generate_spreadsheet so an AED brief never ships $ (per-column numFmt override too); (8) **native mobile builds get their own design core** (RN type scale, 8pt grid, 44pt targets, tab bar not hamburger, dual-theme contrast) that explicitly supersedes the inapplicable web-CSS rules; (9) **revise/failure messages explain HOW to fix** — the web design revise points at tokens/direction/component-level targeted edits, and a failed publish gets an ordered diagnosis path even when no verify detail exists; (10) the **bare expertise standards uplifted** (finance/people/data dashboards, tech-doc, team tracker) to the structure→method→measurable-bar→common-failure pattern. All ten are locked by `promptAudit.test.ts` so they can't silently regress.
- Task classification (`taskProfile`) → deliverable type + isVisual + tier.
- **Art-direction-first design — bespoke per subject, not a house template** (`design_direction`) — the biggest quality lever: before a brand/marketing/portfolio site is built, the agent **locks a bespoke design concept grounded in the subject's real world** (an immigration consultancy → "Port of Entry", travel documents — where the page *structure* encodes something true, e.g. real country/airport codes, never generic 01/02/03), commits to a **deliberate type trio** (display / body / a **mono DATA face** for codes·labels·figures — the cheapest tell of expensive editorial design), a **concept-grounded palette with a rationale**, and **one meaningful signature element**. It writes a durable, inspectable artifact (`design-direction.json` + `DESIGN.md` + a generated `tokens.css`) that the build reads and the design gate **enforces** — so the look holds across a long build instead of drifting back to defaults. The agent **names and avoids the AI-default looks** (generic minimal-muted blue-on-white, cream+serif+terracotta, black+acid-green, broadsheet hairlines) — "minimal · muted" is now **one deliberate option, not the mandated house style**. For a quick ask it runs silently with a sensible concept; for a brand it surfaces the one-line concept first.
- **Curated design menu (the quick-pick fallback)** — when a full art-direction pass isn't needed, the bundled UI kit still offers a restrained menu: **5 complete design kits** (`data-kit`: minimal/paper/linen/calm/harbor), **10 muted colour themes** (`data-theme`: slate, ink, indigo, ocean, sage, teal, clay, stone, plum, olive), and **10 typography pairings** (`data-type`: geometric · editorial · clean · startup · tech · journal · warm · humanist · **grotesk** · **institutional** — the last two adding deliberate faces + a mono role). All from **20+ self-hosted typefaces**, curated toward a **smooth, calm, uniform** set — calm/vintage serifs (Source Serif 4, Lora, Newsreader) + clean sans (Inter, DM Sans, Manrope, Plus Jakarta, Outfit) + a mono data role (**IBM Plex Mono**, **Space Mono**); the agent is steered AWAY from irregular/spiky display faces, because distinctiveness comes from the concept + palette + signature, never a loud font (no network needed) plus a new **`craft.css`** of high-craft primitives + meaningful **signature components built robust by construction** (`.eyebrow-rule`, `.board`, `.spec`, `.stamp`, and a clip-safe **`.ticket`** boarding-pass/receipt card — overflow-hidden, no rotation traps, stub that reflows instead of colliding on a phone) so an ambitious signature is *composed from a proven component* instead of hand-built and debugged for twenty rounds. It also ships a **reduced-motion-safe motion layer** — `.nav-underline`, `.lift`, `.tilt`, `.sheen` (CTA light-sweep), `.with-arrow`, `.float`, plus the scaffold's `site.js` wiring **`[data-reveal]` scroll-in + `[data-count]` stat count-up** for free, and a curated **hero-engagement** set (`.ken-burns` background drift, `.gradient-drift`, pointer `[data-tilt]`, and a LIGHT transform-only `[data-parallax]`) — the agent picks **one** that fits the brand (never forced, never heavy scroll-jacked parallax), all **desktop-pointer-gated and auto-off under reduced-motion**, transform/opacity only so there's no jank — so pages feel alive, not static. **Modern techniques, used with restraint**: a legible-by-default **glassmorphism** utility (`.glass` / `.glass-nav` — frosted with `backdrop-filter`, near-solid fallback so text stays AA, and a `prefers-reduced-transparency` solid path), a vendored **Open Props** subset (MIT, self-hosted — richer easings, 25 keyframe animations, 30 gradients), and **careful colour systems** (the locked palette now derives a proper accent **ramp** — `--accent-deep` / `--accent-2` / `--accent-tint` via `color-mix` — plus glass tokens, so the accent reads considered, not flat). Every technique is steered to stay **minimal · modern · polished · fully responsive** — a kit is a shortcut to the bar, never an excuse to drop below it. The menu is the muted fallback; `design_direction` is preferred for anything branded. Same system powers websites, reports and decks for one consistent identity.
- **Direction library — 40 tested modern design recipes the agent picks from** (`server/src/agent/directions.ts`): the answer to "everything looks templated / not modern enough." Where the curated menu above is the *smooth, muted* fallback, this is the **bolder, current, structurally-varied** set — **40 complete recipes**, each a **layout archetype + a font trio + an accent + a signature**, grouped as **18 modern product archetypes** (Linear dashboard, Apple bento, Vercel mono, Notion, Raycast command-bar, Stripe SaaS, Swiss grid, fintech, terminal, neo-brutalist, bold-media, premium-dense…), **8 glassmorphism levels** (clear → frosted → heavy → aurora → **Liquid Glass** (Apple 2025) → iridescent → tinted → mono), **8 structural archetypes** that change the *layout* not just the skin (calendar-heatmap, sidebar/rail, native bottom-tab, kanban, swipe-cards, timeline, focus-stepper, masonry), and **6 fresh aesthetics** (Frutiger-Aero/Y2K-aqua, cyberpunk-HUD, claymorphism, risograph, editorial-luxury, synthwave). Injected as a compact grouped catalog into the design context for visual builds; the agent **picks the ONE direction that fits the subject** (varying it across builds so output is modern + varied, never one default), applies its exact font trio via a Google-Fonts link (these vetted recipes intentionally unlock bolder faces — Bricolage/Unbounded/Bodoni/Sora — that the smooth-only list otherwise avoids), builds its **signature archetype** as the page structure, and derives the neutral system + AA-validated ramp from its accent. Two guards baked in: **originality** (invent a real brand name + copy grounded in the subject — never reuse a recipe's name) and **self-explanatory** (label every number with its meaning — "3 day streak", never a bare digit). Validated by building ~26 different app/website types across the library — habit trackers, a live-API restaurant guide, weather/recipe/budget/timer apps, dashboards with hand-built SVG charts, e-commerce+checkout, booking, music, real-estate, chat, email, video, social, calendar, fitness, and 9 website types — fast, modern, non-templated. `DIRECTION_LIBRARY` config, on by default. **Enforced, not just suggested:** the M3 design-gate rubric now *rewards committing to a recognizable modern direction/archetype + its signature* (a build with no discernible direction — a generic hero over a plain list — is a REVISE), and it no longer auto-flags a bold display face (Bricolage/Unbounded/Bodoni) just for being characterful — only when it actually hurts legibility or is off-concept — so the gate backs the library instead of fighting it. **`design_direction` can seed from a direction:** pass `direction:"cyber"` (or any recipe id) and it pre-fills the locked brief's type trio + accent + signature from that tested recipe (explicit args still win; a dark recipe seeds dark neutrals), attaches the fonts via a `tokens.css` `@import` so even non-embedded faces load without `add_fonts`, and the gate then checks the built page for fidelity to that locked direction.
- **Simple-build fast path — a small ask stays small + fast** (`server/src/agent/simpleBuild.ts`): the fix for a trivially-simple request (a counter, a tip calculator, "a little thing to track X") ballooning into 100+ tool calls and many minutes because the agent adds pages/features/state nobody asked for and re-polishes through every gate round. For a **light-tier code build** the pipeline goes lean: (a) an **anti-over-engineering** instruction is injected ("build the SIMPLEST version that fully satisfies the request, then STOP — one screen unless asked, no extra pages/settings/auth, sensible defaults over configurability; an over-built result for a simple ask is a FAILURE, not thoroughness"), (b) the internal **design-critique gate is capped to a single round** (vs 2), and (c) if the run is clearly over-building it gets a **one-time "ship the simplest working version now" nudge**. Model-agnostic — it changes the pipeline, not the model, so it works on the current stack (and the design system still injects, so simple ≠ ugly). Re-evaluated across a chat→code mode switch. `SIMPLE_FAST_PATH` config, on by default.
- **ArksAI Swift — a second model provider (BytePlus/Dola) for the fast lane** (`server/src/agent/router.ts` + runner `createOpenAIStream`): the runner is no longer single-provider. Alongside MiniMax (M3, via the Anthropic adapter) there's now an **OpenAI-compatible provider path** to **BytePlus's coding-plan endpoint** running **Dola-Seed-2.0-pro** (branded **"ArksAI Swift"**), validated this session across 126 builds to be fast (~40–90s) and high-quality on small builds. It streams the native OpenAI chunks the agent loop already consumes (no translation), and its tool-calling + streamed usage were verified against the live endpoint. Routing: a **light-tier CODE build auto-routes to Swift** (pairing with the simple-build lean pipeline) **only when BytePlus is configured**; a hard failure escalates once to M3, so a simple build never dead-ends on the fast lane. **Fully dormant until `ARK_API_KEY` is set** in the Droplet env — with no key, `resolveProvider` never returns the byteplus provider and simple builds stay on MiniMax (zero behaviour change). Flat-rate coding plan, so the cost bar uses a low nominal estimate; user-facing labels stay ArksAI-branded ("Swift"), never the underlying model name.
- **ArksAI Heavy — GLM-5.1 takes the heavy build lane, working plan-by-plan** (`router.ts` + `checkpoint.ts` + the CODE-mode craft block): a complex/heavy code build now auto-routes to **GLM-5.1** on the BytePlus coding plan (branded **"ArksAI Heavy"**), chosen by a live bake-off on an identical full-app brief where GLM-5.1 + the new discipline prompt one-shot a complete, correct, self-contained app for ~$0.11 while the previous heavy engine went 0-for-2 (a broken publish and a 26-minute non-converging loop). Three pipeline fixes underpin it: (1) a **CRAFT & RESTRAINT block** in the code prompt (plan once/write once, targeted edits in revise rounds, code economy + single-file preference, max two actually-used fonts, try/catch storage + focus rings + reduced-motion, and a pre-finish self-check for console errors / 360px overflow / working controls / persistence) — bake-off-validated to lift an open model to reference-level discipline; (2) **plan-by-plan checkpointed heavy builds ON by default** (outline ordered tasks → build one → git `checkpoint()` → verify → next, with a durable ledger + resume note so an interrupted build continues instead of restarting; `CHECKPOINT_BUILDS=0` disables); (3) the BytePlus stream deadline is now **idle-based** (re-armed on every chunk, so a healthy 9-minute single-pass write is never killed — the old fixed 240s total timer was aborting them) and `max_tokens` raised so a large reasoning-model file write can't be truncated mid-JSON (the silent cause of retry-loop token burn). M3 stays the escalation target and the report-mode engine. Other heavy coders (GLM-4.7, Kimi-K2.5, DeepSeek-V4-Pro, Seed-2.0-Code) remain selectable by id for future bake-offs.
- **Checkpointed long-build mode — big builds become durable + resumable** (`server/src/agent/checkpoint.ts` + the `checkpoint` tool): a large/complex build no longer runs as one fragile marathon that loses everything to a crash, stall, or restart. With checkpointing on, the agent is steered to work **task-by-task** — outline the build as ordered tasks, build ONE to a working state, then call `checkpoint("<milestone>")`, which **commits the workspace** (a real git checkpoint, git-init-ing if needed) and records it in a durable ledger (`.arksai/checkpoints.json`). If the run is interrupted and the session resumes, a **resume note lists the completed checkpoints** so the agent CONTINUES from the last one instead of rebuilding finished work — trading a single fragile pass for steady, recoverable progress. Opt-in (`CHECKPOINT_BUILDS=1`) while it beds in; the `checkpoint` tool is gated so it's invisible when off, and the large-build steering fires for heavy-tier code builds. (Phase 4 of the per-session execution-infra plan; Phases 1–2 shipped the atomic preview-port allocator, and the agent shell's always-on destructive-command denylist already contains the Phase-3 P0 — a build can't `pkill`/`kill $(pgrep)`/`fuser -k`/`shutdown` the ArksAI server. Full per-session container isolation + a dedicated compute worker is the remaining operator-gated cutover.)
- Opinionated design system + a bundled UI kit (tokens/components/themes, `add_ui_kit`) injected for visual tasks; per-type design standards (web/app, report, docx, xlsx, pptx). The design contract is **typography-first, minimal, and high-quality by default**: a real modular type scale + refined font pairing, a **distinctive confident palette** (not the generic "AI-default" blue) drawn from a **curated 28-palette swatch book** (`server/src/agent/palettes.ts` + the UI-kit `data-theme` set) — a deep **confident/vivid tier** (emerald, cobalt, grape, crimson, coral, amber, midnight… matched to the subject) alongside the quiet muted tier, **every accent pre-validated for WCAG-AA** against its button text and offered to the agent as a named menu so the colour layer is good *and* fast by construction (no more falling back to a generic grey-blue) — plus `validate_palette` for any custom pair, purposeful **micro-interactions** (hover/focus/enter, reduced-motion respected), real interactive/empty/loading/error states, and a senior-design-director self-critique. Enforced, not just suggested: the build gate runs a **deterministic mobile-responsive check** (renders at 390px and hard-fails a layout that overflows the screen), a **deterministic text-legibility check** (measures WCAG contrast on every text block vs its background — including copy placed directly in section bands, cards and heroes, not just `<p>` tags — and hard-fails washed-out muted text or borderline-low-contrast buttons that vanish into the page; a muted tone is allowed only while it stays legible) **including interaction states** (it actually hovers every button/link and re-checks contrast, so a control whose background changes on hover but whose text colour doesn't — making the label disappear — is caught, not just the resting state) **and BOTH colour schemes** (it re-runs the contrast measurement under emulated `prefers-color-scheme: dark`, so a half-baked dark theme — text token flips light but the page background stays light, invisible to a light-only check — is caught before it ships to the ~half of phones in dark mode; it also steers the agent to either pin `color-scheme: light` or implement dark mode fully, and to keep design tokens in one clean `:root` so `--accent`/`--ink` always resolve), plus the M3 design-director rubric — now upgraded to judge **distinctiveness FIRST** (competent-but-generic is a *fail*: it flags the named AI-default looks, rewards a deliberate concept carried through type+colour+structure and a meaningful signature element, and — when a `design_direction` is locked — checks the built page for **concept fidelity**), as well as any hard-to-read text over an image, so an output that isn't responsive, has illegible copy, or just looks like a template gets revised before it ships.
- **Design-brief compiler — turns a casual ask into an expert brief before the build** (`server/src/agent/designBrief.ts`): the per-request "think first" step (the planning a senior designer does in their head before touching the canvas). Auto-Brief deliberately skips visual work, so an artifact/app used to get the big *static* design system + the user's *raw casual* prompt and nothing in between — and a one-liner vs an expert spec produce wildly different output from the same model. Now, for a visual build, an **upfront bounded M3 pass rewrites the request into a tight, specific build brief** — concept (grounded in the subject) · a fitting palette **by name** · type roles (display/body/mono-data) · concrete layout · the ONE signature moment · real sample data · craft details · an **AVOID list** (emoji-as-icons, dead grey, generic centered-hero-glow, edge-bleed) — which the build then follows. Result: *"make me a little thing to track how much water I drink"* (no design hints at all) produced a floating card with a progress-ring signature, a fitting blue palette, mono tabular figures, an inline line-SVG drop icon and quick-add controls — Claude-quality from a one-liner, with **no hand-fed reference**. Fail-open (key off / error / timeout → the build proceeds unchanged); `DESIGN_BRIEF` config, on by default; emits an "understanding" beat so the upfront thinking is visible.
- **First-try correctness — compose proven blocks + self-check before finishing** (the answer to "latency"): the real cost of a slow build was never the model's thinking time, it was the **iteration to fix defects** — the agent would hand-build a fragile thing (an SVG progress ring, a 3-statement model, a stat grid), then loop through the verify/design gate to debug it. Two reinforcing fixes. **(1) Correct-by-construction blocks** the agent now composes instead of hand-rolling: a **`.gauge` progress ring** drawn by `stroke-dasharray` from a single `--val` (no rotation — the exact trap that used to burn 20+ inspect passes), a **`.stat` KPI card** (tabular figures + a currentColor up/down/flat trend arrow, no emoji), a **responsive `.table-wrap`/`.table`** (scrolls instead of overflowing a phone), and a **`financial-model` spreadsheet template** (`generate_spreadsheet template:"financial-model"`) that emits a complete Assumptions → Income → Cash-Flow model with **live cross-sheet formulas and a populated Cash Flow by construction** — so the empty-Cash-Flow / hard-coded-model defect can't happen. **(2) A cheap pre-completion self-audit** (`runCheapSelfAudit`) runs the deterministic, model-free defect checks (web hygiene — missing viewport / broken local asset refs; the xlsx formula/banner/section/numeric audits) **before** the expensive gate, so the agent fixes a known-bad result *in-context* with **no vision call and no full re-loop**; it's bounded by its own counter, independent of the verify/design budgets, and the gates still run afterwards as a backstop (now usually finding nothing). Paired with a front-loaded **Definition-of-Done** checklist injected into the system prompt (the exact bar the gate enforces, in the prompt up front) so the first build aims at it. Result: defects are prevented or fixed early, so the gates and inspect loops rarely fire — the build is right sooner.
- **Shared icon library — one beautiful Lucide set, everywhere** (`server/src/agent/tools/icons.ts`): a curated 120+ line-icon set (24×24, consistent 1.75 stroke) grouped by purpose — brand/lifestyle (coffee, leaf, gift, heart, sparkles…), nature/weather, communication (mail, send, phone, bell…), commerce (shopping-cart, tag, wallet…), data/charts, people, media, UI/navigation, places/travel and tech — used across **every surface**: websites & apps (shipped as a recolourable `icons.svg` sprite by `add_ui_kit`; reference with `<use href="ui-kit/icons.svg#coffee">` and tint via CSS `color`), reports & docs (inlined paths), decks (pptx KPI tiles), and social creatives. **One source of truth** (the sprite is generated from the icon library, with a drift guard test), and the available icon names are advertised to the agent in the tool results + design prompt — so it picks the *right* on-brand icon instead of hand-rolling mediocre SVGs or falling back to emoji.
- **Universal visual-quality gate** (`deliverableCheck`) — EVERY deliverable is rendered to image(s) and design-reviewed by a senior-design-director rubric (M3 vision), then bounded-revised: web apps (live Chromium), PDF reports/decks (mupdf raster, per page), .xlsx/.docx (HTML render), and .pptx (LibreOffice or a faithful preview). The same "looks perfect, works perfectly" guarantee across all types, not just web apps. **Reliable deck→PDF** (`convert_document`): converting a presentation to PDF renders the deck's faithful inline-SVG HTML mirror through headless Chromium, so charts and images come out crisp — sidestepping LibreOffice's habit of silently blanking embedded chart images on .pptx→pdf; plain office docs still fall back to LibreOffice, which handles text/tables well. A **deterministic, model-free structural pre-check** (per-page ink coverage **and per-page content extent**) catches both lonely near-empty pages **and under-filled pages** (content that stops high on the page leaving a big blank lower band — the ≥60% page-fill rule, which interior pages must meet; the cover and final page are exempt) instantly — even when no vision model is available — so each revise round is cheap and targeted (reports now allow up to 2 quality-first revise rounds). The gate also covers the cases that used to slip: generated **images** are validity-checked (a 0-byte / truncated / error-page download is rejected, not shipped); an **app built in report mode** gets a real runtime boot + smoke-test (not just doc rendering); the **formula audit** flags a typed-in derived row (Total/Growth/Margin) even when the rest of the model uses formulas; and if the visual review ever can't run (vision/renderer unavailable), it **says so out loud** instead of silently passing.
- **React + Tailwind path for complex apps (`create_react_app`)** — for a stateful, interactive web APP (a dashboard, a multi-view tool) where the React/Tailwind ecosystem earns its keep, the agent scaffolds a **Vite + React + TypeScript + Tailwind** app with **shadcn-style components** (Button/Card/Input/Label/Tabs/Dialog, all MIT, vendored into `src/components/ui`), pre-themed to the brand accent (hex → the shadcn HSL token), **self-hosted smooth fonts** (Inter + Source Serif 4, bundled by Vite with relative paths), and `base: './'` so it serves correctly at `/apps/<slug>/`. The existing **publish pipeline builds it** (runs the Vite build). Verified end to end (npm install + `tsc` + vite build + render). For a simple marketing/content site, the lighter vanilla `create_web_app` is still the default — the agent is steered to pick the right one. **Full-stack mode (`backend:true`)** also scaffolds a correct Express + SQLite server as ONE deployable service — the API routes (and an `/api` 404) are pre-wired BEFORE the SPA history fallback so `GET /api/*` always returns JSON (never the `Unexpected token '<'` trap), it reads `process.env.PORT`, and `npm start` serves the built SPA *and* the API on one port. Verified by booting it (CRUD persists to SQLite; unknown `/api/*` → JSON 404; client routes → index.html) and it's robust across Express 4 and 5. The canvas preview now RUNS that server for a full-stack app (so the API works in preview) instead of statically serving `dist/` on a colliding port.

- **Instant artifacts (`create_artifact`)** — turn a self-contained React component into a single, self-contained HTML file that renders **instantly with NO build** (the React runtime is vendored inline — no CDN, no npm install, no Vite). The JSX/TSX is compiled server-side (esbuild), the design tokens + a confident palette are baked in, and the component mounts to `#root`. The fast path for a small *visual* deliverable — a chart, calculator, interactive widget, a single landing section — it previews in the Canvas in ~1s and publishes as-is. Boot-verified: an interactive tip-calculator artifact rendered with zero console errors and live state updates. **Contrast is guaranteed** even though artifacts skip the build gate: a `theme:"dark"` option flips the whole token set to a coherent dark surface with **LIGHT ink**, and an always-on **runtime contrast guard** measures the background actually painted across the viewport and flips the ink tokens light/dark to match — so a component that paints a dark background while using the light text tokens can never render dark-on-dark (verified: a "focus timer" that previously showed near-invisible dark-on-navy text now measures 15.8:1 / AA, while a normal light artifact is left untouched). **Type is elegant + uniform by construction**: the artifact ships a cohesive font set **embedded as base64 woff2** — Inter (`--font-sans`, body/UI), **Space Grotesk** (`--font-display`, headings + large numerals — `h1–h4` use it automatically), **IBM Plex Mono** (`--font-mono`, tabular figures for timers/prices/stats), Source Serif 4 (`--font-serif`, editorial) — so the artifact renders in the *intended* faces on every device instead of falling back to a different system font each time (it previously referenced Inter/Source Serif but loaded neither). **Polish execution layer** (the difference between "clean template" and "expensive minimal"): a **bespoke TINTED neutral system** derived from the accent so bg/surface/ink/hairlines all share one undertone — warm accent → warm ivory, cool accent → cool slate — instead of dead grey (the single biggest polish tell); an **elevation scale** (`--shadow-sm/-md/-lg`) of soft, layered, accent-tinted shadows (lifted paper, not a hard drop shadow); an **optical type scale** (`--text-xs…-5xl` + `--tracking-*`) with per-size letter-spacing and tuned line-heights applied to headings by default; a radius scale; and refined **0-specificity defaults** (`:where(…)`) — a tasteful focus ring, smooth transitions, and a crafted look for an unstyled button/input — that any component style still overrides. In dark mode the neutrals tint to a deep accent-charcoal (not flat black) and the emphasis ramp brightens so accent-on-tint chips stay legible. Verified across warm/cool/dark palettes: the same restrained component renders polished and WCAG-AA in every case. (For a data-backed/multi-page app, `create_react_app` is still the path.)
- **Complete "looks" + a richer palette swatch book** — beyond the 10 quiet muted themes, a deep **confident/vivid tier** (emerald, cobalt, grape, crimson, coral, amber, midnight… 18 in all) and **9 complete "looks"** (`data-kit`: fresh, bold, studio, warm, tech, boutique, trust, vibrant, signal) that pair a vetted accent + a type pairing + a radius/shadow style in ONE pick — a high design floor instantly. **Every accent is WCAG-AA validated at generation**, the catalog (`palettes.ts` / `looks.ts`) is injected into the design prompt as a named menu, and the agent is steered to pick a *fitting, confident* colour by subject (verified live: a coffee-roastery build chose a warm burnt-orange `#b8541f`, not the old generic grey).
- **Real interactive components, self-hosted (`add_web_components`)** — when a site genuinely needs interactivity it would otherwise hand-roll (and then hand-debug), the agent installs a **lean, self-hosted Web Awesome / Shoelace runtime** (MIT, ~2.6MB — the TypeScript defs, React wrappers and the heavy Bootstrap-Icons bundle are stripped; component chrome uses an inline icon set) and drops in **`<sl-dialog>` (modal), `<sl-tab-group>`, `<sl-carousel>`, `<sl-tooltip>`, `<sl-dropdown>`/`<sl-menu>`, `<sl-details>` (accordion), `<sl-select>`, `<sl-switch>`** etc. in plain HTML — **no framework, no build step, no CDN**, lazy-loaded (only what you use is fetched). A **token bridge** maps the components onto the site's own `--accent` / `--font` / `--radius`, so they render minimal and on-brand (never Shoelace-default blue) and still pass the responsive + contrast gate. Verified to upgrade + theme correctly even when served from a subfolder like `/apps/<slug>/`. Gated behind the tool, so plain sites stay lean.
- **Animated hero backgrounds, fallback-first (`add_hero_fx`)** — when a brief truly wants an expressive, atmospheric hero (a **brand site, product/app launch, or creative landing page** — never a dashboard, booking flow or utilitarian tool), the agent installs a tiny **self-hosted Canvas-2D effect** (no CDN, **no WebGL** — 2D renders reliably everywhere, incl. low-end phones and headless CI, where WebGL silently blanks) and drops in one of three ambient backgrounds: **aurora** (a soft drifting gradient mesh), **particles** (a gently pointer-reactive constellation field), or **waves** (layered flowing lines). It's **correct-by-construction safe**: the animated canvas overlays a **solid `--fx-bg` colour** that is both the fallback shown when JS/Canvas is unavailable or the visitor prefers reduced motion, **and** the surface the legibility gate measures the headline against — so it can never blank the hero or hurt contrast. It caps device-pixel-ratio, budgets particle counts, and pauses when scrolled offscreen or the tab is hidden (no wasted battery). One hero per page, themed to your `--fx-accent`; a calm/corporate brand sticks to the free craft.css hero moments instead. Gated behind the tool.
- **React + Tailwind apps (`create_react_app`)** — for a complex, stateful, interactive web app, scaffold a **Vite + React + TypeScript + Tailwind** project with **shadcn-style components** (Button/Card/Input/Label/Tabs/Dialog in `src/components/ui`), self-hosted Inter + Spectral fonts, and a minimal token theme pre-set to the brand accent (hex→HSL). It uses `base:"./"` so the built app works under `/apps/<slug>/`, and `publish_app` runs the Vite build. The **in-canvas preview "just works": for any build-to-static frontend (Vite/CRA/Astro/Vue/Angular/Parcel/Gatsby) the preview builds the app and serves the static output** (relative-pathed) rather than a dev server — so it renders reliably through the canvas proxy with no dev-server/HMR setup (SSR frameworks like Next keep running their own server; node apps preview on their own port). For a simple marketing/content site, `create_web_app` stays the lighter choice.
- **Websites — correct by construction (`create_web_app`)** — every website starts from a proven, responsive skeleton (the web counterpart of `create_expo_app` for Android), so the classic mobile failures are *structurally impossible*, not just caught later: an overflow-proof CSS reset (box-sizing, `max-width:100%` media, `overflow-x` guards), a **working hamburger nav** wired in `site.js`, a `<meta viewport>`, fluid type + auto-fit grids, and Home/About/Contact pages — plus the UI kit (incl. `craft.css`) copied in for richer components. The scaffold is now **theme-agnostic**: the structural guarantees live in `site.css` (mechanics), and the *look* lives in a separate `tokens.css` — so applying a bespoke `design_direction` never fights the reliable skeleton (the tool **preserves a locked `tokens.css`** instead of clobbering it). The agent fills in real content + theme + one meaningful signature, never the fragile layout/nav. The bundled scaffold is verified to pass the full mobile gate (no overflow at 320/390/768, the menu opens, zero hygiene defects) out of the box, so a generated site is right on a phone from the first render.
- Per-type functional check ("it actually works"): xlsx re-open + no #REF/error cells **+ calculation models must be formula-driven** (a finance/cash-flow/budget/forecast sheet built with hard-coded totals/balances/growth is auto-flagged and sent back to add live `=SUM(...)`/cross-sheet formulas — the vision gate can't catch this since it renders computed values, so this reads the cells' formulas directly); docx re-open + embedded fonts; pptx valid slides; pdf page/blank/bleed; apps boot + routes + interaction. **A numeric-sanity gate** then catches a model that's formula-driven but whose numbers still can't be right (the cross-reference failure mode): an **absurd outlier** (a 140‑million line in a small-business model — a formula that landed on the wrong cell), a **rate/percent leaked into a money column** (a revenue cell reading 0.30 instead of 6,000), or a **derived row (Total/Net/Balance) computing to 0** while real numbers exist — and a **section/divider-row detector** flags a plain-text or em-dash divider ("SPACE & LEASE", "—— REVENUE ——") interleaved in a calc sheet, since it shifts every row and corrupts absolute references. Both are deterministic (no vision needed) and feed the bounded revise loop.
- Vision runs on M3's Anthropic endpoint (thinking off → fast, decisive; bounded by a timeout so it degrades, never hangs).
- Visual model floor — non-trivial visual/report work never runs on the cheapest model.

## Bulletproof verification
- Static gate (typecheck/lint/test/build) + runtime gate (boots the app, exercises routes).
- **Code-review gate (works on a connected repo, not just "does it build")** — after the static checks pass, a second model reads the session's **actual git diff** (committed + uncommitted, vs the commit the workspace started from) and flags real defects the change *introduces*: correctness bugs (wrong condition/operator, missing `await`, null/undefined), regressions (a changed contract that breaks an existing caller), security issues on a new path, and leftover debug/throwaway code. Concrete findings are fed back into the same bounded-revise loop the design gate uses (capped, **fail-open** so a flaky review never blocks a verified ship). The agent is also told to **review its own `git diff`** (new `git_diff` tool — full unified diff) before finishing. On by default (`AGENT_CODE_REVIEW`), reviewer model configurable (`CODE_REVIEW_MODEL`, defaults to the run's model — the brain stays swappable).
- **Repository primer — the agent starts grounded on an existing codebase** — when a real repo is connected, the system prompt leads with a cheap, deterministic **`## Repository`** orientation: detected stack + frameworks, the package scripts, a shallow file tree, and the top of `AGENTS.md`/`CLAUDE.md`/`README`/`CONTRIBUTING` — so it answers questions and makes edits like a careful contributor instead of discovering the whole layout blind via glob/grep.
- Parameterized-route filling (`:id`/`{id}`/`<int:id>`/`:slug`) + an interaction pass (seeds inputs, submits a form, clicks primary buttons).
- Headless-Chromium UI smoke test (blank/console-error/failed-request detection) — now runs for **static sites too** (a plain HTML site is served + browser-checked during the build, not just at publish), so a static website can no longer skip the gate.
- **Multi-breakpoint check** — horizontal-overflow and the mobile-menu test run at **320 / 390 / 768px** (small phone, phone, tablet), so a layout that breaks at any common width is caught, not just one.
- **Mobile-menu check** — at phone width the gate finds the hamburger/menu toggle, taps it, and **fails the build if the navigation doesn't open** (no nav links appear). Catches the dead mobile menu that a desktop-width pass can't see, with an actionable fix note for the agent.
- **`inspect_ui` — the agent diagnoses complaints like a developer, not by guessing** — when you say anything in a built site "looks wrong / is broken / doesn't work / the X button does nothing", the agent opens the live app in a REAL browser (desktop + 390px mobile), **looks** at it (vision describes each view), **clicks every button/toggle and reports whether it actually did anything or is dead**, reads the **console/network** (uncaught JS errors, 404 assets/links/APIs), checks **mobile overflow**, and — when you name a thing — reports that element's **exact computed state** (hidden? what colour on what background? what size?). It's told to run this BEFORE editing (so it fixes the real cause, not a guess) and AGAIN after, to verify the fix actually worked — ending the blind edit-and-hope loops that burned tokens. Works for any complaint, not just mobile. **It also knows when to stop**: a per-build inspection budget + steering caps the inspect→fix→re-inspect loop so the agent doesn't whack-a-mole one hard element for hundreds of steps — once it's looked enough, it's told to ship or **simplify** a fragile element (use a robust `craft.css` component instead of fighting CSS), because a clean version that ships beats a pixel-perfect one that never does. A flaky/closed inspection now retries once so a round isn't wasted.
- **Libraries aren't booted like apps; static sites aren't mistaken for servers** — the runtime gate only tries to boot a project when its entry point **actually looks like a server** (uses Express·Fastify·Koa·Hono / `createServer` / binds `process.env.PORT`). A *library* or CLI runs its static checks + its own tests and skips the boot; and a **static client app is never misread as a server** just because its JavaScript contains common idioms like `app.run()` or `history.listen()` — so a plain site no longer dead-loops on a phantom "booting the app" failure.
- **Database-backed apps actually work, end to end (SQLite by default, Postgres provisioned)** — when an app needs to persist data server-side, publishing handles the database for it. **SQLite** is the zero-infra default (ships as a file with the app); **Postgres is fully supported** — publishing **provisions an isolated per-app Postgres database** (its own role + database on the managed instance), **injects the connection string** into the app at runtime (via its `.env`, loaded by the deployment even if the app doesn't use dotenv, and surviving restarts), and runs the app's `prisma migrate deploy` (→ `db push` fallback) + seed. Per-app credentials are derived deterministically so a re-publish reuses the same database and **data persists**. The agent is steered to pick one database + one migration and let the platform wire it (no hand-set URLs, no dual-schema tangles). A not-yet-supported DB (MySQL/Mongo/Redis) returns a clear, actionable message instead of deploying broken — and the provisioning layer is built to add those drivers next. (Postgres provisioning activates when the operator sets `MANAGED_PG_ADMIN_URL` to the managed instance; until then DB apps use SQLite.) **No leaks:** when a 24-hour preview expires or is deleted, its provisioned Postgres database **+ role are dropped** (the janitor reaps them, like the Android build droplet is always destroyed); a SQLite DB is just the file in the deployment dir, removed with it. Re-publishing the same app **reuses its database** (data persists); a superseded slug's database is reaped so nothing accumulates on the managed instance. The verify gate also won't dead-loop "booting the app" on a DB app it can't boot in the build sandbox. **Published apps serve cleanly behind the path proxy** (verified live with an Express + Postgres guestbook that persisted real form submissions): (a) the build step runs at `NODE_ENV=production` even though install ran at development for devDeps — Next.js inheriting development used to break its /404,/500 prerender; (b) the reverse-proxy forwards **any** request body content-type (a normal HTML `<form>` POSTs `application/x-www-form-urlencoded`, uploads send multipart) — Fastify previously 415'd those before the handler, so no-JS form submissions failed and the post-publish smoke test (which submits the first form) **false-marked working apps as errored**, withholding a live link for an app that worked; both fixed. The agent is steered toward a proxy-friendly stack (static/SPA + a simple Express/Fastify API, or a server-rendered Express app) and away from Next.js App Router/RSC for published data apps, whose RSC navigation fights the `/apps/<slug>/` rewrite. It's also steered to deliver **both deployment surfaces at once**: a **live preview URL now** (publish_app builds the app and serves it on ArksAI's own DigitalOcean infrastructure at `/apps/<slug>/`) **and** a **DigitalOcean App Platform–deployable repo** (a committed `app.yaml`) that auto-deploys when the user pushes to git. To be servable by publish_app the app is built as a **single web service at the workspace root** (one Express/Fastify server serving the built frontend *and* the API — one `npm install`/`build`/`start`), which is *also* a valid single-service DO App Platform app — so both are true together. Only when an app genuinely needs a **multi-service** architecture (separate static frontend + API + managed DB as distinct DO components) that the single-path preview can't serve as one unit does the agent **ask the user to choose**: a live ArksAI link now, or the multi-service DO App Platform auto-deploy. Installs are resilient too: `npm ci` falls back to `npm install` when a hand-authored lockfile is out of sync, instead of failing the publish. **Review happens BEFORE publication:** a freshly published app boots into a `verifying` state, is smoke-tested through the real public URL, and is only promoted to **live** once it passes (a failure is surfaced up front, never a broken link marked live).
- **Bounded inspection (no whack-a-mole, faster builds)** — `inspect_ui` is for diagnosing a *specific* reported defect, not proactive nit-hunting: a soft nudge kicks in after a few passes and a **hard ceiling refuses further inspections** in a build, so a run can't spiral into a 20-pass, 18-minute polish loop. The agent is also steered to treat its own "probably a false positive / rendering glitch / sub-pixel" findings as DONE — and to rely on the automatic verification gate rather than re-inspecting. Same output quality, a fraction of the time/cost.
- **Deterministic web-hygiene preflight** — source-level checks that even a headless browser can't see (most importantly a missing `<meta viewport>`, which Playwright hides because it sets the viewport itself): flags missing viewport, a hamburger with no wiring, fixed min-widths wider than a phone, **and a referenced local file that doesn't exist** — a `<link>`/`<script>`/`<img>` pointing at a workspace file that was never created (it 404s at runtime, so its CSS/JS/image is silently missing and the page renders broken — e.g. the recurring "linked `landing.css` but only created `site.css`" bug, where the bespoke signature element + lists fall back to unstyled defaults). The gate hard-fails with the exact missing filenames so the agent creates them or fixes the reference, instead of shipping a page with a broken signature — so the basics gate even with no Chromium/vision/egress.
- **Installable PWAs, correct by construction (`add_pwa`):** "make it an app / installable / works offline" is now one fast, first-try step instead of a slow loop. `add_pwa` writes a valid `manifest.webmanifest`, a safe offline service worker (network-first navigation so updates show, cache-first assets, APIs passed through untouched), and **real generated app icons** (a branded vector icon always, plus 192/512 PNGs when possible — never a missing icon file), then wires the manifest link, theme-color, apple-touch-icon and service-worker registration into the app HTML (works for a plain static site and for Vite/CRA, where assets go in `public/`). The agent is steered to use it instead of hand-writing a manifest/SW/icons (the hand-rolled version reliably missed an icon or shipped a SW that broke the checks, sending the build into a long fix loop). The verify probe now **blocks the service worker** during checks, so a PWA's caching can never make verification see stale/blank content and loop — and the app is built to work fully without the SW (progressive enhancement).
- **Review BEFORE publish (no publish-then-fix):** calling `publish_app` now runs the full verify + design review *first*; the app only goes live once it passes. If the review flags issues, publishing is held and the agent fixes them before anything goes live — so the user never receives a link to a draft and never sees the app get reworked *after* it was "published". (Previously the agent could publish mid-build and the design review fired afterwards, producing a visible publish → rework → republish loop.) The completion gate is skipped when this pre-publish review already passed, so the work is never double-reviewed.
- Post-publish smoke test of the real `/apps/<slug>/` URL — a broken deploy is caught and handed back to the agent, never the user. **Publish failures are now ACTIONABLE, not a loop:** when a server app builds but never starts listening (the #1 cause of a 404 / "not running" published link), the failure handed back to the agent names the most likely cause (not reading `process.env.PORT`) **and includes the app's real startup log** — so it fixes the one true cause and republishes once, instead of blindly re-publishing + running `node -c`/`ls` for 15+ minutes (a real loop we observed). **Full-stack SQLite path is correct by construction:** a generated app's database file lives at `./data/app.db` beside the app (a dedicated `APP_DATA_DIR` override only — never the generic `DATA_DIR` the host sets for its own storage), and deployments no longer leak the platform's `DATA_DIR` into the app — so the agent never burns turns hunting for / relocating a stray `/data/app.db` (the over-correction loop we observed on a live car-wash build).
- **Resilient app/preview proxy** — the reverse proxy that serves published apps (`/apps/<slug>/`) and the in-app preview reads the upstream response defensively and retries once on a dropped/stale connection, so a transient "premature close" can't surface as a 500 error page; an upstream that's genuinely down returns a clean "restart it" 502 instead.
- **Restart-resilient runs (no stuck spinner)** — a run interrupted by a server restart/deploy isn't lost OR left hanging: on boot the server clears any orphaned "running" session and **auto-resumes the recent ones** ("continue where you left off", recency- + crash-loop-guarded), and the **chat reconnects on its own** — a restart behind the proxy 502s and permanently closes the event stream, so the client now reconnects with backoff and **re-syncs the session on every reconnect**, reflecting the resumed run or cleanly ending it. The "working…" spinner can no longer hang forever after an update.
- **Resilient model streaming** — if the connection to the model drops mid-reply (a "premature close" / terminated socket), the agent reconnects and redoes the turn (bounded), and a `turn_reset` event clears any partial output so the retry never shows doubled text. If M3 stalls, the run switches within MiniMax to the fast model to finish — so a slow or flaky turn still completes instead of dying with an error.
- **Self-contained apps (no CDN)** — built apps vendor their libraries (charting, etc.) locally and self-host fonts, with one real root `index.html`; nothing is pulled from an external CDN at runtime, so a published dashboard's charts can't silently break when a third-party CDN rate-limits/403s.

## Deliverables & deployment
- **Publish & share a 24-hour preview** (`publish_app` + the TopBar "Publish & share") — ship a site/app to a **public URL anyone can open, no login** at `/apps/<slug>/`, served via a process registry that survives restarts (boot recovery). **Any app type works**: a plain static site, a **React/Vite (or CRA/Vue/Parcel) SPA** — which is *built* (`npm run build`) and served from its `dist/`, never run as a dev server — and **Node/Python server apps**, reverse-proxied. A small injected shim makes a published app's root-absolute calls (`fetch('/api/…')`, XHR, WebSocket) and assets resolve correctly under the `/apps/<slug>/` path, so an app's API actually works in the browser, not just the page load. Every publish is a **24-hour preview**: a boot-started janitor auto-deletes it (process + files + record) 24h after publishing; re-publish to refresh. The dialog shows a live "expires in …" countdown. Opening a published link from the **installed PWA** hands off to the user's real browser (a true external window) instead of taking over the app window, so the ArksAI session is preserved. (Single-Droplet constraint for now — a permanent tier comes with a bigger server; pre-existing deployments without an expiry are left running.)
- Auto-export (zip download chip) + auto-canvas — the canvas auto-opens and loads the result (web app, PDF, spreadsheet, or doc). Generated **compliance machine files** (WPS `.sif`, PINT AE e-invoice `.xml`) are surfaced as downloads too, so a produced filing actually reaches the user. **The downloads list is the finished thing, not the workshop floor:** build **intermediates** that only exist to be embedded into a deliverable — rasterised icons (`icon-4-0.png`), slide-chart images, `render_chart` SVGs under `charts/`, and the `.pptx`/artifact `.preview.html` mirror — are filtered out, and **revise-round versions are collapsed to the latest** (one `deck.pdf`, not `deck.pdf` + `deck-v2.pdf` + `deck-v3.pdf`), so a single deck no longer buries you under a dozen files. The dedup is conservative — it only merges an explicit `-v2`/`(2)`/`-final` suffix on an otherwise-identical name, so distinct files like `sales-2024.pdf` and `sales-2025.pdf` are never merged.
- Styled office docs: `generate_spreadsheet` (exceljs, formula-driven, validated — and a built-in **formula recalculator** recomputes each formula's cached value before saving, so the in-app preview shows correct numbers instead of a model's stale/0/blank cached value; this now runs **authoritatively on the built workbook using each cell's real coordinates**, so it can't be defeated by a title row, merged cells or object-keyed rows that previously left formula cells blank in the preview; the formula is preserved so the download always recalculates in Excel — and **every formula cell is guaranteed to survive**: ExcelJS silently drops a formula that has no cached result, so a formula our evaluator can't compute (NPV/IRR/lookup, refs to text) used to vanish entirely — it now always keeps a placeholder result so even advanced formulas are preserved, never lost). **Large, granular, multi-sheet models build incrementally** — a 3-year **month-by-month CAPEX + OPEX** model (Assumptions → CAPEX → OPEX → Personnel → Summary, with cross-sheet formulas) is assembled **one sheet per call** (`append: true`) instead of one huge payload, so a detailed model **reliably finishes and returns output** rather than stalling, while staying fully formula-driven and gate-passing. **Total/roll-up rows are auto-styled** (bold + a hairline top rule) so a model reads like a real financial statement. **Cross-sheet links can't silently break**: a model that writes a reference without the leading `=` (e.g. `Assumptions!$B$10` typed as plain text — which used to sit inert and break every dependent cell with `#VALUE!`) is **auto-healed into a live formula**, the design gate **flags any stringy reference that slips through**, and every workbook is marked **recalculate-on-open** so Excel/Sheets always show correct numbers. The preview recalculator now also evaluates **`IF()` and comparison conditions** (`>`, `<`, `>=`, `<=`, `=`, `<>`), so an `IF(C4>0,…)`-driven row and everything downstream show the right values, not 0. The model is steered to a **clean calc structure** (header = row 1, data = row 2+, no decorative banner/title rows inside a calc sheet) with a deterministic **banner-row detector** that flags a `── SECTION ──`-style row which would shift cells and corrupt formula references, so totals/balances point at the right numbers, and to **research real drivers** (rents, salaries, equipment costs) for the Assumptions sheet rather than inventing figures. **Authoritative recalc + a blessed "build it by hand when needed" path (`recalc_spreadsheet`)** — the in-tool calculator covers SUM/IF/arithmetic/cross-sheet refs, but a real model often needs functions it can't compute (NPV/IRR/VLOOKUP/INDEX-MATCH/dates) or circular links (interest↔debt↔cash). For those, the agent is now explicitly cleared to do what an expert does — **build the model with a Python/openpyxl script** (real formulas, never dumped literals) — and then call **`recalc_spreadsheet`**, which runs the workbook through headless **LibreOffice** to compute EVERY value authoritatively, writes the true results back so the preview and first open are correct, and reports any **error cells** (#REF!/#VALUE!/#DIV0!) in ONE trustworthy check (no more hand-looping LibreOffice to verify). The **delivery gate runs the same LibreOffice recalc on every model**, so even a workbook the agent didn't explicitly verify ships with correct numbers and a real error-cell check (degrades to the built-in calculator when LibreOffice is unavailable). The guiding rule: a correct model is worth a longer build — a wrong-but-fast model is the failure. (The full set of spreadsheet failure modes + their handlers is catalogued in `SPREADSHEET_EDGE_CASES.md`.) `generate_doc` (docx) — documents embed the editorial typefaces (Source Serif 4 display + Inter body, the same as the reports/app), so they render designed everywhere instead of as office default. `generate_doc` also takes a **designed cover page** (masthead · accent title · one-line thesis · a **KPI band** of headline numbers · metadata footer · an optional **logo**) and `chart` blocks that embed **publication-grade `render_chart` images** — so a brief/report reads designed end-to-end, not as a wall of text. **Word documents are now truly bilingual**: Arabic text anywhere in a document (a heading, a paragraph, one clause inside an English sentence) is detected per-script-run and set in an **embedded Noto Naskh Arabic** face with proper **right-to-left paragraph direction and right alignment** — so the UAE legal deliverables (bilingual contracts, police reports, notarised POAs) render as native Arabic typography, not tofu boxes or left-aligned Latin-font Arabic. Long documents get **real document furniture**: an auto-updating **Contents page** (`toc`), running **page headers** with the document title and **"Page X of Y" footers** (cover page exempt), plus new **`heading3`**, **`callout`** (a shaded accent key-finding box) and **`image`** (embed a workspace image with a caption, honest placeholder if missing) blocks and a **landscape** orientation option. Every document is **re-open validated** after writing (parsed back, word count checked) so a corrupt or empty .docx can never ship silently.
- **`generate_pptx`** — real, editable PowerPoint (PptxGenJS): editorial 16:9 decks with a **designed cover** (masthead · accent title + thesis · a **KPI band** of headline numbers · metadata footer · `CONFIDENTIAL`) and a **dark/light slide rhythm** (per-slide theme), **publication-grade charts** via `render_chart` (dual-axis/heatmap/… embedded as crisp images, beyond the basic native bar/line/pie), title/section/bullets/two-col/stat/quote/table/image layouts, one accent, and an embedded preview for the canvas + the visual gate.
- Report mode — PDFs and 16:9 decks with a full editorial protocol (newspaper margins, anti-orphan `.lede`, kickers, rules, page mechanics); charts/figures are ATOMIC (`.keep`/`.fig`) so nothing splits across a page, and every page is auto design-reviewed before delivery. A **"get the first render right"** rule set targets the three defects the gate used to churn on — **lonely / half-empty pages** (forced per-section page-breaks are now forbidden; content FLOWS to fill ~85–100% of every page, a stray card grid sits with its narrative, never alone), **cornered/tiny charts** (every chart is a full-measure `.fig` on its own row and the SVG is auto-made **fluid** so it fills the column instead of collapsing with colliding axis labels), and the **white-frame background** (the page colour lives only on html/body or a `.bleed` page) — so the first render passes review fast instead of looping expensive revise rounds. **Token-efficient by design:** once the PDF/deck is rendered, the raw web-research dumps from earlier turns (web_search / web_fetch / fetch_data — already baked into the document) are auto-trimmed from context so the QC/revise turns don't re-pay for them, cutting a research report's token use dramatically. If a run ever does brush the per-run safety budget **after a good document was produced**, it now **completes normally and shows the file** (with a soft "this took a bit more processing" note) instead of erroring over a finished PDF. The protocol also hardens the anti-half-empty rule (NO interior page below ~70% full — a short profile/overview section merges with its neighbour) and enforces a single **output language** (English, or the user's language) so stray non-Latin characters never leak into the prose.
- **`render_chart`** — publication-grade charts as embeddable SVG (Vega-Lite → SVG, server-side, no browser/Python): line, multi-line, **dual-axis** (volume bars + a trend line), bar/bar-h, stacked, area, donut, and **heatmap** (e.g. month×year). Editorial defaults baked in — flat 2D, muted base + the report accent on the key series only, direct value labels, light gridlines — so charts are on-brand by default and inline straight into the report HTML. The returned SVG is **fluid** (full-measure `width:100%; height:auto`, viewBox kept) so it fills its `.fig` block instead of collapsing into a corner, and the intrinsic size auto-grows with the data (more rows → taller) so axis labels never collide; it also rasterizes to a transparent PNG so the SAME editorial charts embed into `.pptx` decks and `.docx` documents. **A chart can never silently ship empty**: Vega will render valid-but-blank axes (no bars/line) when the `x`/`y`/`series`/`value` field names don't match the keys in the data rows — so `render_chart` now **validates the field binding up front** (and a post-render backstop checks a bar/area chart actually contains mark elements) and returns an actionable error listing the real data keys, instead of writing a dataless SVG that would inline into the report as empty axes. Caught live: a "Top 10 nations" horizontal bar chart that rendered as bare axes.
- **Two deterministic quality tools** — `validate_palette` checks a brand palette for **WCAG 2.2 AA contrast** (body text + links at 4.5:1, accent/large text/UI at 3:1) before any visual build and returns a corrected colour for any failing pair plus a ready-to-use token set (primary/accent/bg/surface/text/muted/line/link) so text is never shipped unreadable; `compute_financials` runs the **DCF (NPV + terminal value), the standard ratio set, budget variance (favourable/unfavourable), and a linear/growth forecast** so every derived number in a report or sheet is computed, not hand-typed. Both are pure, instant, no key needed, available in chat/code/report, and the relevant finance + design standards steer the agent to use them.
- **Designed, full-bleed cover** — the report cover is a **true edge-to-edge canvas** (a named zero-margin `@page` so the background field reaches all four paper edges — interior pages keep their margins), with a typography-led foreground: the **brand logo** in the masthead, accent title line, one-line thesis, a **KPI band** (even cell padding, breathing room), and a metadata footer. Dark / deep-accent / light field options.
- **Analysis & methodology rigor** — the report protocol now demands a systematic analysis pass first (profile columns, full cross-tabs, reconcile conflicting fields), an insight-led narrative (counter-intuitive reframe → evidence → ranked recs tied to specific data points), and a Methodology/Notes section (proxies + their limits, data gaps, source attribution).
- **Android Apps — a dedicated page** (`/android`, 📱 in the sidebar): a guided intake wizard (app name + what it does · app type/archetype · a **visual style kit** — Material You / Minimal-Editorial / Bold-Playful / Clean-Pro / Dark-Premium · how it runs: native APK / PWA / both · a broad set of **device capabilities** — camera/QR, photos, location, notifications, contacts, microphone, biometrics, calendar, files, audio/video, haptics, share, Bluetooth, NFC · accounts/sync/realtime backend · a 12-colour accent palette) composes a precise brief. Because a good native app needs a plan, the agent does a short **plan + clarifying round** (proposes screens, data model, flows, permissions; asks a couple of essential questions) and waits for approval before the long build — then builds fully autonomously and hands off to the live session view. Native APKs are compiled on an ephemeral build machine (Kotlin-pinned Expo prebuild + Gradle, never EAS) and returned as an installable file; if a backend is needed it auto-deploys to our infra and the client is wired with a typed JWT API. Honest about timing.
- **Publish to Google Play — per-org developer accounts**: each company connects its **own** Google Play Developer account on the Android page (admin-only), so apps publish under that company's identity + billing — never a shared account. The service-account credentials are stored AES-256-GCM encrypted at rest, write-only, org-isolated (admin-gated). *(Connection scaffold is live; direct-to-Play AAB upload is the next step — until then native builds hand back a sideloadable APK.)*
- In-app doc viewer (xlsx/csv/docx/pptx → styled HTML); **Canvas as a clean result viewer** — it no longer auto-pops a developer panel over the chat on completion; instead the clickable completion card is the moment, and tapping it opens a decluttered viewer (just a title · **Open ↗** · Close, with the finished result full-bleed). The port pills, file tree and refresh now live behind a single unobtrusive **Dev** toggle for power users — so a non-technical user never sees `:4000`/`:2024` port chrome, while everything stays one click away.
- **Mobile apps (in progress)** — a bundled **mobile UI kit** (React Native / Expo: light+dark design tokens, ThemeProvider/Screen/AppText/Button/Card/Field/EmptyState/Loading + a root crash-safe ErrorBoundary, `add_mobile_ui_kit`) and a matching **app backend kit** (Fastify + SQLite: JWT auth, a sample CRUD resource, a consistent error envelope, per-route JSON-schema validation, CORS, idempotent migrations, `add_app_backend`) that installs the API into `server/` **and a matching typed client into `src/api/`** (a JWT+AsyncStorage fetch wrapper that unwraps the error envelope, plus login/register/me and a single `API_BASE_URL`) — so a generated app's screens *and* its API are wired together out of the box: the backend deploys zero-config to our infra (`<slug>.apps.arksai.studio`), you set one URL, and every screen calls it the same safe way. Screens *and* the API both start from a minimal, modern, typography-first, validated foundation — the same house quality as the web builds. PWA is the default lightweight path; native Android APKs build on our own infra (Apple/iOS via EAS).

## The flow (effortless AND expert)
- **Home / Workspace** — the default screen when no chat is open is now a real **workspace hub**, not just a task launcher (one consolidated, role-aware look — the layout is identical for every team; only the content adapts). It stacks: a personalized greeting, a **"Needs you"** strip (robot replies to approve, a failed publish to fix, low balance), a **"Jump back in"** row of recent work, the **"Start something"** zone (the team picker + describe-box), **"Running for you"** (your live apps with their URLs, scheduled tasks with their next run, active robots), and **"Your team can also…"** — a few one-tap capability cards (in each team's language) that surface the platform's breadth (hire an inbox robot, schedule a recurring report, connect your tools, design a creative) without a feature menu. A brand-new user with nothing yet just sees the greeting + picker (exactly as before); a returning user sees their whole workspace.
- **Activity — one inbox for everything that runs for you** — a dedicated **Activity** surface (sidebar → 🔔, linkable at `/activity`) gathers all the background work the platform does into a single tended feed: a **"Needs you"** tab (approve & send / dismiss each robot reply right there, fix a failed publish, top up a low wallet) and a **"Delivered"** tab (finished scheduled runs, click to open). The sidebar shows a live badge so it's never invisible.
- **Recents-first sidebar + one-tap tools** — your **recent chats sit right at the top** (under New session + search), taking the main space, so getting back to past work is instant — no hunting or searching. Everything else (Activity · Robots · Scheduled · Live apps · Android · Connect · New project · Admin) is a **compact, labeled strip pinned at the bottom** with live count badges (drafts waiting, a failed publish, low balance) — discoverable but never pushing the chats down. A ⌂ Home shortcut sits on the Recent header, and a global **Live apps** view lists every app you've published across the workspace.
- **Pick where the work goes, from the start** — the describe box has a **Connect** control next to Attach: choose the **GitHub repo** this build should push to before it even begins (or connect your account inline), with a one-tap path to the Connections hub for email / ad platforms. The chosen repo is attached to the new session at creation, so code lands in the right place from the get-go (the same picker is also under any active chat).
- **Connectors live in the composer, not an always-on bar** — a single unified **connector icon** (next to the + / attach button, in the Connect modal, and the sidebar) opens a modal to connect GitHub + pick the repo this session pushes to (with a path to the full connectors hub). There's no persistent repo bar eating screen space.
- **Commit & push, exactly when it's useful** — once a repo is attached, a **Commit & push** button appears next to **Send** *only when there are uncommitted changes*, and stages + commits + pushes to the connected repo deterministically (no model turn). The live commit/push state (Pushed · `<sha>` / committed-not-pushed / N changes) shows inside the Connect modal — so the user never guesses whether their code reached the repo, without cluttering the chat.
- **One-tap new chat** — starting a new session opens a fresh chat immediately (no dialog to click through); advanced options still live behind the New-session dialog for power users.
- Smart intake — a short, type-aware brief (one round), then fully autonomous.
- Launchpad — input-first onboarding: a **personalized, time-aware greeting** opens it (by name — Claude-style "Coffee and ArksAI time, Kamran?", with varied lines per part of the day and an editable name that's **remembered server-side**, so it follows you across devices), the **chat box sits at the top**, and the team's example tasks are denser below — there to pick when you want them. The describe box accepts **file uploads, drag-and-drop, and clipboard paste** (staged, then attached to the new session before its first run), just like the main composer.
- Live "smart-work" progress bar — named phases with an anticipation creep so it never looks frozen; technical work stays visible (visible competence = trust). It also shows an **estimated time remaining** that counts down through the phases (slightly generous so a run usually beats it — a small positive surprise, not a broken countdown); when a build runs past the estimate it eases into a soft "larger job — hang tight / almost there" instead of a stuck 0:00. The estimate is **self-calibrating, and gets sharper the more you use it** — each completed run folds its real per-phase durations into a rolling average kept at two levels: a broad **per-mode** prior AND a precise **per-(mode + task)** bucket (e.g. a finance cash-flow model vs a quick doc tweak — both `code`, but minutes apart). The countdown uses the task-specific history when it has enough samples and falls back to the mode average otherwise, so it converges on what THIS deployment actually does for THIS kind of request instead of a fixed guess. Learned timings persist on disk, so they survive restarts and keep improving across sessions.
- **Build you can watch (engagement-tuned)** — the moment you hit go the bar + footer appear (optimistic, zero gap); a live **play-by-play ticker** names the current action in human terms ("Looking at your image", "Setting the typography", "Designing the PDF", "Publishing it live") and auto-expands while working; switching chats never flashes; if a step needs retrying the studio says so kindly (plain language, never a scary error).
- **A real "it's ready" moment** — a successful build ends on a staged completion card with a **live thumbnail** of the finished app/document, one-tap **Open**, and **Share** — which opens the focused **Publish & share modal** where the live link (and any error, in full, if a check fails) is clearly visible and copyable, never a cramped strip in the chat flow. Fires for every build, including static single-file sites. Publishing walks its real phases (Snapshotting → Installing → Booting → Checking the live URL) so the wait shows motion; a held-back URL explains exactly what failed so it can be fixed and re-published.
- **Completion time on every result** — each finished reply shows a quiet "Completed in 3m 12s" — the wall-clock from your message (input) to the result (output). It's **stored with the chat** (in the timeline), so it's still there on reload and across devices, letting you see how long each input vs output actually took.
- **Pick up where you left off + calm dialogs** — the home screen shows a "Jump back in" row of your recent work; destructive actions use a styled in-app confirm (no jarring browser popup) and **Esc closes any dialog**; the live preview locks onto the exact port the build produced (no more wrong-port "Error").
- Self-healing reframed as confident "hardening it (pass N)" forward progress, not failure.
- Delivery moment — "Booting your live app…" loading + retry, and a completion card ("✓ Your app is ready" → Open / Get a shareable link) with a **"What's next?"** row of contextual follow-up chips (e.g. add a feature, tweak the design, turn a report into a deck) that pre-fill the message box so there's always an obvious next step.
- One-time "what's new" modal — shown once per user per version (remembered locally) right after sign-in, to keep people in the loop on recent updates.

## Smart agent
- **Talk to ArksAI — mic + conversation mode in the chat**: a 🎙 button in the message bar records your voice and transcribes it (same language-preserving engine the robots use) into the box — or flip on **Conversation mode** and it becomes hands-free-ish: your recording sends immediately as the message, and when the reply arrives it's **read aloud** (MiniMax speech; markdown/links/code stripped for the ear, one playback per reply). The toggle is remembered per browser. Degrades honestly: no speech engine configured → the controls don't render dead chrome; TTS-only missing → voice-in still works with text-out. Recording auto-stops at 2 minutes; "no speech heard" gets a friendly nudge, never a fake transcript.
- Sees **every** upload, automatically — on each run the agent is told exactly what was just uploaded and how to open it (images → `see_image`, **available in every mode**; a logo → `extract_palette` for exact brand hex; **spreadsheets (`.xlsx/.xls/.csv`) → `read_spreadsheet`**, which lists EVERY sheet with dimensions + headers + samples and reads exact values on request — never a lossy text dump that silently drops sheets, and heavy analysis crunches the raw file with Python; **presentations (`.pptx`) → `read_presentation`**, which reads every slide's title/bullets/tables + speaker notes (never a binary blob); prose docs (`.pdf/.docx`) → their auto-extracted `.extracted.txt` sidecar; anything else → `read_file`), so it acts without the user re-instructing. **When several files are dropped at once (multiple spreadsheets + images + a deck), it processes EVERY one** — every sheet, every slide, every image — not just the first. It never claims it "can't view" an image — vision is always on. Files attach via the `+` button, **drag-and-drop, or clipboard paste** (copy a file/screenshot → ⌘/Ctrl-V); a `📎` chip shows what's attached. Uploads **retry automatically** through a transient network or deploy/restart blip (with backoff; a real over-limit `413` still reports immediately), so a momentary hiccup never surfaces as a "failed to fetch".
- **Make an image LIKE an uploaded one** — `generate_image` takes an optional `reference_image`: to recreate or vary an image the user uploaded, the agent looks at it (`see_image`) to read its subject/style/composition/palette and prompts from that, and for a person whose likeness should carry across it passes the reference directly to the engine (MiniMax subject reference). "Create an image like this" just works.
- **Brand from the logo** — `extract_palette` reads the real colours out of an uploaded logo (deterministic, no vision model needed) so the accent is taken from the brand; the **same identity (logo, accent, type) is applied across every deliverable** — web apps, decks, docs, and reports — and a deliberately beautiful palette is chosen when there's no logo.
- Auto mode/engine switching — `switch_mode` lets a chat become a build/report mid-conversation; the runner reloads the toolset, prompt, and engine on the fly with an inline note. It also routes **back**: a build/report session isn't sticky — if your next message is a different kind of request (a question, a deck, an image, a brand-new build) ArksAI moves itself to the right mode for it instead of forcing it through the current one. Built into the seamless flow: a vague ask → ArksAI asks a short round of clarifying questions first (never refuses, never blindly guesses); a coding ask → it **plans first** (shows exactly what it'll build) and waits for the user to approve (→ autonomous build) or revise.
- Plan approval gate — when ArksAI plans a build it presents the plan, then shows an explicit **"✓ Approve & build" / "Revise"** card (Approve kicks off the autonomous build). **Revise** is now unambiguous: clicking it hides the approval card and drops you into a clearly-labelled **"Revising the plan"** compose mode — a banner above the message box (with a "Keep plan" escape that restores the card) and a revision-specific placeholder — so it's obvious your next message edits the plan, after which ArksAI re-plans and re-presents the gate. The gate is **structural**, not just a suggestion: building can only begin on your next turn after a plan — the agent cannot skip straight from planning to building in the same breath. Approve **reliably** starts the build: the plan must be complete (no trailing question) before it's offered, and on approval the agent is blocked from re-submitting the plan and goes straight to building (so Approve never loops back to the card).

## B2B department platform
- Department-aware studio — Marketing, Sales, Finance/Strategy, HR·People & Ops, Engineering, BI & Analytics, Tax & Compliance (UAE), Legal (UAE), plus **Personal** and **Learning** for everyday non-business use. Pick your function and ArksAI is **ready with that team's skills** (its persona/standards load on the session). Each task is an **example you tap to open a fresh chat already specialized for it** — no canned prompt is fired; the chat just waits for you, and your first message names it. Or type your own brief in the box.
- ~70-task catalog grouped **Create / Analyze / Operate**, each a ready-to-run brief in the right mode.
- Per-task expert standards (server-side) — a department persona (FP&A rigor, inclusive HR, RevOps, brand-growth, senior-eng, BI/analytics, UAE tax manager) + research-backed standards injected per task.
- **Developer tier for the Engineering team** — five deep developer expertises (canon harvested from the claude-skills library, reimplemented in TypeScript, zero new deps): **database schema design** (normalize → keys/indexes/relationships, Mermaid ERD + DDL + migration plan), **API design & review** (REST conventions, versioning, pagination, idempotency, error shapes, auth + a design scorecard), **API test suite** (happy-path + input-validation + the auth matrix + error codes + contract checks, then exercised), **code review** (correctness/security/readability/performance, severity-tagged with concrete fixes + blast-radius), and **dependency audit** backed by a new deterministic **`dependency_auditor`** tool — pure, offline, no key: parses package.json / requirements.txt and flags floating/unpinned ranges, duplicates, pre-1.0 pins, and a missing lockfile, honestly noting it can't see live CVEs offline (never fabricates one; recommends `npm audit` / `pip-audit`). Every developer deliverable is a **designed, typography-first document** (generate_doc / render_report), not a raw markdown/code dump.
- **Auto-selected expertise (zero-pick)** — you no longer have to pick a department to get expert standards: a free-form message ("make me a monthly budget", "write a job description", "draft an NDA", "build a cash-flow forecast") is read by a fast, deterministic trigger-based router that silently loads the right expertise (the exact task standard when it's confident, else the department persona). A picked play always wins; if nothing matches, it stays the general agent — so it's purely additive. (Off-switch: `EXPERTISE_AUTOROUTE=false`.)
- **Thoughtful when it's unsure (confidence → clarify)** — the auto-router now behaves like a careful expert instead of guessing. When it's confident it silently loads the exact task expertise; when the signal is softer it loads only the **department persona** (still the right expert voice, never a wrong specific); and when a message is genuinely too thin to know what you want ("make me something nice", "help with my thing tomorrow"), it asks **one warm, specific question** to narrow it — never a blind guess, never "I can't help with that". A clear request with a real subject still goes straight to building. A specific skill is **never** fired on a low-confidence match (the "never confidently wrong" guard). (Off-switch: `EXPERTISE_CLARIFY=false`.)
- **Personal / everyday expertise (ArksAI for everyone, not just business)** — two non-business lanes anyone can use without picking anything: a **Personal** department (my monthly/household budget — friendly, formula-driven; savings & debt-payoff plan; **"should I buy / sell" big-purchase valuation** that researches real cited listings and gives a buy + resale range — the "value my Porsche / iPhone" case; résumé/CV; cover letter; complaint/dispute letter; formal letter; email/message rewrite; trip itinerary; event/party plan; personal checklist) and a **Learning** department (explain a concept at your level; study guide / revision notes; faithfully summarize a document). Plain language, no jargon, real cited facts (never fabricated). A free-form message ("household budget for next month", "should I buy a 2019 Cayenne at 200k km", "explain compound interest to a 15-year-old", "write a complaint letter — the airline lost my bag") auto-routes to the right one.
- **Drift-proof expertise registry** — every expertise's `<dept>.<task>` key + department now lives in ONE source of truth (`shared/expertiseKeys.ts`) that both the client play catalog and the server standards derive from (the client play key is even compiler-typed against it). Cross-file **sync tests** fail the build the moment a key is half-added — a play with no standard/triggers, a standard/trigger with no play, a department with no persona, or two tasks sharing a trigger phrase — so adding a skill is a safe, consistent, template-driven act (recipe in `EXPERTISE_AUTHORING.md`). No behavior change: every existing play, standard, and trigger is unchanged.
- **Leaner prompts, same quality (progressive disclosure)** — the system prompt is now assembled from a slim always-on core plus on-demand slices (the music/image/document-tool notes, the report page-mechanics, the design system, the per-task expert standards), and a slice loads **only when the current task can use it**. A read-only planning turn no longer carries the build/generation tool notes it can't act on (~48% shorter), and the per-task expertise loads only for that task — so each turn costs fewer tokens with **no loss of capability**. The guarantee is hard-locked: a **report build receives every report rule byte-for-byte** as before (a test asserts the report and code prompts are identical with the feature on vs off), so the painstakingly-tuned report engine cannot regress. (Off-switch: `EXPERTISE_PROGRESSIVE=false` restores the full prompt instantly.)
- **Auto-Brief — closing the novice↔expert prompt gap** — a thin request and an expert brief produce wildly different output; ArksAI silently compiles every request into the brief an expert would have written, before it acts. A deterministic, zero-latency layer (`server/src/agent/brief.ts`) — no per-request LLM call, so no intent drift and no fabricated specifics — detects the deliverable family (research · financial model · report · data analysis · formal document · a conservative non-visual generic fallback) and injects three composed layers into the system prompt: **(1) brief-first compilation** — restate the goal, state explicit ASSUMPTIONS for anything unspecified, and ask AT MOST one question only when an input is genuinely critical, else proceed (invisible, auto-proceed); **(2) a per-family operating procedure** — ROLE · SUCCESS CRITERIA / HARD FILTERS · METHOD · VERIFICATION RULES · OUTPUT CONTRACT · SELF-AUDIT; **(3) a self-audit gate** — don't deliver output that fails its own audit; label every figure verified (dated source) or unverified. So a one-line "research which VCs invest pre-seed in the UAE" comes back with qualifying filters, a dated source per item, the right metric (typical INITIAL cheque, not a one-off round), High/Medium/Low confidence, an "Adjacent / stretch" section, and a "Could not verify" section instead of padding — **verified live on the real agent**. Missing inputs become explicit assumptions, one question, or a marked `[placeholder]` — never fabrication. Plain visual builds are left untouched (they already get the design system + verify/design gates). (Off-switch: `AUTO_BRIEF=0`.) Remaining tuning (a separate fast-model pre-rewrite, a standalone re-review audit pass, and A/B telemetry) is optional — the deterministic prompt layer already delivers the lift.
- **Marketing makes finished creatives (image + crisp text + logo)** — a `generate_creative` capability produces a ready-to-post **PNG/JPEG**: it generates an on-brand, text-free AI background with reserved negative space, uses **M3 vision to pick the calmest zone + the text colour that reads there**, then composites the headline/subhead/**check-marked feature list**/CTA as **pixel-perfect type** (HTML/CSS, never the image model's garbled lettering) plus the **user's uploaded logo** (or a tasteful top-left "LOGO" placeholder) if provided, with a contrast scrim and a vision QC + auto-correct pass. It also **scrubs any copy the model put into the imagery prompt** (literal headline/CTA, quoted strings, "text that says…") so the background can't come back with garbled lettering, and re-generates with a hard no-text directive if text still leaks in. The dedicated **Ad & social creative** play, the email/social kit, and landing/event heroes all use it at channel sizes (1:1 / 4:5 / 9:16 / 16:9 / 1.91:1); graceful CSS/SVG fallback when unkeyed. The agent **asks for your logo first** (or uses a clean placeholder), and social creatives bake in **high-conversion tactics** (a benefit hook, one clear CTA, truthful proof/specifics, thumbnail-legible type) — kept **separate from email**, which is optimized for the inbox instead.
- **Logo & brand identity — a real designer submission** — a `generate_logo` capability (Marketing → **Logo & brand identity**) designs a complete brand mark, not a clip-art guess. It plays to the model's strengths and bans its weakness: **letters are always real type** (chosen from a curated ~23-family suite of geometric sans + editorial serifs + display faces) and **symbols are always built from overlapping geometric primitives** — it **never warps/morphs** a letter into an object (the thing that produced lollipops), so the mark reads crisply at every size. It **auto-picks the route** (a pictorial concept mark vs a lettermark), generates several drafts, uses **M3 vision to select its own strongest** and name the fixes, then does a **structure-locked polish**. The deliverable is a full **designer submission, zipped**: a **brand directive** (concept rationale + palette table + typography), **light & dark** variants, an **app-icon, favicon, and website-navbar placement**, and a multi-format **asset kit** (self-contained **SVG** with fonts embedded, plus **PNG & JPEG** at 1024/512/256/128/64 on **both light and dark** backgrounds). The brand sheet is delivered as a **downloadable PDF** that **auto-opens in the canvas** (universally viewable, unlike a raw `.html`); graceful config message when unkeyed. The agent **gathers a quick brief first** (brand + what it does, a direction or two, a colour, the vibe).

## UAE Legal
- A dedicated **Legal (UAE)** department that drafts **execution-ready** legal work — commercial contracts, NDAs, employment contracts, POAs, MOA + shareholders' agreements, board resolutions, **legal notices / letters before action**, **police reports / criminal complaints**, **legal opinions**, **legal forensic audits**, contract-review risk reports, compliance audits, dispute briefs, licensing/jurisdiction advice, and a legal calendar/filing tracker (15 plays).
- **Bilingual documents** (the chat follows your language; only the produced document is bilingual) — formal British English + eloquent, posh **Modern Standard Arabic that is not a literal translation** (native legal idiom), grounded in current UAE **federal + emirate + DIFC/ADGM** law (jurisdiction-resolved first), citing the specific law and article, with full clauses/boilerplate and bracketed placeholders so **counsel reviews and signs rather than rewrites**. Every output carries a mandatory "have a licensed UAE lawyer review and sign — not legal advice" notice. Defaults to the **MiniMax M3** model (chosen via a live legal-drafting bake-off).

## UAE Tax & Compliance
- A dedicated **Tax & Compliance (UAE)** department that produces the format-strict filing documents a UAE finance team needs — **e-invoicing (PINT AE)**, **VAT (VAT 201 + FAF)**, **Corporate Tax (the 9% CT 300 computation)**, **Excise**, and monthly **WPS** salary disbursement — each built to the researched FTA / MoF / MOHRE / Peppol spec and self-validated.
- **`generate_compliance_file`** — a self-validating generator for the three strict machine formats (like `generate_spreadsheet`, it returns errors instead of a bad file): the **WPS `.sif`** (builds the SCR header + EDR rows, computes the control total = Σ EDR to the fils, validates the 13-digit employer ID / 14-digit labour IDs / 9-digit routing / 23-char "AE" IBANs / no-comma AED amounts, and names the file `[employerID][YYMMDD][HHMMSS].sif`), the **FAF** VAT-audit CSV (company + supplies + purchases + totals, reconciled to the VAT 201 output/input VAT), and the **PINT AE e-invoice** UBL 2.1 XML (parties with 15-digit TRNs + scheme `0235`, a tax subtotal per VAT category code S/Z/E/AE/O/G, well-formed, totals reconcile; tax invoice + credit/debit notes). The VAT 201 / CT / Excise working papers and the readable invoice reuse `generate_spreadsheet` (formula-driven) / `generate_doc` / `render_report`.
- **Guided filing wizard** (`tax.guided`) — walks a non-expert finance user through one obligation step by step: entity profile → tax period + deadline → import & validate the data (never fabricated, gaps flagged) → build and validate each document → a review checklist + the assembled filing pack and exactly where to submit.
- **Honest framing on every output**: these are draft working papers for professional review — ArksAI does not file or pay; the org submits via **EmaraTax / its accredited ASP / its WPS agent bank**, and every file carries a "validate against the official schema / your agent bank before live submission" note (the PINT AE spec is partly draft; the WPS SIF layout varies by bank; DIFC/ADGM use their own systems).
- B2B acquisition landing (pre-login) — positioned as **"the AI that empowers your business"** for the UAE, and **SEO/LLM-optimized for "AI in the UAE"**: a keyword-rich `<title>`/description/keywords + Open Graph + **JSON-LD structured data** (Organization, SoftwareApplication, FAQPage) in `index.html` so search and answer engines can read and cite it pre-JS. Meatier content: a **"what it makes"** capability grid (live apps, dashboards, reports/decks, on-brand ad & social creatives, UAE compliance, UAE legal), a **"Built for the UAE"** compliance block (VAT 201/FAF, PINT AE e-invoicing, Corporate Tax, WPS, Excise), a by-department value grid, a UAE-focused FAQ, an early-adopter strip (the ARKS group), and a "watch it build" demo. Kept **alpha · invite-only** throughout (nav pill + hero note + request-access funnel → `POST /api/leads`). Excludes any admin/analytics surfaces.
- **Multi-page marketing site + shareable link previews + LLM SEO** — the headline message is **"give every team AI without hiring an AI expert" (help UAE businesses adopt AI with no AI-skilled staff)**. Dedicated **per-team pages** at `/for/{marketing,sales,finance,hr,tax,legal}` (`client/src/components/VerticalPage.tsx`), each a focused landing with that team's value prop, its full play catalog, how-it-works, and a pre-filled request-access form. Because crawlers and link-preview bots don't run the SPA's JS, the server **injects per-route `<title>`/description/canonical/OpenGraph** into the served HTML (`server/src/seo/marketingMeta.ts`, wired into the SPA fallback in `app.ts`) so a shared `/for/finance` link previews as the Finance page, not the generic homepage. A real **1200×630 branded OG card** (`og-cover.png`, replacing the old square icon) makes links look professional when shared on Reddit/X/LinkedIn/WhatsApp/Slack, with full `og:image:width/height/alt` + `twitter:image`. Plus **`robots.txt`** (explicitly welcoming GPTBot/ClaudeBot/PerplexityBot/Google-Extended and friends, disallowing transient `/apps/` + auth surfaces), a **`sitemap.xml`**, and an **`llms.txt`** — a clean, answer-engine-friendly description of the whole product + per-team capabilities, led by the UAE/no-AI-skills positioning (LLM SEO).
- **Features page (`/features`)** — a full, comprehensive public catalog of everything ArksAI does (`client/src/components/FeaturesPage.tsx`), grouped into nine accent-coded domains: Build & ship (web apps, full-stack + DB, payments, Android/PWA), Design & document (reports/decks/PDF, editable docx/xlsx/pptx, the design studio, charts), Data & business intelligence (dashboards, combine/reconcile, variance + recurring memory, SQL over workbooks), Video & motion (Seedance video, narrated explainers, story films, music), Marketing & brand (finished creatives + A/B hook variants, logo identity, live ad data), Robots & automation (multichannel robots, build-on-command, routines + gated actions, scheduling + webhooks), UAE compliance (VAT 201/FAF, CT 300, WPS/SIF, PINT AE e-invoicing, Excise), UAE legal (bilingual, citing law), and Your workspace (isolated orgs, connect GitHub/Google, wallet + invoicing, metadata-only analytics) — plus the quality system and a per-team grid. Server-side SEO meta on `/features`. **No pricing anywhere** — CTAs are "Join the waitlist".
- **ArksAI Research hub (`/research`) + cited articles (LLM SEO)** — a public thought-leadership surface (`client/src/components/Research.tsx`) with sourced, cited essays written for business operators. Articles live as JSON in `client/public/research/` (single source: fetched by the client reader AND read by the server SEO layer). Each article page gets **server-injected per-route `<title>`/description/canonical/OpenGraph + full JSON-LD `Article` structured data (headline, datePublished, keywords, and the complete `articleBody`)** so non-JS crawlers and answer engines can read and cite the content. First three pieces (each 14–15 verified sources, no fabricated figures): *Energy is becoming the next currency*, *The East and the AI frontier*, *How to read AI benchmarks*. Listed in `sitemap.xml` + summarised in `llms.txt`.
- **Waitlist positioning + focused landing funnel** — the landing (`Landing.tsx`) is a tight conversion path, not an everything-page: hero + live "watch it build" demo → social proof (ARKS adopters) → a "see it work" proof strip (four function vignettes — email robot, finance model, bilingual legal draft, marketing creative) → how-it-works → a **"why join early"** benefit strip (full access to every team, shape what gets built, no lock-in — **no public price during the private alpha**) → a **two-step low-friction waitlist** (email first, optional team details second; both post to `POST /api/leads`) → a founder's note → a tight 4-question FAQ (the "how do I get access?" answer says pricing is shared with members during onboarding — never a number on the page). CTAs read "Join the waitlist" everywhere (Landing, `/features`, `/research`). The encyclopedic content (the full capability grid, the UAE-compliance grid, the every-team department grid) lives on `/features`, linked prominently — so the homepage stays focused on converting.

## Day-to-day capabilities
- Recurring/scheduled tasks — a durable server scheduler (daily/weekly/interval) that fires a fresh session even with the browser closed; managed via a Schedules dialog. Times are read in **your local timezone** (captured from your browser, DST-correct), so "daily 08:00" fires at 08:00 where you are — not on the server's clock.
- **Large multi-tab spreadsheet reads — query, don't dump** (`query_spreadsheet`): the right way to analyse a big or many-tab workbook the way a senior analyst (or Claude) would — **load every tab into a DuckDB runtime and run SQL across them, getting back ONLY the result** (totals, group-bys, **joins across tabs**, pivots, filters, reconciliation) instead of paging thousands of raw cells into the chat. The data stays in a separate process, so it scales to huge, many-tab files without ever filling context. `read_spreadsheet` is now the cheap **map** (it lists every sheet with dims, a **query-table name**, **inferred column types**, and samples); `query_spreadsheet` is the calculator (each sheet is a `lower_snake_case` table; call once with empty SQL to discover the tables, then query). It also has a **`profile` mode** — the ideal first move on a messy/unfamiliar file: per-column **type, min/max, distinct count, average, and % null** for every tab, no cells paged. pandas + openpyxl + **duckdb are preinstalled** in the agent image so reads are instant (no mid-task `pip install`), with a graceful "install + retry" path if ever absent.
- Data in — `fetch_data` pulls a public CSV/JSON/published-Sheet URL (SSRF-guarded).
- **Ad-platform connectors (Meta · Google Ads · TikTok)** — org admins connect their ad accounts under **Settings → Connections** (OAuth); the agent then pulls **live** performance via `fetch_ads` straight into dashboards and reports (spend/impressions/clicks/ctr/conversions, by campaign/ad/day). Per-org, OAuth tokens stored **encrypted** (AES-256-GCM) and never exposed to the model; auto-refreshed on expiry; each provider gates on its app credentials. Reusable adapter framework (one interface per platform). Operator setup in `CONNECTORS.md`.
- **Connect Google (Gmail · Calendar · Drive/Sheets · Google Ads)** — each user links their **own** Google account from the Connections panel (right next to GitHub), so the agent & robots can work with their Google data: send/read email (Gmail), manage events (Calendar), read Sheets/Drive, and pull Google Ads performance — all under **one** consent. Per-user, **strictly isolated** (resolved by the authenticated user id), CSRF-protected via a signed state cookie, with **access + refresh tokens AES-256-GCM encrypted at rest** (never returned by any API or shown to the model) and transparent token refresh. The OAuth client is set by the operator via env (`GOOGLE_OAUTH_CLIENT_ID/SECRET`) **or** an operator endpoint that stores it encrypted at rest (no `.env`/SSH). Operator must register `<publicBaseUrl>/api/google/callback` as an authorized redirect URI. These are Google **restricted** scopes, so until the OAuth consent screen passes Google verification only Cloud "test users" can connect; Google Ads reporting additionally needs a developer token. (This is *connect-only* — there is no "Sign in with Google" login.)
- **Connect your GitHub & push to your own repo (per-user OAuth)** — each user links their **own** GitHub account (OAuth App) and **picks which repository** ArksAI pushes generated code to, or **creates a new repo** right from the New-session dialog (searchable list of repos you can push to). The session's clone/push then use **your** token (falling back to the operator PAT only for self-host). Tokens are **encrypted at rest** (AES-256-GCM), never returned by any API or shown to the model, and **scrubbed from all git output**; strict **per-user isolation** (you can only ever use your own connection). The operator turns it on either via env (`GITHUB_OAUTH_CLIENT_ID/SECRET`) or, with no SSH, an operator endpoint that stores the app credentials encrypted at rest (flips it on instantly). A documented trade-off of the OAuth-App approach is the `repo` scope grants access to all your repos (a future GitHub-App upgrade would scope to chosen repos).
- **Connecting a repo actually pulls in its code** — attaching a repository to a session (via the Connect modal, *after* the session was created) used to set the push target only and leave the workspace empty, so "go through the code" found nothing. Now connecting a repo to an empty workspace **clones it in on demand** — eagerly on attach (with clone progress) and, as a guarantee, at the start of the next run — so the agent has the real code to read, answer about, and edit. An existing build in the workspace is never clobbered (that stays a pure push-target).
- **Edit a connected repo via a pull request (branch-and-PR workflow)** — pointed at a real repository, the agent works like a careful contributor: it cuts a **working branch** (`git_push` with `create_branch`), and opens a **GitHub pull request** with `open_pull_request` (title + a body covering what/why/how-verified, returned as a PR URL) instead of pushing straight onto the user's branch. It can **`git_fetch`/`git_pull`** to stay current and **`git_branches`** to see what exists. The PR uses the same per-user encrypted token (scrubbed from output) as clone/push. So "improve my code" lands as a reviewable PR + CI, not a surprise commit.
- **Connections hub + connect GitHub from the chat** — a dedicated **Connections** surface (sidebar → 🔌) is the home for every connector, built to grow. The **GitHub card** lets you connect/disconnect your account and **create a new repo right there**; the ad-platform connectors (Meta/Google/TikTok) live there too for admins. And under any chat there's a slim **push-target bar**: connect GitHub and pick (or create) the repo *this* build pushes to, inline — no dialog needed. The bar reflects your real connection state (once connected it shows "GitHub connected as @you — choose a repo" rather than still prompting you to connect) and updates instantly when you connect or disconnect anywhere in the app.
- **Prepaid org wallet + invoicing (USD-canonical)** — each organization has a **prepaid balance** it spends as it builds. Usage is **debited automatically** at the end of every run (the real USD cost), recorded in an **append-only ledger** that doubles as the statement/invoice (top-ups + usage, with running balance), exportable to **CSV**. An **operator tops up** an org's wallet from Admin (amount in USD or the org's local currency); the top-up flows through the *same* credit path a **Stripe/PayPal** webhook will use later (the gateway abstraction is scaffolded — `payments/provider.ts`). All money is integer **micro-USD** so balances never drift. Spend **enforcement is observe-first** (a kill-switch, off by default): usage + balance are tracked and a **low-balance banner** shows, and turning `WALLET_ENFORCE` on blocks new runs at zero. Idempotent charges (a re-fired run can't double-bill); strict per-org isolation (an org admin only sees its own wallet; only the operator tops up).
- **BDT and other floating currencies, shown honestly** — alongside the fixed-peg GCC set, costs can now display in **Bangladeshi Taka (৳)**. Because BDT *floats*, USD stays the **canonical** number and BDT is a clearly-labelled **indicative** conversion: the rate is **operator-set** (in Admin, stamped with an "as of" date) — a live FX feed is a drop-in upgrade — and the conversion **basis is always visible** ("৳128 · 1 USD = ৳122 · 24 Jun"), with USD shown alongside. The wallet, charges and invoices all settle in USD; BDT is display-only.
- **Reads SCANNED PDFs by sight (accurate PDF→Excel)** — a text-only extractor (`pdf-parse`) returns *garbage* for scanned/image documents (e.g. bank challans, invoices), silently destroying a table's structure so the model has to guess — which drops suppliers, mis-files months, and scrambles totals. Now, when an uploaded PDF's text extract looks unreliable, ArksAI **renders each page to a high-resolution image** and routes the agent to **read the pages with vision (`see_image`)** instead of the dead text — the same way a person would. The steering also mandates the right discipline for document→spreadsheet work: **transcribe every line item** (with its own date/month, never inferred) into an **audit-trail sheet**, build the summary with **live formulas**, **reconcile each source document's extracted total to its printed total**, and **de-duplicate** repeated documents — so a missing row or a wrong month is *caught*, not shipped.
- Deliver out — `send_webhook` posts a result to a Slack/Zapier/Discord hook.
- **Email (per-org mailbox)** — each organization connects its own mailbox (SMTP for outbound, IMAP for inbound) in Settings → Email; passwords are stored **AES-256-GCM encrypted at rest** and never returned to the client. The agent can then `send_email` (with workspace attachments — a PDF, deck, sheet, or image) and `read_inbox` (triage/summarize/draft a reply), scoped strictly to the session's own org. A "Test connection" check verifies both legs before use.
- **One-step mailbox connect (auto-detect)** — connecting a mailbox (org OR per-robot) defaults to a **quick mode**: enter just the **email address + password** and ArksAI auto-discovers the SMTP/IMAP host, port and security, verifies the login, and saves — no server settings to look up. Detection escalates through a built-in provider table (Gmail/Outlook/Yahoo/iCloud/Zoho/GoDaddy/…), an **MX-record lookup** (Google Workspace / Microsoft 365 custom domains), then the **Mozilla/Thunderbird ISPDB** (`server/src/email/autoconfig.ts`, `POST …/email/autoconfig`). If detection or the sign-in fails, it **falls back to the manual form, pre-filled** with whatever was found (and a clear hint that 2FA accounts need an app password).

### The email robot (the engine behind the console)
- **Each robot has its own mailbox** (a Mailbox panel in the robot's office — SMTP + IMAP, encrypted at rest). Connect it and that robot becomes a working **email agent** with its own email identity: it polls *its* inbox and drafts a reply per new message, grounded in its mandate/knowledge. (An org-level mailbox also exists for the general chat/code agent's `send_email`/`read_inbox`.)
- **Drafts for approval (default)** — each reply waits in the **"Needs You" inbox** for one-tap **Approve & send** or Dismiss. Once trusted, set a robot to **auto-send**.
- **Safe by construction** — replies are **locked to the inbound sender** (a prompt-injected "forward to…" can't redirect them), the model sees only that one message plus the robot's own knowledge (data-minimized), and anything out of scope (refunds, billing, money, legal, angry complaints) **escalates to a human**.
- **Model choice** — robots draft with **ArksAI Max (M3)** or **ArksAI v4 (DeepSeek)**, or **Compare both**.

## Visual identity & theming
- Editorial light/warm identity — ivory canvas, Source Serif 4 + Inter + Space Grotesk (same fonts the reports use), publication masthead, hairline rules, per-department accent coding. Token-driven.
- **Subtle glassmorphism on floating chrome** — the top bar, dialogs, the completion card and the canvas head get a minute frosted-glass treatment (translucent tint **derived from `--bg-elev`** so the warm hue + accent are unchanged — only depth/translucency — plus a 1px light-catch edge), light & dark both. Reading surfaces (sidebar, chat bubbles, report/doc panels) stay solid for clarity. Enhancement-only: a `--glass-bg`/`--glass-blur` token pair behind an `@supports (backdrop-filter)` guard, fully reverting to solid under `prefers-reduced-transparency`.
- Dark mode for the Engineering (developer) team, with a smooth animated dark↔light crossfade (mobile + desktop, reduced-motion aware).

## Platform plumbing
- **Operator key rotation, no SSH (Admin → Keys)** — the super-admin can rotate the **DigitalOcean API token** and the **ArksAI Swift (BytePlus/Dola)** key from a new **Keys** tab in the Admin panel, without touching the Droplet or redeploying. Keys are **encrypted at rest** and **write-only** — the panel shows only whether each is set (a ● / ○ dot), never the value — and are picked up by the running server immediately (DB-backed runtime config, `app_settings`). Both endpoints (`POST /api/admin/providers/do` · `/byteplus`) validate the key prefix (`dop_v1_…` · `ark-…`) and are super-admin-gated (401 unauth, 403 for a non-operator).
- **Master-infra protection — the automation can never take the platform down** — the DigitalOcean client used by the Android build orchestrator **hard-refuses to destroy or modify the master ArksAI production droplet** (and the baked build snapshot). A two-layer guard checks by **id** first (`isProtectedResource` → `assertDestroyable`) and then defensively by **name/tag** before any DELETE, so even a bug or an AI-driven tool passing the wrong id can't reap the live host — only disposable build droplets (a distinct tag) are ever destroyed. The protected droplet is surfaced in the Admin → Keys panel ("🔒 Protected — never touched by automation"). This is the enforced "what the AI must not touch."
- **Per-session preview ports (no more canvas race)** — each session's in-canvas app preview now gets ONE unique, recorded port (range 42000–42999) via an atomic in-process allocator, instead of every app sharing port 4000 and the client scanning `/proc` to *guess* which port is which. Two users previewing two apps at once no longer collide or mis-load each other's app; the `/ports` endpoint returns the session's own port authoritatively (no noise-port mis-pick on reload). Flag-gated (`PREVIEW_PORT_ALLOC=0` restores the legacy path); verified with concurrent sessions each serving its own app.
- Projects — persistent workspaces (instructions, defaults, branding, knowledge base) that sessions inherit.
- Memory (global/repo/project scopes + ARKS.md); custom slash commands.
- Dual-driver storage (Postgres or SQLite); durable timeline; cost/token accounting.
- Auth: per-user accounts (email + scrypt password) + DB-backed, instantly-revocable sessions, OR the single operator password (now a platform **super-admin**); public lead endpoint allowlisted; PWA (installable: light manifest, service worker, offline shell; HTTPS via Caddy + Let's Encrypt once on a domain).
- **Hardened input handling + accessibility + speed** — every API endpoint returns a clean, actionable 4xx (never a 500) for malformed JSON, wrong-typed, or oversized/hostile input, and never silently accepts garbage; a global error handler surfaces the actionable message to the user while an unexpected server error is logged internally and shown as a calm, generic line (never leaking a DB/filesystem detail); uploaded filenames are sanitised against path traversal and downloads use RFC-6266 `Content-Disposition` so non-ASCII (e.g. Arabic) names download correctly and can't inject a header; outbound data fetches are SSRF-guarded **including across redirects** (every hop re-validated, so a public URL can't bounce to a private/metadata address); every form control across the app exposes an accessible name to screen readers; and the session-list/detail reads project only the columns they need (no longer hauling each chat's full transcript) so the sidebar stays fast at scale. Locked in by systematic test batteries (HTTP input-validation + error-leak, upload sanitisation, download headers, SSRF-redirect, CSV-parser edge cases, generator edge-cases, an authenticated cross-surface a11y sweep).

## Organizations (multi-tenant "spaces")
- **Isolated org spaces** — every session/project/deployment is scoped to an org; **everyone — members AND the platform operator — sees only their own workspace's data** (cross-org access → 404). The operator's **super-admin** role unlocks platform administration (provisioning orgs, reviewing the waitlist, managing any org's members), **not** a commingled view of other orgs' chats.
- **Roles**: super-admin (operator) → org **admin** → **member**; new sessions/projects are stamped with the creator's org.
- **Project-level visibility** — each project is org-wide by default, or **private to its creator + invited members** (enforced in every list/get query; cross-visibility access → 404). The owner or an admin manages sharing.
- **Invite-only, link-based onboarding** (no email infra): the super-admin creates an org + invites its admin; org admins invite their team — each invite is a one-time link to copy/share. Opening it sets a password and logs the member straight in; memberships revoke instantly (killing live sessions). Being **granted access removes that person from the waitlist** automatically (on invite issue and on accept), so the operator's review list only shows people not yet actioned.
- **In-app admin panel**: create orgs, **delete an org and ALL its data** (operator-only, cascades sessions/projects/published apps/schedules/commands/profile/analytics + removes member accounts orphaned by the deletion; the Default workspace is protected), review the waitlist (leads), manage members + invite links; team-member email login (the platform operator signs in separately at the unlinked `/operator`) and an `/invite/<token>` accept page. The invite field is a prominent full-width email; **free/personal email domains are blocked** (server-authoritative, gmail/outlook/…) and an invite whose domain differs from the admin's own is **flagged as out-of-org** (confirm to proceed). A company's workspace is its OWN and is **not swappable** — there is no workspace switcher in the UI for anyone (the sidebar just names the current workspace).
- **Cross-org isolation, audited & hardened** — every user-facing resource is scoped to the authenticated org (never a client-supplied id): sessions + all `/api/sessions/:id/*`, projects (+ creator/invited visibility), **the live sidebar event stream** (a tenant only receives its own org's session status/titles), **deployments** (list + stop/restart/delete — a slug from another org → 404), **schedules** (list/create/enable/delete, and a fired run lands in the owning org), **custom commands** (per-org keyed `(org_id, name)` so orgs never see/overwrite each other's), and org **memory**. Mutating a project (edit/delete/upload) additionally requires being its owner or an org admin. Backed by isolation tests **including an HTTP-level red-team** that signs in as one org and confirms it cannot reach another org's data on any route (list, by-id, sub-routes, or destructive writes) — and a check that the **operator** is confined to its own workspace (it opens its own chats but gets a 404 on another org's session/events/projects/schedules/deployments) while keeping the admin provisioning surface.
- Shipped **non-breaking**: the existing operator login keeps its platform-admin powers via the super-admin role (now scoped to its own workspace for data reads, like every tenant), and all prior data lives in a bootstrapped "Default" org.
- **Per-org shared brain + an onboarding designed to be FINISHED** — a new org admin gets a fully-visible, agent-led first run built on how anticipation works (dopamine = a seeking signal driven by a reward that beats expectation): a vivid promise + one tiny ask opens a loop they want to close, an **endowed 3-step tracker** (Brand → Profile → First build) shows them already moving, and a **live brand-reveal panel** beside the chat lights up with their real accent, palette and "about" the moment ArksAI reads their site or logo — the studio visibly becomes theirs. ArksAI **crawls the org's website** **or reads an uploaded logo's colours** (`extract_palette`, deterministic — and vision works in every mode, so it never claims it can't see the image), saves the brand early so the panel pops, asks a couple of quick questions, then `save_org_profile` (complete) and tees up an exciting first build. That brand + "about" + a recalled digest of the org's own past chats are injected into **every** session in the org (brand applied consistently unless the user overrides). Members joining later inherit it silently; the admin can re-run/skip/edit.
- **Strict per-org memory isolation** — org memory lives under an `org:<id>` scope derived only from the authenticated identity (never client input). A tenant org reads/writes **only its own** scope — never the deployment-`global` notes, never another org — enforced in the runner's context builder *and* the memory API (read/write/delete), with repo memory namespaced per org. Cross-org isolation is unit-tested.

## Analytics & insight (admin)
- **User feedback / rating system** — every finished result carries a quick **👍 / 👎**, and a thumbs-down opens a short "what's wrong?" box for the user's own words. Ratings are captured server-side (`feedback` table), **org-scoped** (a complaint is never attributed to or readable by another org), and **content-safe** (a write-side allowlist persists only metadata — mode/task/deliverable kind/name — so raw chat or document text can never leak into a feedback record). The **operator console** shows "What users said" — the down-rated complaints with their context, a 👍/👎 tally and an open-complaints count, each markable reviewed — so the team sees exactly what's failing and can act on it. (Endpoints: `POST /api/sessions/:id/feedback`; super-admin `GET/PATCH /api/admin/feedback`.)
- **Country-specific cost display (per-org currency)** — costs are computed in USD but **shown in each organization's chosen currency**, starting with **AED** (the platform default). An admin picks the org's currency in the Admin panel and it flows everywhere cost appears — the per-session cost bar, the `/cost` command, and the operator + per-org analytics consoles (KPIs, charts, tables). GCC currencies are USD-pegged at fixed rates (AED 3.6725, plus SAR/QAR/BHD/OMR), so conversions are exact with no rate feed; more currencies can be added later.
- **Operator analytics console** (super-admin, full-screen from the sidebar) — a **dense, editorial "data desk"** (tabular figures, hairline rules, **week-over-week delta chips + inline sparklines** on every headline metric for fast reading) to understand how the whole platform is used at a glance: headline KPIs (active users 7d/30d, **stickiness = DAU/MAU**, sessions, success rate, **time-to-first-build**, cost, orgs/users, live apps, waitlist), active-users/sessions/cost-per-day trends, the **acquisition funnel** (lead → signup → onboarded → built → published), **feature adoption** by play, splits by mode and model, quality (done vs error), cost by model, a **signup-cohort retention** grid, and named **per-org and per-user drill-down** tables.
- **Per-user drill-down** — click any user (operator) or member (org admin) to open a metadata-only detail panel: their sessions, success rate, cost, time-to-first-build, a 30-day activity chart, what they build (plays/modes/models), and their orgs — scoped to the viewer's permissions.
- **Activation: time-to-first-build** — median time from signup to a user's first build, surfaced as a KPI and per-user, so onboarding friction is visible.
- **Actionable alerts (no setup)** — derived from existing metrics with no thresholds to configure: **cost-spike** detection (an org's last-7-days spend vs its trailing weekly baseline) and **churn-risk** orgs (onboarded + once-active but gone quiet) for the operator; per-org admins see their own cost-spike + inactive members. Quiet by default — shows "all clear" when nothing's wrong.
- **Scheduled digests** — a periodic platform snapshot (every `ANALYTICS_DIGEST_HOURS`, default daily) is computed and stored for the operator to review in-app (with deltas vs the previous one), and pushed to a webhook if `ANALYTICS_DIGEST_WEBHOOK` (Slack/Zapier/…) is configured.
- **CSV export** — one-click download of the orgs, users, and per-org members tables (client-side, no round-trip).
- **Per-org dashboards** — an org admin (or the operator drilling in) gets the focused subset inside the admin panel's **Usage & engagement** tab: engagement KPIs, active-members/sessions trends, the activation funnel, what the team builds (plays/modes/models), retention cohorts, alerts, and a clickable per-member activity table — scoped strictly to that org.
- **Lightweight event-tracking layer** — a non-blocking `track()` writes usage events (`app_open`, `run_finished`, …) to an `analytics_events` table with a precomputed epoch-day bucket (zero date-SQL, SQLite+Postgres portable); metrics also derive from existing tables so they work from day one and get richer as events accrue.
- **Privacy is absolute — metadata only.** Analytics read only counts, timestamps, status, mode/model, the play key, tokens and cost; they **never** touch the `timeline` table or any message/document content. Enforced at both boundaries (a write-side scalar-only `props` allowlist + a grep-able test that the analytics modules contain no content reads) and gated end-to-end: operator routes are super-admin-only, per-org routes require that org's admin (a per-user drill is membership-gated so an admin can't probe non-members) — covered by an HTTP red-team test (a non-operator gets 403 on platform analytics; an org admin cannot read another org's).

---

## Status & honest caveats
- **343 automated tests green**; typecheck + build clean.
- Anything needing the **model key or open egress** (live builds, real vision calls, scheduled runs firing, external data/webhook delivery) is wired + unit/integration-tested but fully exercised only on the **Droplet**.
- **Staged (needs Droplet credentials):** OAuth Google Sheets/Drive & CRM connectors, a Slack app, SMTP email.
- **Multi-tenant org platform — core SHIPPED** (per-user accounts, roles/invites, org-scoped data, in-app admin panel). Remaining: the invite-only landing revamp, per-org department templates, and the AUDIT-P0 low-priv agent uid-drop before any untrusted/self-serve org. The live multi-user flow should be validated on the Droplet.
