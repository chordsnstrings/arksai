# ArksAI — Feature List

A self-hosted, Claude-Code-style web coding agent for **non-technical teams**: describe
something once → get one finished thing that looks perfect, works perfectly, and is
actually live/usable. Powered by MiniMax (the LLM brain + image/vision/speech/video), with Suno for music.

> **Maintenance:** keep this file current — every shipped change that adds, removes, or
> meaningfully alters a feature should update this list in the same commit.
> _Last updated: 2026-06-24._

---

## Robots — the agentic surface
- A **separate, full-page workspace for standing agents**, reached from a **"🤖 Robots"** link in the sidebar and linkable at **`/robots`** — its own destination. The classic **Chat stays exactly as it is**; Robots is an additive second surface.
- **Role-branded agents** (Finance Agent, Marketing Agent, Sales Agent… one per department, with the department's accent + icon) instead of one-shot chats — **standing teammates** that work and check in with you when they need a decision.
- **Three surfaces**: a **roster** ("Your robots"), a **"Needs You" approval inbox** (Approve & send / Dismiss cards — the async heart of the product), and a **per-robot office** (editable plain-language **mandate**, **autonomy dial** — *Ask me before everything → Ask me for the big things → Just keep me posted* — **triggers**, journal, deliverables).
- A guided **Hire wizard**: (1) pick the kind — **Customer assistant / Personal assistant / Department specialist** (this sets the robot's real reply persona, and Specialist reveals the department grid for its expertise) → (2) teach it (name, mandate, knowledge, escalation rules, signature) → (3) **connect its own mailbox** inline (Test connection; skippable — it stays paused until connected) → (4) autonomy + triggers → Activate. The roster and office show each robot's mailbox status ("connected ✓ / needs a mailbox").
- **Backed by a real, durable per-org engine** (`/api/orgs/:id/robots`): robots persist, run on the server, and their pending work fills the "Needs You" inbox. The **first working robot type is the email robot** (below) — connect a mailbox and a robot drafts real replies you approve & send. (Department-specific automations beyond email are the next build.)

## Core product & agent
- Autonomous agent loop — streaming, tool-calling, stall guard, context truncation, graceful error handling.
- **Shareable per-chat links** — every session has its own URL (`/s/<id>`) reflected in the address bar (deep-link / bookmark / reload reopens it — including older chats past the first page, fetched on demand; back-forward works), plus a **🔗 Link** button in the top bar to copy it. A link to a deleted or inaccessible chat shows a clear "this chat isn't available" panel (the URL is kept so it can be retried) rather than silently dumping you on the home screen.
- One seamless chat — no modes to pick. The user just talks; ArksAI reads the request and moves itself into whatever it needs — chat, plan, build (code), report, or image generation — with the right curated toolset. Under the hood there are still 4 modes (Chat / Plan / Code / Report) + capability tools, but the user never toggles them (a read-only chip in the TopBar shows what it's doing). Power users can still force one with `/mode`.
- Tooling: file read/write/edit, glob/grep, bash + persistent background processes, git, web search/fetch.

## Orchestration & engines
- **One engine: MiniMax.** ArksAI runs entirely on MiniMax — the LLM brain (M3 + the fast M2.7-highspeed model) and all multimodal capabilities (vision, image, speech, video). No model menu for everyday use: the chat picks the model itself.
- **ArksAI Auto (the default)** — picks the concrete model per task: **M3** for quality/visual/agentic/report/legal work, the **fast model (Flash)** for quick/light turns; escalates Flash → M3 when verification needs more muscle. Server-authoritative cost in the footer. Power users can still force **ArksAI Max** (M3) or **ArksAI Flash** (M2.7-highspeed) via `/model` or the advanced New-session dialog.
- **ArksAI Max** — MiniMax M3. Called via M3's **Anthropic-compatible endpoint** (thinking off by default → fast, decisive tool use; the OpenAI surface forces unbounded thinking and stalls), through an adapter that keeps the rest of the agent loop unchanged. **Never hangs, quality kept first:** the rare long-build "hang" was root-caused (raw-API probing) to **server-side starvation when several M3 turns run at once** — the losers get keepalive pings (so a gap-based timer never trips) while producing no output. Fixed with **(a) a global concurrency limiter** (caps simultaneous M3 streams so they can't starve each other; excess turns queue), **(b) a content-based stall detector** (the idle timer resets only on *real* content, not pings, so a starved no-output turn is caught and the run auto-switches **within MiniMax to the fast model** to finish), and **(c) a quality-first policy on big outputs** — large single-shot generations (a full spreadsheet/deck/doc) are now *allowed to finish on M3* rather than bailing early to a weaker model (a head-to-head bake-off showed M3 completes a detailed 3-statement financial model with hundreds of live cross-sheet formulas, where the alternative didn't finish). A turn that's genuinely large surfaces a **"still working — hang tight"** note so a long-but-healthy build never looks frozen. **Quality-first stall recovery is per-turn:** a single slow M3 step finishes on the fast model for *that step only* and the run then **returns to M3** (each staged build step is small, and M3 references cells far more accurately on a financial model) — only after repeated stalls does the whole run drop to the fast model.
- **MiniMax capability tools:** `see_image` (vision), `generate_image`, `text_to_speech`, `generate_video` (Hailuo).
- **Suno** music engine — full toolkit, per-track cost: `generate_music` (AUTO mode from a description, or CUSTOM mode with a rich ~1000-char style field + structured lyrics; controls for vocal gender, style weight, weirdness, and negative tags to exclude styles), `generate_lyrics` (structured words only, no audio — free), `extend_music` (lengthen/continue a prior track), and `cover_audio` (AI-cover a source track in a new style). The agent is steered as a Suno expert (style = comma-separated components, vocal cues in parens, strongest line first) and confirms the brief before the first paid generation. Default model is config-driven (`SUNO_MODEL`).

## One-shot quality system
- Task classification (`taskProfile`) → deliverable type + isVisual + tier.
- **Curated, all-muted design library (house style: minimal · modern · muted)** — the bundled UI kit gives the agent a tight, restrained set so output stays calm and considered by default (loud/high-contrast looks were deliberately removed): **5 complete design kits** (one-token looks via `data-kit` — minimal/paper/linen/calm/harbor, each bundling a muted palette + radius + shadow + font pairing), **10 muted colour themes** (`data-theme`: slate, ink, indigo, ocean, sage, teal, clay, stone, plum, olive — soft desaturated accents, light/dark), and **8 clean typography pairings** (`data-type`: geometric · editorial · clean · startup · tech · journal · warm · humanist — clean modern + refined serif, no high-contrast display faces). All from **11 self-hosted typefaces** (Inter, Source Serif 4, Space Grotesk, Fraunces, Outfit, Sora, DM Sans, Manrope, Plus Jakarta Sans, Lora, Newsreader — no network needed). The base accent default is a muted slate, and the agent is steered to use ONE soft accent at ~5–10%, never a saturated fill. The agent offers one quick curated look (a kit, or a colour + pairing) matched to the brand/audience, then builds automatically. Same system powers websites, reports and decks for one consistent identity.
- Opinionated design system + a bundled UI kit (tokens/components/themes, `add_ui_kit`) injected for visual tasks; per-type design standards (web/app, report, docx, xlsx, pptx). The design contract is **typography-first, minimal, and high-quality by default**: a real modular type scale + refined font pairing, a **distinctive confident palette** (not the generic "AI-default" blue) **validated for WCAG-AA contrast** via `validate_palette`, purposeful **micro-interactions** (hover/focus/enter, reduced-motion respected), real interactive/empty/loading/error states, and a senior-design-director self-critique. Enforced, not just suggested: the build gate runs a **deterministic mobile-responsive check** (renders at 390px and hard-fails a layout that overflows the screen), a **deterministic text-legibility check** (measures WCAG contrast on every text block vs its background — including copy placed directly in section bands, cards and heroes, not just `<p>` tags — and hard-fails washed-out muted text or borderline-low-contrast buttons that vanish into the page; a muted tone is allowed only while it stays legible) **including interaction states** (it actually hovers every button/link and re-checks contrast, so a control whose background changes on hover but whose text colour doesn't — making the label disappear — is caught, not just the resting state), plus the M3 design-director rubric (now flagging the generic-AI look *and* any hard-to-read text over an image), so an output that isn't responsive, has illegible copy, or doesn't look genuinely designed gets revised before it ships.
- **Shared icon library — one beautiful Lucide set, everywhere** (`server/src/agent/tools/icons.ts`): a curated 120+ line-icon set (24×24, consistent 1.75 stroke) grouped by purpose — brand/lifestyle (coffee, leaf, gift, heart, sparkles…), nature/weather, communication (mail, send, phone, bell…), commerce (shopping-cart, tag, wallet…), data/charts, people, media, UI/navigation, places/travel and tech — used across **every surface**: websites & apps (shipped as a recolourable `icons.svg` sprite by `add_ui_kit`; reference with `<use href="ui-kit/icons.svg#coffee">` and tint via CSS `color`), reports & docs (inlined paths), decks (pptx KPI tiles), and social creatives. **One source of truth** (the sprite is generated from the icon library, with a drift guard test), and the available icon names are advertised to the agent in the tool results + design prompt — so it picks the *right* on-brand icon instead of hand-rolling mediocre SVGs or falling back to emoji.
- **Universal visual-quality gate** (`deliverableCheck`) — EVERY deliverable is rendered to image(s) and design-reviewed by a senior-design-director rubric (M3 vision), then bounded-revised: web apps (live Chromium), PDF reports/decks (mupdf raster, per page), .xlsx/.docx (HTML render), and .pptx (LibreOffice or a faithful preview). The same "looks perfect, works perfectly" guarantee across all types, not just web apps. A **deterministic, model-free structural pre-check** (per-page ink coverage **and per-page content extent**) catches both lonely near-empty pages **and under-filled pages** (content that stops high on the page leaving a big blank lower band — the ≥60% page-fill rule, which interior pages must meet; the cover and final page are exempt) instantly — even when no vision model is available — so each revise round is cheap and targeted (reports now allow up to 2 quality-first revise rounds). The gate also covers the cases that used to slip: generated **images** are validity-checked (a 0-byte / truncated / error-page download is rejected, not shipped); an **app built in report mode** gets a real runtime boot + smoke-test (not just doc rendering); the **formula audit** flags a typed-in derived row (Total/Growth/Margin) even when the rest of the model uses formulas; and if the visual review ever can't run (vision/renderer unavailable), it **says so out loud** instead of silently passing.
- Per-type functional check ("it actually works"): xlsx re-open + no #REF/error cells **+ calculation models must be formula-driven** (a finance/cash-flow/budget/forecast sheet built with hard-coded totals/balances/growth is auto-flagged and sent back to add live `=SUM(...)`/cross-sheet formulas — the vision gate can't catch this since it renders computed values, so this reads the cells' formulas directly); docx re-open + embedded fonts; pptx valid slides; pdf page/blank/bleed; apps boot + routes + interaction. **A numeric-sanity gate** then catches a model that's formula-driven but whose numbers still can't be right (the cross-reference failure mode): an **absurd outlier** (a 140‑million line in a small-business model — a formula that landed on the wrong cell), a **rate/percent leaked into a money column** (a revenue cell reading 0.30 instead of 6,000), or a **derived row (Total/Net/Balance) computing to 0** while real numbers exist — and a **section/divider-row detector** flags a plain-text or em-dash divider ("SPACE & LEASE", "—— REVENUE ——") interleaved in a calc sheet, since it shifts every row and corrupts absolute references. Both are deterministic (no vision needed) and feed the bounded revise loop.
- Vision runs on M3's Anthropic endpoint (thinking off → fast, decisive; bounded by a timeout so it degrades, never hangs).
- Visual model floor — non-trivial visual/report work never runs on the cheapest model.

## Bulletproof verification
- Static gate (typecheck/lint/test/build) + runtime gate (boots the app, exercises routes).
- Parameterized-route filling (`:id`/`{id}`/`<int:id>`/`:slug`) + an interaction pass (seeds inputs, submits a form, clicks primary buttons).
- Headless-Chromium UI smoke test (blank/console-error/failed-request detection).
- **Mobile-menu check** — at phone width the gate finds the hamburger/menu toggle, taps it, and **fails the build if the navigation doesn't open** (no nav links appear). Catches the dead mobile menu that a desktop-width pass can't see, with an actionable fix note for the agent.
- Post-publish smoke test of the real `/apps/<slug>/` URL — a broken deploy is caught and handed back to the agent, never the user.
- **Resilient app/preview proxy** — the reverse proxy that serves published apps (`/apps/<slug>/`) and the in-app preview reads the upstream response defensively and retries once on a dropped/stale connection, so a transient "premature close" can't surface as a 500 error page; an upstream that's genuinely down returns a clean "restart it" 502 instead.
- **Resilient model streaming** — if the connection to the model drops mid-reply (a "premature close" / terminated socket), the agent reconnects and redoes the turn (bounded), and a `turn_reset` event clears any partial output so the retry never shows doubled text. If M3 stalls, the run switches within MiniMax to the fast model to finish — so a slow or flaky turn still completes instead of dying with an error.
- **Self-contained apps (no CDN)** — built apps vendor their libraries (charting, etc.) locally and self-host fonts, with one real root `index.html`; nothing is pulled from an external CDN at runtime, so a published dashboard's charts can't silently break when a third-party CDN rate-limits/403s.

## Deliverables & deployment
- **Publish & share a 24-hour preview** (`publish_app` + the TopBar "Publish & share") — ship a site/app to a **public URL anyone can open, no login** at `/apps/<slug>/`, served via a process registry that survives restarts (boot recovery). **Any app type works**: a plain static site, a **React/Vite (or CRA/Vue/Parcel) SPA** — which is *built* (`npm run build`) and served from its `dist/`, never run as a dev server — and **Node/Python server apps**, reverse-proxied. A small injected shim makes a published app's root-absolute calls (`fetch('/api/…')`, XHR, WebSocket) and assets resolve correctly under the `/apps/<slug>/` path, so an app's API actually works in the browser, not just the page load. Every publish is a **24-hour preview**: a boot-started janitor auto-deletes it (process + files + record) 24h after publishing; re-publish to refresh. The dialog shows a live "expires in …" countdown. Opening a published link from the **installed PWA** hands off to the user's real browser (a true external window) instead of taking over the app window, so the ArksAI session is preserved. (Single-Droplet constraint for now — a permanent tier comes with a bigger server; pre-existing deployments without an expiry are left running.)
- Auto-export (zip download chip) + auto-canvas — the canvas auto-opens and loads the result (web app, PDF, spreadsheet, or doc). Generated **compliance machine files** (WPS `.sif`, PINT AE e-invoice `.xml`) are surfaced as downloads too, so a produced filing actually reaches the user.
- Styled office docs: `generate_spreadsheet` (exceljs, formula-driven, validated — and a built-in **formula recalculator** recomputes each formula's cached value before saving, so the in-app preview shows correct numbers instead of a model's stale/0/blank cached value; this now runs **authoritatively on the built workbook using each cell's real coordinates**, so it can't be defeated by a title row, merged cells or object-keyed rows that previously left formula cells blank in the preview; the formula is preserved so the download always recalculates in Excel — and **every formula cell is guaranteed to survive**: ExcelJS silently drops a formula that has no cached result, so a formula our evaluator can't compute (NPV/IRR/lookup, refs to text) used to vanish entirely — it now always keeps a placeholder result so even advanced formulas are preserved, never lost). **Large, granular, multi-sheet models build incrementally** — a 3-year **month-by-month CAPEX + OPEX** model (Assumptions → CAPEX → OPEX → Personnel → Summary, with cross-sheet formulas) is assembled **one sheet per call** (`append: true`) instead of one huge payload, so a detailed model **reliably finishes and returns output** rather than stalling, while staying fully formula-driven and gate-passing. **Total/roll-up rows are auto-styled** (bold + a hairline top rule) so a model reads like a real financial statement. **Cross-sheet links can't silently break**: a model that writes a reference without the leading `=` (e.g. `Assumptions!$B$10` typed as plain text — which used to sit inert and break every dependent cell with `#VALUE!`) is **auto-healed into a live formula**, the design gate **flags any stringy reference that slips through**, and every workbook is marked **recalculate-on-open** so Excel/Sheets always show correct numbers. The preview recalculator now also evaluates **`IF()` and comparison conditions** (`>`, `<`, `>=`, `<=`, `=`, `<>`), so an `IF(C4>0,…)`-driven row and everything downstream show the right values, not 0. The model is steered to a **clean calc structure** (header = row 1, data = row 2+, no decorative banner/title rows inside a calc sheet) with a deterministic **banner-row detector** that flags a `── SECTION ──`-style row which would shift cells and corrupt formula references, so totals/balances point at the right numbers, and to **research real drivers** (rents, salaries, equipment costs) for the Assumptions sheet rather than inventing figures. `generate_doc` (docx) — documents embed the editorial typefaces (Source Serif 4 display + Inter body, the same as the reports/app), so they render designed everywhere instead of as office default. `generate_doc` also takes a **designed cover page** (masthead · accent title · one-line thesis · a **KPI band** of headline numbers · metadata footer) and `chart` blocks that embed **publication-grade `render_chart` images** — so a brief/report reads designed end-to-end, not as a wall of text.
- **`generate_pptx`** — real, editable PowerPoint (PptxGenJS): editorial 16:9 decks with a **designed cover** (masthead · accent title + thesis · a **KPI band** of headline numbers · metadata footer · `CONFIDENTIAL`) and a **dark/light slide rhythm** (per-slide theme), **publication-grade charts** via `render_chart` (dual-axis/heatmap/… embedded as crisp images, beyond the basic native bar/line/pie), title/section/bullets/two-col/stat/quote/table/image layouts, one accent, and an embedded preview for the canvas + the visual gate.
- Report mode — PDFs and 16:9 decks with a full editorial protocol (newspaper margins, anti-orphan `.lede`, kickers, rules, page mechanics); charts/figures are ATOMIC (`.keep`/`.fig`) so nothing splits across a page, and every page is auto design-reviewed before delivery. A **"get the first render right"** rule set targets the three defects the gate used to churn on — **lonely / half-empty pages** (forced per-section page-breaks are now forbidden; content FLOWS to fill ~85–100% of every page, a stray card grid sits with its narrative, never alone), **cornered/tiny charts** (every chart is a full-measure `.fig` on its own row and the SVG is auto-made **fluid** so it fills the column instead of collapsing with colliding axis labels), and the **white-frame background** (the page colour lives only on html/body or a `.bleed` page) — so the first render passes review fast instead of looping expensive revise rounds. **Token-efficient by design:** once the PDF/deck is rendered, the raw web-research dumps from earlier turns (web_search / web_fetch / fetch_data — already baked into the document) are auto-trimmed from context so the QC/revise turns don't re-pay for them, cutting a research report's token use dramatically. If a run ever does brush the per-run safety budget **after a good document was produced**, it now **completes normally and shows the file** (with a soft "this took a bit more processing" note) instead of erroring over a finished PDF. The protocol also hardens the anti-half-empty rule (NO interior page below ~70% full — a short profile/overview section merges with its neighbour) and enforces a single **output language** (English, or the user's language) so stray non-Latin characters never leak into the prose.
- **`render_chart`** — publication-grade charts as embeddable SVG (Vega-Lite → SVG, server-side, no browser/Python): line, multi-line, **dual-axis** (volume bars + a trend line), bar/bar-h, stacked, area, donut, and **heatmap** (e.g. month×year). Editorial defaults baked in — flat 2D, muted base + the report accent on the key series only, direct value labels, light gridlines — so charts are on-brand by default and inline straight into the report HTML. The returned SVG is **fluid** (full-measure `width:100%; height:auto`, viewBox kept) so it fills its `.fig` block instead of collapsing into a corner, and the intrinsic size auto-grows with the data (more rows → taller) so axis labels never collide; it also rasterizes to a transparent PNG so the SAME editorial charts embed into `.pptx` decks and `.docx` documents.
- **Two deterministic quality tools** — `validate_palette` checks a brand palette for **WCAG 2.2 AA contrast** (body text + links at 4.5:1, accent/large text/UI at 3:1) before any visual build and returns a corrected colour for any failing pair plus a ready-to-use token set (primary/accent/bg/surface/text/muted/line/link) so text is never shipped unreadable; `compute_financials` runs the **DCF (NPV + terminal value), the standard ratio set, budget variance (favourable/unfavourable), and a linear/growth forecast** so every derived number in a report or sheet is computed, not hand-typed. Both are pure, instant, no key needed, available in chat/code/report, and the relevant finance + design standards steer the agent to use them.
- **Designed, full-bleed cover** — the report cover is a **true edge-to-edge canvas** (a named zero-margin `@page` so the background field reaches all four paper edges — interior pages keep their margins), with a typography-led foreground: the **brand logo** in the masthead, accent title line, one-line thesis, a **KPI band** (even cell padding, breathing room), and a metadata footer. Dark / deep-accent / light field options.
- **Analysis & methodology rigor** — the report protocol now demands a systematic analysis pass first (profile columns, full cross-tabs, reconcile conflicting fields), an insight-led narrative (counter-intuitive reframe → evidence → ranked recs tied to specific data points), and a Methodology/Notes section (proxies + their limits, data gaps, source attribution).
- **Android Apps — a dedicated page** (`/android`, 📱 in the sidebar): a guided intake wizard (app name + what it does · app type/archetype · a **visual style kit** — Material You / Minimal-Editorial / Bold-Playful / Clean-Pro / Dark-Premium · how it runs: native APK / PWA / both · a broad set of **device capabilities** — camera/QR, photos, location, notifications, contacts, microphone, biometrics, calendar, files, audio/video, haptics, share, Bluetooth, NFC · accounts/sync/realtime backend · a 12-colour accent palette) composes a precise brief. Because a good native app needs a plan, the agent does a short **plan + clarifying round** (proposes screens, data model, flows, permissions; asks a couple of essential questions) and waits for approval before the long build — then builds fully autonomously and hands off to the live session view. Native APKs are compiled on an ephemeral build machine (Kotlin-pinned Expo prebuild + Gradle, never EAS) and returned as an installable file; if a backend is needed it auto-deploys to our infra and the client is wired with a typed JWT API. Honest about timing.
- **Publish to Google Play — per-org developer accounts**: each company connects its **own** Google Play Developer account on the Android page (admin-only), so apps publish under that company's identity + billing — never a shared account. The service-account credentials are stored AES-256-GCM encrypted at rest, write-only, org-isolated (admin-gated). *(Connection scaffold is live; direct-to-Play AAB upload is the next step — until then native builds hand back a sideloadable APK.)*
- In-app doc viewer (xlsx/csv/docx/pptx → styled HTML); Canvas preview (Preview/Files/Doc tabs, port auto-detect).
- **Mobile apps (in progress)** — a bundled **mobile UI kit** (React Native / Expo: light+dark design tokens, ThemeProvider/Screen/AppText/Button/Card/Field/EmptyState/Loading + a root crash-safe ErrorBoundary, `add_mobile_ui_kit`) and a matching **app backend kit** (Fastify + SQLite: JWT auth, a sample CRUD resource, a consistent error envelope, per-route JSON-schema validation, CORS, idempotent migrations, `add_app_backend`) that installs the API into `server/` **and a matching typed client into `src/api/`** (a JWT+AsyncStorage fetch wrapper that unwraps the error envelope, plus login/register/me and a single `API_BASE_URL`) — so a generated app's screens *and* its API are wired together out of the box: the backend deploys zero-config to our infra (`<slug>.apps.arksai.studio`), you set one URL, and every screen calls it the same safe way. Screens *and* the API both start from a minimal, modern, typography-first, validated foundation — the same house quality as the web builds. PWA is the default lightweight path; native Android APKs build on our own infra (Apple/iOS via EAS).

## The flow (effortless AND expert)
- **Home / Workspace** — the default screen when no chat is open is now a real **workspace hub**, not just a task launcher (one consolidated, role-aware look — the layout is identical for every team; only the content adapts). It stacks: a personalized greeting, a **"Needs you"** strip (robot replies to approve, a failed publish to fix, low balance), a **"Jump back in"** row of recent work, the **"Start something"** zone (the team picker + describe-box), **"Running for you"** (your live apps with their URLs, scheduled tasks with their next run, active robots), and **"Your team can also…"** — a few one-tap capability cards (in each team's language) that surface the platform's breadth (hire an inbox robot, schedule a recurring report, connect your tools, design a creative) without a feature menu. A brand-new user with nothing yet just sees the greeting + picker (exactly as before); a returning user sees their whole workspace.
- **Activity — one inbox for everything that runs for you** — a dedicated **Activity** surface (sidebar → 🔔, linkable at `/activity`) gathers all the background work the platform does into a single tended feed: a **"Needs you"** tab (approve & send / dismiss each robot reply right there, fix a failed publish, top up a low wallet) and a **"Delivered"** tab (finished scheduled runs, click to open). The sidebar shows a live badge so it's never invisible.
- **Recents-first sidebar + one-tap tools** — your **recent chats sit right at the top** (under New session + search), taking the main space, so getting back to past work is instant — no hunting or searching. Everything else (Activity · Robots · Scheduled · Live apps · Android · Connect · New project · Admin) is a **compact, labeled strip pinned at the bottom** with live count badges (drafts waiting, a failed publish, low balance) — discoverable but never pushing the chats down. A ⌂ Home shortcut sits on the Recent header, and a global **Live apps** view lists every app you've published across the workspace.
- **Pick where the work goes, from the start** — the describe box has a **Connect** control next to Attach: choose the **GitHub repo** this build should push to before it even begins (or connect your account inline), with a one-tap path to the Connections hub for email / ad platforms. The chosen repo is attached to the new session at creation, so code lands in the right place from the get-go (the same picker is also under any active chat).
- Smart intake — a short, type-aware brief (one round), then fully autonomous.
- Launchpad — input-first onboarding: a **personalized, time-aware greeting** opens it (by name — Claude-style "Coffee and ArksAI time, Kamran?", with varied lines per part of the day and an editable name that's **remembered server-side**, so it follows you across devices), the **chat box sits at the top**, and the team's example tasks are denser below — there to pick when you want them. The describe box accepts **file uploads, drag-and-drop, and clipboard paste** (staged, then attached to the new session before its first run), just like the main composer.
- Live "smart-work" progress bar — named phases with an anticipation creep so it never looks frozen; technical work stays visible (visible competence = trust). It also shows an **estimated time remaining** that counts down through the phases (slightly generous so a run usually beats it — a small positive surprise, not a broken countdown); when a build runs past the estimate it eases into a soft "larger job — hang tight / almost there" instead of a stuck 0:00. The estimate is **self-calibrating, and gets sharper the more you use it** — each completed run folds its real per-phase durations into a rolling average kept at two levels: a broad **per-mode** prior AND a precise **per-(mode + task)** bucket (e.g. a finance cash-flow model vs a quick doc tweak — both `code`, but minutes apart). The countdown uses the task-specific history when it has enough samples and falls back to the mode average otherwise, so it converges on what THIS deployment actually does for THIS kind of request instead of a fixed guess. Learned timings persist on disk, so they survive restarts and keep improving across sessions.
- **Build you can watch (engagement-tuned)** — the moment you hit go the bar + footer appear (optimistic, zero gap); a live **play-by-play ticker** names the current action in human terms ("Looking at your image", "Setting the typography", "Designing the PDF", "Publishing it live") and auto-expands while working; switching chats never flashes; if a step needs retrying the studio says so kindly (plain language, never a scary error).
- **A real "it's ready" moment** — a successful build ends on a staged completion card with a **live thumbnail** of the finished app/document, one-tap **Open** and **Share** (✓ Live · Copied ✓), and a one-tap **"Fix & republish"** if a publish check fails — fires for every build, including static single-file sites. Publishing walks its real phases (Snapshotting → Installing → Booting → Checking the live URL) so the wait shows motion.
- **Completion time on every result** — each finished reply shows a quiet "Completed in 3m 12s" — the wall-clock from your message (input) to the result (output). It's **stored with the chat** (in the timeline), so it's still there on reload and across devices, letting you see how long each input vs output actually took.
- **Pick up where you left off + calm dialogs** — the home screen shows a "Jump back in" row of your recent work; destructive actions use a styled in-app confirm (no jarring browser popup) and **Esc closes any dialog**; the live preview locks onto the exact port the build produced (no more wrong-port "Error").
- Self-healing reframed as confident "hardening it (pass N)" forward progress, not failure.
- Delivery moment — "Booting your live app…" loading + retry, and a completion card ("✓ Your app is ready" → Open / Get a shareable link) with a **"What's next?"** row of contextual follow-up chips (e.g. add a feature, tweak the design, turn a report into a deck) that pre-fill the message box so there's always an obvious next step.
- One-time "what's new" modal — shown once per user per version (remembered locally) right after sign-in, to keep people in the loop on recent updates.

## Smart agent
- Sees **every** upload, automatically — on each run the agent is told exactly what was just uploaded and how to open it (images → `see_image`, **available in every mode**; a logo → `extract_palette` for exact brand hex; **spreadsheets (`.xlsx/.xls/.csv`) → `read_spreadsheet`**, which lists EVERY sheet with dimensions + headers + samples and reads exact values on request — never a lossy text dump that silently drops sheets, and heavy analysis crunches the raw file with Python; **presentations (`.pptx`) → `read_presentation`**, which reads every slide's title/bullets/tables + speaker notes (never a binary blob); prose docs (`.pdf/.docx`) → their auto-extracted `.extracted.txt` sidecar; anything else → `read_file`), so it acts without the user re-instructing. **When several files are dropped at once (multiple spreadsheets + images + a deck), it processes EVERY one** — every sheet, every slide, every image — not just the first. It never claims it "can't view" an image — vision is always on. Files attach via the `+` button, **drag-and-drop, or clipboard paste** (copy a file/screenshot → ⌘/Ctrl-V); a `📎` chip shows what's attached. Uploads **retry automatically** through a transient network or deploy/restart blip (with backoff; a real over-limit `413` still reports immediately), so a momentary hiccup never surfaces as a "failed to fetch".
- **Make an image LIKE an uploaded one** — `generate_image` takes an optional `reference_image`: to recreate or vary an image the user uploaded, the agent looks at it (`see_image`) to read its subject/style/composition/palette and prompts from that, and for a person whose likeness should carry across it passes the reference directly to the engine (MiniMax subject reference). "Create an image like this" just works.
- **Brand from the logo** — `extract_palette` reads the real colours out of an uploaded logo (deterministic, no vision model needed) so the accent is taken from the brand; the **same identity (logo, accent, type) is applied across every deliverable** — web apps, decks, docs, and reports — and a deliberately beautiful palette is chosen when there's no logo.
- Auto mode/engine switching — `switch_mode` lets a chat become a build/report mid-conversation; the runner reloads the toolset, prompt, and engine on the fly with an inline note. It also routes **back**: a build/report session isn't sticky — if your next message is a different kind of request (a question, a deck, an image, a brand-new build) ArksAI moves itself to the right mode for it instead of forcing it through the current one. Built into the seamless flow: a vague ask → ArksAI asks a short round of clarifying questions first (never refuses, never blindly guesses); a coding ask → it **plans first** (shows exactly what it'll build) and waits for the user to approve (→ autonomous build) or revise.
- Plan approval gate — when ArksAI plans a build it presents the plan, then shows an explicit **"✓ Approve & build" / "Revise"** card (Approve kicks off the autonomous build). **Revise** is now unambiguous: clicking it hides the approval card and drops you into a clearly-labelled **"Revising the plan"** compose mode — a banner above the message box (with a "Keep plan" escape that restores the card) and a revision-specific placeholder — so it's obvious your next message edits the plan, after which ArksAI re-plans and re-presents the gate. The gate is **structural**, not just a suggestion: building can only begin on your next turn after a plan — the agent cannot skip straight from planning to building in the same breath. Approve **reliably** starts the build: the plan must be complete (no trailing question) before it's offered, and on approval the agent is blocked from re-submitting the plan and goes straight to building (so Approve never loops back to the card).

## B2B department platform
- Department-aware studio — Marketing, Sales, Finance/Strategy, HR·People & Ops, Engineering, BI & Analytics, Tax & Compliance (UAE), Legal (UAE), plus **Personal** and **Learning** for everyday non-business use. Pick your function and ArksAI is **ready with that team's skills** (its persona/standards load on the session). Each task is an **example you tap to open a fresh chat already specialized for it** — no canned prompt is fired; the chat just waits for you, and your first message names it. Or type your own brief in the box.
- ~70-task catalog grouped **Create / Analyze / Operate**, each a ready-to-run brief in the right mode.
- Per-task expert standards (server-side) — a department persona (FP&A rigor, inclusive HR, RevOps, brand-growth, senior-eng, BI/analytics, UAE tax manager) + research-backed standards injected per task.
- **Developer tier for the Engineering team** — five deep developer expertises (canon harvested from the claude-skills library, reimplemented in TypeScript, zero new deps): **database schema design** (normalize → keys/indexes/relationships, Mermaid ERD + DDL + migration plan), **API design & review** (REST conventions, versioning, pagination, idempotency, error shapes, auth + a design scorecard), **API test suite** (happy-path + input-validation + the auth matrix + error codes + contract checks, then exercised), **code review** (correctness/security/readability/performance, severity-tagged with concrete fixes + blast-radius), and **dependency audit** backed by a new deterministic **`dependency_auditor`** tool — pure, offline, no key: parses package.json / requirements.txt and flags floating/unpinned ranges, duplicates, pre-1.0 pins, and a missing lockfile, honestly noting it can't see live CVEs offline (never fabricates one; recommends `npm audit` / `pip-audit`). Every developer deliverable is a **designed, typography-first document** (generate_doc / render_report), not a raw markdown/code dump.
- **Auto-selected expertise (zero-pick)** — you no longer have to pick a department to get expert standards: a free-form message ("make me a monthly budget", "write a job description", "draft an NDA", "build a cash-flow forecast") is read by a fast, deterministic trigger-based router that silently loads the right expertise (the exact task standard when it's confident, else the department persona). A picked play always wins; if nothing matches, it stays the general agent — so it's purely additive. (Off-switch: `EXPERTISE_AUTOROUTE=false`.)
- **Thoughtful when it's unsure (confidence → clarify)** — the auto-router now behaves like a careful expert instead of guessing. When it's confident it silently loads the exact task expertise; when the signal is softer it loads only the **department persona** (still the right expert voice, never a wrong specific); and when a message is genuinely too thin to know what you want ("make me something nice", "help with my thing tomorrow"), it asks **one warm, specific question** to narrow it — never a blind guess, never "I can't help with that". A clear request with a real subject still goes straight to building. A specific skill is **never** fired on a low-confidence match (the "never confidently wrong" guard). (Off-switch: `EXPERTISE_CLARIFY=false`.)
- **Personal / everyday expertise (ArksAI for everyone, not just business)** — two non-business lanes anyone can use without picking anything: a **Personal** department (my monthly/household budget — friendly, formula-driven; savings & debt-payoff plan; **"should I buy / sell" big-purchase valuation** that researches real cited listings and gives a buy + resale range — the "value my Porsche / iPhone" case; résumé/CV; cover letter; complaint/dispute letter; formal letter; email/message rewrite; trip itinerary; event/party plan; personal checklist) and a **Learning** department (explain a concept at your level; study guide / revision notes; faithfully summarize a document). Plain language, no jargon, real cited facts (never fabricated). A free-form message ("household budget for next month", "should I buy a 2019 Cayenne at 200k km", "explain compound interest to a 15-year-old", "write a complaint letter — the airline lost my bag") auto-routes to the right one.
- **Drift-proof expertise registry** — every expertise's `<dept>.<task>` key + department now lives in ONE source of truth (`shared/expertiseKeys.ts`) that both the client play catalog and the server standards derive from (the client play key is even compiler-typed against it). Cross-file **sync tests** fail the build the moment a key is half-added — a play with no standard/triggers, a standard/trigger with no play, a department with no persona, or two tasks sharing a trigger phrase — so adding a skill is a safe, consistent, template-driven act (recipe in `EXPERTISE_AUTHORING.md`). No behavior change: every existing play, standard, and trigger is unchanged.
- **Leaner prompts, same quality (progressive disclosure)** — the system prompt is now assembled from a slim always-on core plus on-demand slices (the music/image/document-tool notes, the report page-mechanics, the design system, the per-task expert standards), and a slice loads **only when the current task can use it**. A read-only planning turn no longer carries the build/generation tool notes it can't act on (~48% shorter), and the per-task expertise loads only for that task — so each turn costs fewer tokens with **no loss of capability**. The guarantee is hard-locked: a **report build receives every report rule byte-for-byte** as before (a test asserts the report and code prompts are identical with the feature on vs off), so the painstakingly-tuned report engine cannot regress. (Off-switch: `EXPERTISE_PROGRESSIVE=false` restores the full prompt instantly.)
- **Auto-Brief — closing the novice↔expert prompt gap** — a thin request and an expert brief produce wildly different output; ArksAI silently compiles every request into the brief an expert would have written, before it acts. A deterministic, zero-latency layer (`server/src/agent/brief.ts`) — no per-request LLM call, so no intent drift and no fabricated specifics — detects the deliverable family (research · financial model · report · data analysis · formal document · a conservative non-visual generic fallback) and injects three composed layers into the system prompt: **(1) brief-first compilation** — restate the goal, state explicit ASSUMPTIONS for anything unspecified, and ask AT MOST one question only when an input is genuinely critical, else proceed (invisible, auto-proceed); **(2) a per-family operating procedure** — ROLE · SUCCESS CRITERIA / HARD FILTERS · METHOD · VERIFICATION RULES · OUTPUT CONTRACT · SELF-AUDIT; **(3) a self-audit gate** — don't deliver output that fails its own audit; label every figure verified (dated source) or unverified. So a one-line "research which VCs invest pre-seed in the UAE" comes back with qualifying filters, a dated source per item, the right metric (typical INITIAL cheque, not a one-off round), High/Medium/Low confidence, an "Adjacent / stretch" section, and a "Could not verify" section instead of padding — **verified live on the real agent**. Missing inputs become explicit assumptions, one question, or a marked `[placeholder]` — never fabrication. Plain visual builds are left untouched (they already get the design system + verify/design gates). (Off-switch: `AUTO_BRIEF=0`.) Remaining tuning (a separate fast-model pre-rewrite, a standalone re-review audit pass, and A/B telemetry) is optional — the deterministic prompt layer already delivers the lift.
- **Marketing makes finished creatives (image + crisp text + logo)** — a `generate_creative` capability produces a ready-to-post **PNG/JPEG**: it generates an on-brand, text-free AI background with reserved negative space, uses **M3 vision to pick the calmest zone + the text colour that reads there**, then composites the headline/subhead/**check-marked feature list**/CTA as **pixel-perfect type** (HTML/CSS, never the image model's garbled lettering) plus the **user's uploaded logo** (or a tasteful top-left "LOGO" placeholder) if provided, with a contrast scrim and a vision QC + auto-correct pass. It also **scrubs any copy the model put into the imagery prompt** (literal headline/CTA, quoted strings, "text that says…") so the background can't come back with garbled lettering, and re-generates with a hard no-text directive if text still leaks in. The dedicated **Ad & social creative** play, the email/social kit, and landing/event heroes all use it at channel sizes (1:1 / 4:5 / 9:16 / 16:9 / 1.91:1); graceful CSS/SVG fallback when unkeyed. The agent **asks for your logo first** (or uses a clean placeholder), and social creatives bake in **high-conversion tactics** (a benefit hook, one clear CTA, truthful proof/specifics, thumbnail-legible type) — kept **separate from email**, which is optimized for the inbox instead.
- **Logo & brand identity — a real designer submission** — a `generate_logo` capability (Marketing → **Logo & brand identity**) designs a complete brand mark, not a clip-art guess. It plays to the model's strengths and bans its weakness: **letters are always real type** (chosen from a curated ~23-family suite of geometric sans + editorial serifs + display faces) and **symbols are always built from overlapping geometric primitives** — it **never warps/morphs** a letter into an object (the thing that produced lollipops), so the mark reads crisply at every size. It **auto-picks the route** (a pictorial concept mark vs a lettermark), generates several drafts, uses **M3 vision to select its own strongest** and name the fixes, then does a **structure-locked polish**. The deliverable is a full **designer submission, zipped**: a **brand directive** (concept rationale + palette table + typography), **light & dark** variants, an **app-icon, favicon, and website-navbar placement**, and a multi-format **asset kit** (self-contained **SVG** with fonts embedded, plus **PNG & JPEG** at 1024/512/256/128/64 on **both light and dark** backgrounds). The brand sheet is delivered as a **downloadable PDF** that **auto-opens in the canvas** (universally viewable, unlike a raw `.html`); graceful config message when unkeyed. The agent **gathers a quick brief first** (brand + what it does, a direction or two, a colour, the vibe).

## UAE Legal
- A dedicated **Legal (UAE)** department that drafts **execution-ready** legal work — commercial contracts, NDAs, employment contracts, POAs, MOA + shareholders' agreements, board resolutions, **legal notices / letters before action**, **police reports / criminal complaints**, **legal opinions**, **legal forensic audits**, contract-review risk reports, compliance audits, dispute briefs, licensing/jurisdiction advice, and a legal calendar/filing tracker (15 plays).
- **Bilingual documents** (the chat follows your language; only the produced document is bilingual) — formal British English + eloquent, posh **Modern Standard Arabic that is not a literal translation** (native legal idiom), grounded in current UAE **federal + emirate + DIFC/ADGM** law (jurisdiction-resolved first), citing the specific law and article, with full clauses/boilerplate and bracketed placeholders so **counsel reviews and signs rather than rewrites**. Every output carries a mandatory "have a licensed UAE lawyer review and sign — not legal advice" notice. Defaults to the **MiniMax M3** model (chosen via a live legal-drafting bake-off).

## UAE Tax & Compliance
- A dedicated **Tax & Compliance (UAE)** department that produces the format-strict filing documents a UAE finance team needs — **e-invoicing (PINT AE)**, **VAT (VAT 201 + FAF)**, **Corporate Tax (the 9% CT 300 computation)**, **Excise**, and monthly **WPS** salary disbursement — each built to the researched FTA / MoF / MOHRE / Peppol spec and self-validated.
- **`generate_compliance_file`** — a self-validating generator for the three strict machine formats (like `generate_spreadsheet`, it returns errors instead of a bad file): the **WPS `.sif`** (builds the SCR header + EDR rows, computes the control total = Σ EDR to the fils, validates the 13-digit employer ID / 14-digit labour IDs / 9-digit routing / 23-char "AE" IBANs / no-comma AED amounts, and names the file `[employerID][YYMMDD][HHMMSS].sif`), the **FAF** VAT-audit CSV (company + supplies + purchases + totals, reconciled to the VAT 201 output/input VAT), and the **PINT AE e-invoice** UBL 2.1 XML (parties with 15-digit TRNs + scheme `0235`, a tax subtotal per VAT category code S/Z/E/AE/O/G, well-formed, totals reconcile; tax invoice + credit/debit notes). The VAT 201 / CT / Excise working papers and the readable invoice reuse `generate_spreadsheet` (formula-driven) / `generate_doc` / `render_report`.
- **Guided filing wizard** (`tax.guided`) — walks a non-expert finance user through one obligation step by step: entity profile → tax period + deadline → import & validate the data (never fabricated, gaps flagged) → build and validate each document → a review checklist + the assembled filing pack and exactly where to submit.
- **Honest framing on every output**: these are draft working papers for professional review — ArksAI does not file or pay; the org submits via **EmaraTax / its accredited ASP / its WPS agent bank**, and every file carries a "validate against the official schema / your agent bank before live submission" note (the PINT AE spec is partly draft; the WPS SIF layout varies by bank; DIFC/ADGM use their own systems).
- B2B acquisition landing (pre-login) — positioned as **"the AI that empowers your business"** for the UAE, and **SEO/LLM-optimized for "AI in the UAE"**: a keyword-rich `<title>`/description/keywords + Open Graph + **JSON-LD structured data** (Organization, SoftwareApplication, FAQPage) in `index.html` so search and answer engines can read and cite it pre-JS. Meatier content: a **"what it makes"** capability grid (live apps, dashboards, reports/decks, on-brand ad & social creatives, UAE compliance, UAE legal), a **"Built for the UAE"** compliance block (VAT 201/FAF, PINT AE e-invoicing, Corporate Tax, WPS, Excise), a by-department value grid, a UAE-focused FAQ, an early-adopter strip (the ARKS group), and a "watch it build" demo. Kept **alpha · invite-only** throughout (nav pill + hero note + request-access funnel → `POST /api/leads`). Excludes any admin/analytics surfaces.
- **Multi-page marketing site + shareable link previews + LLM SEO** — the headline message is **"give every team AI without hiring an AI expert" (help UAE businesses adopt AI with no AI-skilled staff)**. Dedicated **per-team pages** at `/for/{marketing,sales,finance,hr,tax,legal}` (`client/src/components/VerticalPage.tsx`), each a focused landing with that team's value prop, its full play catalog, how-it-works, and a pre-filled request-access form. Because crawlers and link-preview bots don't run the SPA's JS, the server **injects per-route `<title>`/description/canonical/OpenGraph** into the served HTML (`server/src/seo/marketingMeta.ts`, wired into the SPA fallback in `app.ts`) so a shared `/for/finance` link previews as the Finance page, not the generic homepage. A real **1200×630 branded OG card** (`og-cover.png`, replacing the old square icon) makes links look professional when shared on Reddit/X/LinkedIn/WhatsApp/Slack, with full `og:image:width/height/alt` + `twitter:image`. Plus **`robots.txt`** (explicitly welcoming GPTBot/ClaudeBot/PerplexityBot/Google-Extended and friends, disallowing transient `/apps/` + auth surfaces), a **`sitemap.xml`**, and an **`llms.txt`** — a clean, answer-engine-friendly description of the whole product + per-team capabilities, led by the UAE/no-AI-skills positioning (LLM SEO).
- **Features page (`/features`)** — a full public catalog of everything ArksAI does (`client/src/components/FeaturesPage.tsx`), grouped into Build & ship, Design & document, Automate & operate (email robots, scheduled deliveries, live data in, webhook delivery out), Finance & strategy (formula-driven models), UAE compliance (VAT 201/FAF, CT 300, WPS/SIF, PINT AE e-invoicing, Excise) and UAE legal (bilingual, citing law), plus the quality system and a per-team grid. Server-side SEO meta on `/features`.
- **ArksAI Research hub (`/research`) + cited articles (LLM SEO)** — a public thought-leadership surface (`client/src/components/Research.tsx`) with sourced, cited essays written for business operators. Articles live as JSON in `client/public/research/` (single source: fetched by the client reader AND read by the server SEO layer). Each article page gets **server-injected per-route `<title>`/description/canonical/OpenGraph + full JSON-LD `Article` structured data (headline, datePublished, keywords, and the complete `articleBody`)** so non-JS crawlers and answer engines can read and cite the content. First three pieces (each 14–15 verified sources, no fabricated figures): *Energy is becoming the next currency*, *The East and the AI frontier*, *How to read AI benchmarks*. Listed in `sitemap.xml` + summarised in `llms.txt`.
- **Founding-member positioning + focused landing funnel** — the landing (`Landing.tsx`) is a tight conversion path, not an everything-page: hero + live "watch it build" demo → social proof (ARKS adopters) → a "see it work" proof strip (four function vignettes — email robot, finance model, bilingual legal draft, marketing creative) → how-it-works → the **founding-cohort offer** (flat **39 AED/month** during alpha, no lock-in, a direct line to shape the roadmap) → a **two-step low-friction application** (email first, optional team details second; both post to `POST /api/leads`) → a founder's note → a tight 4-question FAQ. The encyclopedic content (full capability grid, the UAE-compliance grid, the every-team department grid) lives on `/features`, linked prominently — so the homepage stays focused on converting.

## Day-to-day capabilities
- Recurring/scheduled tasks — a durable server scheduler (daily/weekly/interval) that fires a fresh session even with the browser closed; managed via a Schedules dialog. Times are read in **your local timezone** (captured from your browser, DST-correct), so "daily 08:00" fires at 08:00 where you are — not on the server's clock.
- **Large multi-tab spreadsheet reads — query, don't dump** (`query_spreadsheet`): the right way to analyse a big or many-tab workbook the way a senior analyst (or Claude) would — **load every tab into a DuckDB runtime and run SQL across them, getting back ONLY the result** (totals, group-bys, **joins across tabs**, pivots, filters, reconciliation) instead of paging thousands of raw cells into the chat. The data stays in a separate process, so it scales to huge, many-tab files without ever filling context. `read_spreadsheet` is now the cheap **map** (it lists every sheet with dims, a **query-table name**, **inferred column types**, and samples); `query_spreadsheet` is the calculator (each sheet is a `lower_snake_case` table; call once with empty SQL to discover the tables, then query). It also has a **`profile` mode** — the ideal first move on a messy/unfamiliar file: per-column **type, min/max, distinct count, average, and % null** for every tab, no cells paged. pandas + openpyxl + **duckdb are preinstalled** in the agent image so reads are instant (no mid-task `pip install`), with a graceful "install + retry" path if ever absent.
- Data in — `fetch_data` pulls a public CSV/JSON/published-Sheet URL (SSRF-guarded).
- **Ad-platform connectors (Meta · Google Ads · TikTok)** — org admins connect their ad accounts under **Settings → Connections** (OAuth); the agent then pulls **live** performance via `fetch_ads` straight into dashboards and reports (spend/impressions/clicks/ctr/conversions, by campaign/ad/day). Per-org, OAuth tokens stored **encrypted** (AES-256-GCM) and never exposed to the model; auto-refreshed on expiry; each provider gates on its app credentials. Reusable adapter framework (one interface per platform). Operator setup in `CONNECTORS.md`.
- **Connect your GitHub & push to your own repo (per-user OAuth)** — each user links their **own** GitHub account (OAuth App) and **picks which repository** ArksAI pushes generated code to, or **creates a new repo** right from the New-session dialog (searchable list of repos you can push to). The session's clone/push then use **your** token (falling back to the operator PAT only for self-host). Tokens are **encrypted at rest** (AES-256-GCM), never returned by any API or shown to the model, and **scrubbed from all git output**; strict **per-user isolation** (you can only ever use your own connection). The operator turns it on either via env (`GITHUB_OAUTH_CLIENT_ID/SECRET`) or, with no SSH, an operator endpoint that stores the app credentials encrypted at rest (flips it on instantly). A documented trade-off of the OAuth-App approach is the `repo` scope grants access to all your repos (a future GitHub-App upgrade would scope to chosen repos).
- **Connections hub + connect GitHub from the chat** — a dedicated **Connections** surface (sidebar → 🔌) is the home for every connector, built to grow. The **GitHub card** lets you connect/disconnect your account and **create a new repo right there**; the ad-platform connectors (Meta/Google/TikTok) live there too for admins. And under any chat there's a slim **push-target bar**: connect GitHub and pick (or create) the repo *this* build pushes to, inline — no dialog needed. The bar reflects your real connection state (once connected it shows "GitHub connected as @you — choose a repo" rather than still prompting you to connect) and updates instantly when you connect or disconnect anywhere in the app.
- **Prepaid org wallet + invoicing (USD-canonical)** — each organization has a **prepaid balance** it spends as it builds. Usage is **debited automatically** at the end of every run (the real USD cost), recorded in an **append-only ledger** that doubles as the statement/invoice (top-ups + usage, with running balance), exportable to **CSV**. An **operator tops up** an org's wallet from Admin (amount in USD or the org's local currency); the top-up flows through the *same* credit path a **Stripe/PayPal** webhook will use later (the gateway abstraction is scaffolded — `payments/provider.ts`). All money is integer **micro-USD** so balances never drift. Spend **enforcement is observe-first** (a kill-switch, off by default): usage + balance are tracked and a **low-balance banner** shows, and turning `WALLET_ENFORCE` on blocks new runs at zero. Idempotent charges (a re-fired run can't double-bill); strict per-org isolation (an org admin only sees its own wallet; only the operator tops up).
- **BDT and other floating currencies, shown honestly** — alongside the fixed-peg GCC set, costs can now display in **Bangladeshi Taka (৳)**. Because BDT *floats*, USD stays the **canonical** number and BDT is a clearly-labelled **indicative** conversion: the rate is **operator-set** (in Admin, stamped with an "as of" date) — a live FX feed is a drop-in upgrade — and the conversion **basis is always visible** ("৳128 · 1 USD = ৳122 · 24 Jun"), with USD shown alongside. The wallet, charges and invoices all settle in USD; BDT is display-only.
- **Reads SCANNED PDFs by sight (accurate PDF→Excel)** — a text-only extractor (`pdf-parse`) returns *garbage* for scanned/image documents (e.g. bank challans, invoices), silently destroying a table's structure so the model has to guess — which drops suppliers, mis-files months, and scrambles totals. Now, when an uploaded PDF's text extract looks unreliable, ArksAI **renders each page to a high-resolution image** and routes the agent to **read the pages with vision (`see_image`)** instead of the dead text — the same way a person would. The steering also mandates the right discipline for document→spreadsheet work: **transcribe every line item** (with its own date/month, never inferred) into an **audit-trail sheet**, build the summary with **live formulas**, **reconcile each source document's extracted total to its printed total**, and **de-duplicate** repeated documents — so a missing row or a wrong month is *caught*, not shipped.
- Deliver out — `send_webhook` posts a result to a Slack/Zapier/Discord hook.
- **Email (per-org mailbox)** — each organization connects its own mailbox (SMTP for outbound, IMAP for inbound) in Settings → Email; passwords are stored **AES-256-GCM encrypted at rest** and never returned to the client. The agent can then `send_email` (with workspace attachments — a PDF, deck, sheet, or image) and `read_inbox` (triage/summarize/draft a reply), scoped strictly to the session's own org. A "Test connection" check verifies both legs before use.
- **One-step mailbox connect (auto-detect)** — connecting a mailbox (org OR per-robot) defaults to a **quick mode**: enter just the **email address + password** and ArksAI auto-discovers the SMTP/IMAP host, port and security, verifies the login, and saves — no server settings to look up. Detection escalates through a built-in provider table (Gmail/Outlook/Yahoo/iCloud/Zoho/GoDaddy/…), an **MX-record lookup** (Google Workspace / Microsoft 365 custom domains), then the **Mozilla/Thunderbird ISPDB** (`server/src/email/autoconfig.ts`, `POST …/email/autoconfig`). If detection or the sign-in fails, it **falls back to the manual form, pre-filled** with whatever was found (and a clear hint that 2FA accounts need an app password).

### The email robot (the engine behind the console)
- **Each robot has its own mailbox** (a Mailbox panel in the robot's office — SMTP + IMAP, encrypted at rest). Connect it and that robot becomes a working **email agent** with its own email identity: it polls *its* inbox and drafts a reply per new message, grounded in its mandate/knowledge. (An org-level mailbox also exists for the general chat/code agent's `send_email`/`read_inbox`.)
- **Drafts for approval (default)** — each reply waits in the **"Needs You" inbox** for one-tap **Approve & send** or Dismiss. Once trusted, set a robot to **auto-send**.
- **Safe by construction** — replies are **locked to the inbound sender** (a prompt-injected "forward to…" can't redirect them), the model sees only that one message plus the robot's own knowledge (data-minimized), and anything out of scope (refunds, billing, money, legal, angry complaints) **escalates to a human**.
- **Model choice** — robots draft with **ArksAI Max (M3)** or **ArksAI v4 (DeepSeek)**, or **Compare both**.

## Visual identity & theming
- Editorial light/warm identity — ivory canvas, Source Serif 4 + Inter + Space Grotesk (same fonts the reports use), publication masthead, hairline rules, per-department accent coding. Token-driven.
- Dark mode for the Engineering (developer) team, with a smooth animated dark↔light crossfade (mobile + desktop, reduced-motion aware).

## Platform plumbing
- Projects — persistent workspaces (instructions, defaults, branding, knowledge base) that sessions inherit.
- Memory (global/repo/project scopes + ARKS.md); custom slash commands.
- Dual-driver storage (Postgres or SQLite); durable timeline; cost/token accounting.
- Auth: per-user accounts (email + scrypt password) + DB-backed, instantly-revocable sessions, OR the single operator password (now a platform **super-admin**); public lead endpoint allowlisted; PWA (installable: light manifest, service worker, offline shell; HTTPS via Caddy + Let's Encrypt once on a domain).
- **Hardened input handling + accessibility + speed** — every API endpoint returns a clean, actionable 4xx (never a 500) for malformed JSON, wrong-typed, or oversized/hostile input, and never silently accepts garbage; a global error handler surfaces the actionable message to the user while an unexpected server error is logged internally and shown as a calm, generic line (never leaking a DB/filesystem detail); uploaded filenames are sanitised against path traversal and downloads use RFC-6266 `Content-Disposition` so non-ASCII (e.g. Arabic) names download correctly and can't inject a header; outbound data fetches are SSRF-guarded **including across redirects** (every hop re-validated, so a public URL can't bounce to a private/metadata address); every form control across the app exposes an accessible name to screen readers; and the session-list/detail reads project only the columns they need (no longer hauling each chat's full transcript) so the sidebar stays fast at scale. Locked in by systematic test batteries (HTTP input-validation + error-leak, upload sanitisation, download headers, SSRF-redirect, CSV-parser edge cases, generator edge-cases, an authenticated cross-surface a11y sweep).

## Organizations (multi-tenant "spaces")
- **Isolated org spaces** — every session/project/deployment is scoped to an org; **everyone — members AND the platform operator — sees only their own workspace's data** (cross-org access → 404). The operator's **super-admin** role unlocks platform administration (provisioning orgs, reviewing the waitlist, managing any org's members), **not** a commingled view of other orgs' chats.
- **Roles**: super-admin (operator) → org **admin** → **member**; new sessions/projects are stamped with the creator's org.
- **Project-level visibility** — each project is org-wide by default, or **private to its creator + invited members** (enforced in every list/get query; cross-visibility access → 404). The owner or an admin manages sharing.
- **Invite-only, link-based onboarding** (no email infra): the super-admin creates an org + invites its admin; org admins invite their team — each invite is a one-time link to copy/share. Opening it sets a password and logs the member straight in; memberships revoke instantly (killing live sessions). Being **granted access removes that person from the waitlist** automatically (on invite issue and on accept), so the operator's review list only shows people not yet actioned.
- **In-app admin panel**: create orgs, **delete an org and ALL its data** (operator-only, cascades sessions/projects/published apps/schedules/commands/profile/analytics + removes member accounts orphaned by the deletion; the Default workspace is protected), review the waitlist (leads), manage members + invite links; team-member email login (the platform operator signs in separately at the unlinked `/operator`) and an `/invite/<token>` accept page. The invite field is a prominent full-width email; **free/personal email domains are blocked** (server-authoritative, gmail/outlook/…) and an invite whose domain differs from the admin's own is **flagged as out-of-org** (confirm to proceed). A company's workspace is its OWN and is **not swappable** — there is no workspace switcher in the UI for anyone (the sidebar just names the current workspace).
- **Cross-org isolation, audited & hardened** — every user-facing resource is scoped to the authenticated org (never a client-supplied id): sessions + all `/api/sessions/:id/*`, projects (+ creator/invited visibility), **the live sidebar event stream** (a tenant only receives its own org's session status/titles), **deployments** (list + stop/restart/delete — a slug from another org → 404), **schedules** (list/create/enable/delete, and a fired run lands in the owning org), **custom commands** (per-org keyed `(org_id, name)` so orgs never see/overwrite each other's), and org **memory**. Mutating a project (edit/delete/upload) additionally requires being its owner or an org admin. Backed by isolation tests **including an HTTP-level red-team** that signs in as one org and confirms it cannot reach another org's data on any route (list, by-id, sub-routes, or destructive writes) — and a check that the **operator** is confined to its own workspace (it opens its own chats but gets a 404 on another org's session/events/projects/schedules/deployments) while keeping the admin provisioning surface.
- Shipped **non-breaking**: the existing operator login keeps its platform-admin powers via the super-admin role (now scoped to its own workspace for data reads, like every tenant), and all prior data lives in a bootstrapped "Default" org.
- **Per-org shared brain + an onboarding designed to be FINISHED** — a new org admin gets a fully-visible, agent-led first run built on how anticipation works (dopamine = a seeking signal driven by a reward that beats expectation): a vivid promise + one tiny ask opens a loop they want to close, an **endowed 3-step tracker** (Brand → Profile → First build) shows them already moving, and a **live brand-reveal panel** beside the chat lights up with their real accent, palette and "about" the moment ArksAI reads their site or logo — the studio visibly becomes theirs. ArksAI **crawls the org's website** **or reads an uploaded logo's colours** (`extract_palette`, deterministic — and vision works in every mode, so it never claims it can't see the image), saves the brand early so the panel pops, asks a couple of quick questions, then `save_org_profile` (complete) and tees up an exciting first build. That brand + "about" + a recalled digest of the org's own past chats are injected into **every** session in the org (brand applied consistently unless the user overrides). Members joining later inherit it silently; the admin can re-run/skip/edit.
- **Strict per-org memory isolation** — org memory lives under an `org:<id>` scope derived only from the authenticated identity (never client input). A tenant org reads/writes **only its own** scope — never the deployment-`global` notes, never another org — enforced in the runner's context builder *and* the memory API (read/write/delete), with repo memory namespaced per org. Cross-org isolation is unit-tested.

## Analytics & insight (admin)
- **Country-specific cost display (per-org currency)** — costs are computed in USD but **shown in each organization's chosen currency**, starting with **AED** (the platform default). An admin picks the org's currency in the Admin panel and it flows everywhere cost appears — the per-session cost bar, the `/cost` command, and the operator + per-org analytics consoles (KPIs, charts, tables). GCC currencies are USD-pegged at fixed rates (AED 3.6725, plus SAR/QAR/BHD/OMR), so conversions are exact with no rate feed; more currencies can be added later.
- **Operator analytics console** (super-admin, full-screen from the sidebar) — a **dense, editorial "data desk"** (tabular figures, hairline rules, **week-over-week delta chips + inline sparklines** on every headline metric for fast reading) to understand how the whole platform is used at a glance: headline KPIs (active users 7d/30d, **stickiness = DAU/MAU**, sessions, success rate, **time-to-first-build**, cost, orgs/users, live apps, waitlist), active-users/sessions/cost-per-day trends, the **acquisition funnel** (lead → signup → onboarded → built → published), **feature adoption** by play, splits by mode and model, quality (done vs error), cost by model, a **signup-cohort retention** grid, and named **per-org and per-user drill-down** tables.
- **Per-user drill-down** — click any user (operator) or member (org admin) to open a metadata-only detail panel: their sessions, success rate, cost, time-to-first-build, a 30-day activity chart, what they build (plays/modes/models), and their orgs — scoped to the viewer's permissions.
- **Activation: time-to-first-build** — median time from signup to a user's first build, surfaced as a KPI and per-user, so onboarding friction is visible.
- **Actionable alerts (no setup)** — derived from existing metrics with no thresholds to configure: **cost-spike** detection (an org's last-7-days spend vs its trailing weekly baseline) and **churn-risk** orgs (onboarded + once-active but gone quiet) for the operator; per-org admins see their own cost-spike + inactive members. Quiet by default — shows "all clear" when nothing's wrong.
- **Scheduled digests** — a periodic platform snapshot (every `ANALYTICS_DIGEST_HOURS`, default daily) is computed and stored for the operator to review in-app (with deltas vs the previous one), and pushed to a webhook if `ANALYTICS_DIGEST_WEBHOOK` (Slack/Zapier/…) is configured.
- **CSV export** — one-click download of the orgs, users, and per-org members tables (client-side, no round-trip).
- **Per-org dashboards** — an org admin (or the operator drilling in) gets the focused subset inside the admin panel's **Usage & engagement** tab: engagement KPIs, active-members/sessions trends, the activation funnel, what the team builds (plays/modes/models), retention cohorts, alerts, and a clickable per-member activity table — scoped strictly to that org.
- **Lightweight event-tracking layer** — a non-blocking `track()` writes usage events (`app_open`, `run_finished`, …) to an `analytics_events` table with a precomputed epoch-day bucket (zero date-SQL, SQLite+Postgres portable); metrics also derive from existing tables so they work from day one and get richer as events accrue.
- **Privacy is absolute — metadata only.** Analytics read only counts, timestamps, status, mode/model, the play key, tokens and cost; they **never** touch the `timeline` table or any message/document content. Enforced at both boundaries (a write-side scalar-only `props` allowlist + a grep-able test that the analytics modules contain no content reads) and gated end-to-end: operator routes are super-admin-only, per-org routes require that org's admin (a per-user drill is membership-gated so an admin can't probe non-members) — covered by an HTTP red-team test (a non-operator gets 403 on platform analytics; an org admin cannot read another org's).

---

## Status & honest caveats
- **343 automated tests green**; typecheck + build clean.
- Anything needing the **model key or open egress** (live builds, real vision calls, scheduled runs firing, external data/webhook delivery) is wired + unit/integration-tested but fully exercised only on the **Droplet**.
- **Staged (needs Droplet credentials):** OAuth Google Sheets/Drive & CRM connectors, a Slack app, SMTP email.
- **Multi-tenant org platform — core SHIPPED** (per-user accounts, roles/invites, org-scoped data, in-app admin panel). Remaining: the invite-only landing revamp, per-org department templates, and the AUDIT-P0 low-priv agent uid-drop before any untrusted/self-serve org. The live multi-user flow should be validated on the Droplet.
