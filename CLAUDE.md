# ArksAI — project memory

A self-hosted, Claude-Code-style web coding agent powered by DeepSeek, with an
engine-orchestration layer (Suno for music, more later). This file is the
durable context to reuse every session.

## Product vision (north star — read first)
ArksAI is for **general, non-technical people**, NOT developers. Promise: describe it once → get ONE finished thing that **looks perfect, works perfectly, and is actually live/usable** — zero user iteration ("Apple, not Windows"; the model/"brain" is swappable; quality comes from the SYSTEM). Two pillars, both built:
- **A. One-shot quality**: `taskProfile.ts` (classify the request → end-user deliverable type + isVisual + tier) drives an opinionated `designSystem.ts` (`designCore` + per-type packs, injected into the prompt for visual tasks only) + the bundled **UI kit** (`server/assets/ui-kit/` tokens+components+themes; `add_ui_kit` tool) + a **gating internal design-critique loop** (uiCheck `DESIGN_RUBLIC`/`parseDesignVerdict` → `runVerifyGate` REVISE→escalate→re-render, MAX 2 rounds, `config.designGate`) + a visual model floor. The agent iterates internally so the USER never does. Per-build: offer ONE quick curated style choice, then automatic.
- **B. Live deployment**: `publish_app` tool + TopBar Publish button → `deploy/publish.ts` snapshots the app to `data/deployments/<slug>/`, classifies static vs node/python, runs server apps via the durable `deploy/registry.ts` (survives sessions, `recoverDeployments` restarts on boot). Public, auth-free serving at **`/apps/<slug>/`** (`routes/deployments.ts`) — static files or reverse-proxy with HTML rewrite+`<base>`. `deployments` table; client `DeploymentsDialog`. v1 is path-based on the Droplet; subdomains/TLS/custom-domains + external hosts (DO App Platform) are the deferred next step.

## Current state & handoff (read first)
- **ROBOTS MULTICHANNEL + COMMANDER BRIDGE — CODE-COMPLETE (on `main`, 2026-07-04): see `ROBOTS_CHANNELS_PLAN.md`.** Operator directive: WhatsApp/Telegram/SMS (SMSALA) auto-responders + reusable personas + per-robot knowledge base + the end-state "text the robot on Telegram/email → it builds a website/doc/deck with ArksAI → delivers directly to a named email/WhatsApp". ALL FOUR PHASES BUILT (883 tests, 19 new in `robotChannels.test.ts`; boot-verified; console UI Playwright-QA'd 1280+390 incl. a mobile-overflow fix that also fixed the pre-existing email-form overflow): (1) **channel spine** — `robot_channels` (AES-encrypted secrets, write-only) + adapters `robots/channels/{telegram,whatsapp,sms}.ts` + shared `inbound.ts` (`handleChannelInbound` = the email loop's guarantees: message-id dedupe, recipient LOCKED to sender, ask/auto, escalate-not-guess); drafts carry `channel`, send dispatches by adapter; Telegram = getUpdates long-poll from the robot poller (no webhook; offset in channel state; `api.telegram.org` blocked in the SANDBOX only — droplet fine, tests inject fetch). (2) **webhooks** — public `routes/robotHooks.ts` (`/api/hooks/whatsapp` GET hub.challenge verify + POST routed by phone_number_id w/ X-Hub-Signature-256 HMAC when appSecret stored — raw body stashed by the JSON parser as `req.rawBody`; `/api/hooks/sms/:hookKey` per-channel random-key gate + ChannelNumber match) + `routes/robotFiles.ts` (60-min token-gated deliverable links for WhatsApp doc-by-link/SMS). (3) **personas + KB** — org-level `robot_personas` (voice/language/signature; picker in settings; free-text config.persona wins) + `robot_kb_docs` (file upload w/ extractText, or pasted; **pure deterministic retrieval** `selectKnowledge` = chunk + term-overlap → only RELEVANT slices enter the reply context, §5c data-minimization holds) via `robots/personas.ts replyExtrasFor` (wired into email poller + channel inbound + preview). (4) **commander bridge** (`robots/tasks.ts`) — `robot_commanders` (the OWNER's own addresses per channel; ONLY they can trigger builds/name destinations — customers stay §5c reply-only), `classifyCommand` (BUILD_HINT_RE prefilter → M3 strict JSON, **fail-CLOSED to chat**), `startTask` = the scheduler's exact spawn (session `task:'scheduled'` unattended block + AUTO_MODEL), poller `watchRobotTasks` collects deliverables (`collectDeliverables` mtime scan + published `/apps/<slug>/` URL) and delivers per channel (email attachments / tg sendDocument / wa doc-by-link / sms link); 45-min cap, one task per robot, receipt drafts (status 'sent') for dedupe+audit, honest failure messages; `installCommandHook()` at boot. UI: `RobotChannels.tsx` panels (Channels/Persona/Knowledge/Commanders/Builds-on-command) in the robot's ⚙ Settings; `robotsStore.update` now read-merge-writes config (was clobbering personaId/knowledge). **NOT live-verified** (needs real channel creds). **OPERATOR SETUP:** a @BotFather token (2 min — do Telegram first), Meta business app (phone number id + permanent token + verify token; webhook URL `https://arksai.studio/api/hooks/whatsapp`) for WhatsApp, SMSALA api_id/password + **whitelist the droplet IP 159.89.172.210 in SMSALA** (live probe returned "IP not Whitelisted") + set their inbound URL to the hookKey URL shown in the console. NEXT: live-verify Telegram end-to-end (auto-reply → commander build → sendDocument delivery), then WhatsApp/SMS.
- **OPEN / REMIND THE OPERATOR (2026-06-25): clean app subdomains still don't serve → see `SUBDOMAINS.md`.** Verified all green: DNS `*.apps.arksai.studio → 159.89.172.210` EXISTS (zone in DO `marketing.gicbd@gmail.com`), the on-demand-TLS gate `/internal/tls-check` returns `ok` for a real slug + `no` for a bogus one, and `/apps/<slug>/` (path URL) serves 200 — yet `https://<slug>.apps.arksai.studio/` won't load. Remaining suspect = **Caddy failing to issue the on-demand cert on the droplet** (LE rate-limit/challenge, or a stale Caddy config never reloaded with the `*.apps` block). NEXT STEP (needs droplet shell — sandbox can't reach it): grab `docker compose -f /opt/arksai/docker-compose.tls.yml logs caddy | grep -iE 'apps.arksai|on_demand|obtain|error|rate' | tail -60` and `caddy validate`; if stale, `./deploy.sh tls`. Code is READY: `APPS_SUBDOMAIN_BASE` env (off) makes publish advertise the clean URL once it serves; path URL works meanwhile. Operator deprioritized ("look into later"). The TWO DO tokens pasted in chat (`gicbdfacebook`/compute + `marketing.gicbd`/DNS) must be ROTATED.
- **ACTIVE STANDING GOAL (2026-06-22): Android Apps section → see `ANDROID_PLAN.md` + `BUILD_BAKE.md`.** Done-condition = a SMALL (QR scanner) + a LARGE (Tinder-style) app built to real APKs, crash-free, then report back. **CODE-SIDE COMPLETE (all on `main`, 502 tests):** mobile UI kit + `add_mobile_ui_kit`; **`create_expo_app`** (unpacks a runnable, crash-safe, kit-wired Expo/expo-router app from `server/assets/expo-app-template/`); **`add_app_backend`** (Fastify+SQLite kit — JWT/validation/error-envelope/migrations); **Phase 3 build orchestrator** `server/src/build/` (do.ts DO-API client, store.ts `builds` table+one-time token, androidBuild.ts = tar→token-gated source route→droplet-from-snapshot w/ cloud-init build→POST APK back, watcher always destroys droplet + 10-min orphan reaper) + `routes/builds.ts` + `build_apk` tool; designSystem mobile typePack steers create_expo_app→add_app_backend→web-gate→build_apk. **THE WHOLE BUILDER IS DORMANT** (`isBuildConfigured()` = `DO_API_TOKEN`+`ANDROID_SNAPSHOT_ID`; else 503 + reaper no-ops; PWA/web unaffected). **BUILDER IS NOW LIVE (2026-06-22):** the bake ran server-side (`runBake` + operator endpoints `/api/admin/build/{configure,bake,state}`) and SUCCEEDED — snapshot **`233935491`** (`arksai-android-sdk-2026-06-22`, blr1), toolchain `RESULT=OK` (Node20/JDK17/sdkmanager). Builder **activated via DB config** (DO token encrypted + snapshot id in `app_settings`, no `.env`/SSH; `isBuildConfigured()` true). Activation/recovery without re-bake: `POST /api/admin/build/configure {snapshotId}`. **ROTATE the DO token (pasted in chat); re-run configure with the new token, no redeploy.** Phase 4 (emulator crash-smoke + signing) + Phase 5 pricing are next. Remaining done-condition: build a small (QR) + large (Tinder) APK crash-free (in progress — M3 agent-run latency is the current friction). EAS = Apple/iOS only; Android always our droplet.
- **Also shipped this session (2026-06-22), all on `main`:** `MASTER_PLAN.md` (one-shot quality + grounded-research accuracy) and its phases — NODE_ENV devDeps fix, publish builds any node stack, truthful publish guidance + anti-thrash, subdomain serving (`*.apps.arksai.studio`) + on-demand Caddy TLS (wildcard DNS added in the `marketing.gicbd@gmail.com` DO account, which holds the zone), grounded-research module; **Auto-Brief** (deterministic per-deliverable rigor scaffolds, `server/src/agent/brief.ts`); the **email/IMAP timeout fix** + observable robot poller; the **plan-gate Approve-button fix** (agent must call submit_plan, not prose) + the **budget cutoff raised** (MAX_RUN_TOKENS 5M, MAX_ITERATIONS 500 — only loops/errors stop a run); and the **website-creative text-collision fix** (generate_image text-free by default; generate_creative is standalone-only, never a website background — its compositing logic unchanged so social creatives are unaffected). NOTE: DNS zone for arksai.studio lives in the SECOND DO account `marketing.gicbd@gmail.com` (compute/droplet is in `gicbdfacebook@gmail.com`). Operator to ROTATE the two DO tokens + the ecosine mailbox password that were pasted in chat.
- Everything is on **`main`** (GitHub `chordsnstrings/arksai`) and auto-deploys to the Droplet. A new session clones `main` fresh, so all code carries over; this file carries the context. The ephemeral container's gitignored local `.env` does NOT carry over — re-paste keys for local dev; live keys live in `/opt/arksai/.env` on the Droplet.
- **EMAIL ROBOTS — DONE (on `main`, 2026-06-21).** The Robots surface is now a REAL working agent, not a shell. Plan/research in `ROBOTS_PLAN.md` (+ `ROBOTS_RESEARCH.md`); governing rule "**flawless or it isn't live**"; external autonomy is **trifecta-safe** (taint-breaks-egress, admin-allowlisted destinations, per-channel certification, §5c locked-recipient/data-minimized auto-reply). EMAIL is the first channel, built end-to-end:
  - **Per-robot mailbox (multi-tenant — each robot is its OWN email identity).** `robot_email_accounts` table (robot_id PK), `email/accounts.ts` robot-scoped fns, passwords **AES-256-GCM at rest** (`lib/crypto.ts`, key=`ENCRYPTION_KEY`||`APP_PASSWORD`), write-only over the API. There's ALSO an org-level `org_email_accounts` (for the general agent `send_email`/`read_inbox` tools in chat/code/report). Libs: **nodemailer**(SMTP)+**imapflow**+**mailparser**(IMAP); `email/client.ts` has account-taking cores (`sendWithAccount`/`readInboxWithAccount`) + org wrappers (`sendEmail`/`readInbox`) + robot wrappers (`sendEmailForRobot`/`readInboxForRobot`) + `verifyAccount` (Test connection).
  - **Engine**: `robots/store.ts` (robots + drafts; org-scoped, idempotent per inbound Message-ID), `robots/reply.ts` (the §5c reply engine — data-minimized single-message context, role persona, **locked-recipient + injection-resistant** system prompt, strict-JSON escalation signal, **M3-vs-DeepSeek-v4 bake-off** via `model:'compare'`; DeepSeek uses `config.deepseekReplyModel`=`deepseek-chat`), `robots/poller.ts` (durable 60s tick → reads each ACTIVE robot's OWN mailbox unseen → drafts; `ask`=pending for approval, `auto`=send locked to sender; booted in index.ts). Routes `routes/robots.ts` (org-member-gated CRUD + per-robot `/email` CRUD+test + `/preview` bake-off + draft send[recipient LOCKED to stored to_addr]/edit/dismiss).
  - **UI = main's full-page console** (`Robots.tsx`, sidebar 🤖, `/robots`) now **backed by the engine**: `state/robotsStore.ts` rewired to the API (was a client-only mock); pending drafts → the **"Needs You"** inbox (Approve&send/Dismiss); each robot's Office has a **Mailbox** panel (`EmailSettings` reused with a `robotId` prop). Mapping: console autonomy ask_all/ask_big→`ask`, autonomous→`auto`; dept/mandate/triggers live in robot `config`; every console robot is currently a `custom` email agent grounded in its mandate. Org mailbox tab + main's connectors `ConnectionsPanel` both live under Admin.
  - **OAuth (already scaffolded by the connectors arc on main):** public base = `config.publicBaseUrl` (`PUBLIC_BASE_URL`||**`https://arksai.studio`**); OAuth **redirect/callback = `https://arksai.studio/api/connectors/<provider>/callback`** (`redirectUri()` in `connectors/index.ts`; route `GET /api/connectors/:provider/callback` in `routes/connectors.ts`), then it redirects back into the app at `https://arksai.studio/?connect=<provider>&status=…`. Use this pattern for Gmail/Google OAuth mailbox connect (vs SMTP app-password).
  - **NOT yet live-verified** (needs the `sk-cp` MiniMax key + a real mailbox on the Droplet): real model drafting + real send/receive. **Pending/next:** real **iCal accept/decline** for the personal-assistant (operator chose to build it — detect `text/calendar` → draft accept/decline → send `METHOD:REPLY`); a "connect a mailbox" prompt in the Hire flow; surface the customer/personal email-role personas in the console hire.
- **Universal quality engine — DONE (on `main`, 2026-06-15)**: EVERY deliverable type (web app, PDF report, 16:9 deck, .docx, .xlsx, and NEW real editable **.pptx**) is now (a) rendered to image(s), (b) functional-checked ("it works"), and (c) design-reviewed by the M3 senior-design-director rubric, then bounded-revised (≤2 rounds) — the "looks perfect, works perfectly" guarantee generalized from web-apps to all types. New `server/src/agent/deliverableCheck.ts` (`checkDeliverable(file, kind)`): renderers = Chromium (web/html), **mupdf** raster per page (pdf), shared `renderDocHtml` (xlsx/docx), LibreOffice-or-`.preview.html` (pptx); functional = xlsx re-open+formulas+no-#REF, docx re-open+font-embed, pptx valid-slides, pdf page/blank. Wired into `runVerifyGate` (code: gates produced docx/xlsx/pptx/pdf) + new `runReportGate` (report: auto-renders+reviews every page, replacing manual see_image). **Vision (`analyzeImage`) now uses the Anthropic endpoint** (M3 thinking off → ~11s decisive verdict, even catches data inconsistencies; was on the broken /v1) with a 60s timeout→degrade. New `generate_pptx` (PptxGenJS, editorial 16:9, emits a `.preview.html` mirror for canvas+QC), registered in ALL_TOOLS+REPORT_TOOLS. Per-type design standards added to the generate_* tool descriptions. **PDF bleed fix**: report protocol now mandates ATOMIC visuals (`.keep`/`.fig` wrapper, broadened break-inside:avoid) so a chart/bar-list/figure can never split across a page (the user's recurring complaint). Deps added: `mupdf`(ESM+TLA → Function-`import()`), `pptxgenjs`. **NOTE: full end-to-end-through-the-agent validation (report→gate→revise; pptx via the live agent) still needs a run with a model key — each PIECE was validated in isolation (vision 11s; PDF gate caught the Bangladesh-report bleed; pptx builds+renders+gated) but the wired loop wasn't exercised live before this was written.** LibreOffice is an OPTIONAL fidelity path (Dockerfile, non-fatal); default doc/sheet render = HTML→Chromium, pptx = the preview HTML.
- **MODEL ROUTING + LATENCY (validated via a 6-type live sweep, 2026-06-15)**: M3 quality is best (it built a sophisticated LIVE dashboard end-to-end + the reports are magazine-grade), BUT M3 stalls on LARGE single outputs (a full report HTML, a detailed multi-sheet formula .xlsx, a .pptx) — MiniMax serving-bound (~7s first byte, 24–76s buffering gaps; `service_tier:priority` does NOT help). M3 is fine on SMALL/incremental turns (the app, a docx memo = 3min). So: **code mode → M3** (patient, `MINIMAX_TURN_DEADLINE_MS`=150000; falls back on a stall); **report mode → fast model directly** (`config.minimaxFallbackModel`=`MiniMax-M2.7-highspeed`, in `createMinimaxStream`: `useFast = minimaxFellBack || mode==='report'`) — same quality, ~3min vs ~20min. The stall→fallback deadline MUST be armed BEFORE the fetch (M3 is slow to send headers on big prompts; a generator-only timer hangs). **Document gates cap at 1 revise round + demand MINIMAL TARGETED edits** (code `reviewDeliverables` + `runReportGate`) because re-authoring a whole sheet/doc/deck per round is the latency sink. Sweep renders sent to user: app, report (2pp), docx, pptx all best-in-class + no bleed; xlsx structure (Summary/Assumptions/Cash Flow) correct + formula support EXISTS but the model used it INCONSISTENTLY (only 1 of 6 cash-flow models was formula-driven — the rest hard-coded every number) → now ENFORCED (see the Excel-calculations item below). FOLLOW-UP: a clean all-6 sweep on the stable Droplet (open egress + LibreOffice). **DONE (this session): the one-time ~150s M3-stall tax on big xlsx/pptx/docx in CODE mode is fixed** — `HEAVY_GENERATORS` (`generate_spreadsheet`/`generate_pptx`/`generate_doc`, in runner.ts) get a SHORTENED deadline (`MINIMAX_HEAVY_TOOL_DEADLINE_MS`=30000) the instant the tool-call block starts streaming on M3 (detected via the Anthropic SSE `content_block_start` tool name, only when `isPrimary`/not already on the fast model), so the run falls back to the fast model in ~30s instead of 150s and re-issues that turn there.
- **M3 STALL — ROOT CAUSE FOUND + FIXED (2026-06-20, validated with raw API probes against `api.minimax.io/anthropic` using the live sk-cp key)**: the "hang" is **server-side STARVATION under CONCURRENCY**, not slowness. Isolated, M3 buffers a large tool-call arg server-side then bursts (one ~78s silent gap for ~5KB; ~107s for a full 3-statement model) — fine. But with **4+ concurrent M3 streams it STARVES the losers**: they get keepalive **pings** (so inter-chunk gaps stay 2–13s) while producing **ZERO content for 300–400s**. Measured: 1–3 concurrent complete cleanly (73–110s), 4 degrade, **6 → 0-output starvation**. The old gap-based idle timer was **defeated by the pings** (gaps never hit 150s) → the 900s "running, file written but never finishes" hang. **FIX (runner.ts):** (1) a **global `Semaphore` limiter** (`MINIMAX_MAX_CONCURRENCY`=3) caps concurrent MiniMax/Anthropic streams so they can't starve each other (excess turns queue; abort-aware; released in the adapter's `finally`). (2) the idle timer is now **CONTENT-based** — `arm()` resets ONLY on real `text/thinking/input_json` deltas + `content_block_start`, NOT on pings — so a ping-only starved stream trips the 150s backstop and falls back. (3) the **eager 30s `HEAVY_GENERATORS` fallback is DISABLED by default** (`MINIMAX_HEAVY_TOOL_DEADLINE_MS` now defaults to 0/off) — QUALITY-FIRST: with the limiter preventing starvation, M3 finishes a big model in ~107s (< the 150s content-idle), and bailing to the weaker fast model hurt quality. (4) a one-time **"Still working — larger build, hang tight…"** progress notice fires at 55s so the user knows a long-but-healthy turn isn't hung. **QUALITY BAKE-OFF (M3 vs DeepSeek-v4-pro, same large 3-statement model):** M3 finished in 107s with a genuinely formula-driven model (P&L 338 formulas inc. compounding growth, Cash Flow 130 cross-sheet formulas, realistic UAE specifics); **DeepSeek-v4-pro at `reasoning_effort:max` did NOT finish (0 output in 280s — over-reasoned).** → **KEEP M3 for heavy generators; do NOT switch to DeepSeek.** (M3 formula quirk seen in the raw probe: `.`-dot notation `=Assumptions.B10` instead of `!` — the real tool schema steers `!`/`{f,v}` better.) Operator principle going forward: **quality first; long is fine if there's no hang, always eventual output, and the user is told.** NOTE: DeepSeek V4 (shipped 2026-04-24) now has explicit `thinking:{type:disabled}` + `reasoning_effort` and an Anthropic endpoint → TODO: disable DeepSeek thinking on generator turns + migrate off the `deepseek-chat`/`deepseek-reasoner` aliases (deprecated 2026-07-24).
- Built this arc: **ArksAI Auto** orchestrator (complexity routing Flash/Pro/MiniMax + escalation, server-authoritative blended cost), **MiniMax capability tools** (see_image/generate_image/text_to_speech/generate_video), and **Report mode** (bespoke designed PDFs/decks; full design protocol + page mechanics above).
- **"Streamline the core" arc — DONE (all 5 phases on `main`)**: the one-message → ask a short brief → build autonomously → verify → auto-show flow, IDENTICAL across deliverable types ("Apple, for everyone"). (1) **Auto-open + auto-load the canvas for ALL deliverables** (not just code apps): `open_canvas` event carries `{port?,file?,kind:'app'|'pdf'|'sheet'|'doc'}`; `runner.ts` completion block emits it after a successful run (renderable app→exact port via `pickPreviewDoc` for the newest PDF/xlsx/docx); client `Canvas.tsx` has a `doc` tab + `sessionStore.canvasTarget` loads it deterministically; new `routes/docview.ts` (`/api/sessions/:id/docview/*`) renders xlsx/csv→SheetJS HTML + docx→mammoth in a styled shell. (2) **Bulletproof verification**: `runtimeCheck.fillParams()` exercises parameterized routes (`:id`/`{id}`/`<int:id>`/`:slug`) instead of skipping; `uiCheck.browserSmokeTest` runs an **interaction pass** (seed inputs, submit first form, click non-destructive primary buttons, then re-collect errors); **post-publish smoke test** in `deploy/publish.ts` hits the real `/apps/<slug>/` URL and marks the deployment `error` + returns the defect to the agent via `publish_app` so the user never gets a broken link. (3) **Shared smart intake**: `prompts.ts intakeContext(profile)` — a type-aware `## Intake` (one round, then autonomous) wired into CODE mode. (4) **Excel/Word parity**: new `tools/excel.ts` `generate_spreadsheet` (exceljs, styled + re-open-validated) + `tools/docx.ts` `generate_doc` (docx, typographic) — deps `exceljs`+`docx` added; both in code+report modes (and REPORT_TOOLS). (5) **Publish polish**: `DeploymentsDialog` surfaces the live URL (Open/Copy) ONLY when verified-green. Tests: 38 passing (added `runtimeCheck`/`intake`/`docgen` suites).
- **"Effortless AND expert" UX arc — DONE (on `main`)**: guiding principle **"visible competence builds trust"** — keep the technical work on screen, framed as confident expert progress (NOT de-jargoned) so a non-technical user feels in good hands. (A) New structured `progress` AgentEvent (`ProgressPhase` understanding→building→verifying→testing→polishing→publishing→done + `phaseFloor`/`phaseCeiling` bands in `shared/types.ts`); `runner.ts` emits it at every macro boundary (monotonic pct), and `verify.ts`+`runtimeCheck.ts` report sub-steps ("Type-checking", "Running the test suite", "Booting a live instance", "Exercising every route…") via `onPhase` callbacks. Client `ProgressBar.tsx` (rendered under TopBar) shows a slim shimmer bar with an **anticipation "creep"** (eases toward the current phase ceiling so it never looks frozen during install/boot gaps). `StatusFooter` leads with the live expert action (keeps tokens/cost as proof of work). (B) **Self-healing reframed as forward progress, not failure**: a caught issue in `runVerifyGate` reads "hardening it (pass N)" with pct held; red error reserved for terminal only. (C) **Onboarding**: new `Launchpad.tsx` replaces the bare empty state — one-step describe→"Make it" (creates a session with **Code + `arksai-auto`** defaults and sends the first message in one action; sends BEFORE `setActive` to avoid the load race), click-to-fill example cards, "advanced" link → `NewSessionDialog` (now defaults to Auto + has per-mode descriptions). (D) **Delivery moment**: `Canvas.tsx` shows "Booting your live app…" loading + friendly Retry instead of a blank/"no dev server"; doc loading state; slide-in. New **completion card** in `Chat.tsx` ("✓ Your app is ready") with Open + "Get a shareable link" (verified publish). `LiveState` gained `progress`+`completion`. Tests: 42 passing (added `progress.test.ts` phaseFloor bands); Playwright-verified launchpad render + one-step run-start with the progress bar. NOTE: full happy-path e2e (verify→testing→completion→live canvas) needs a real DeepSeek key + a build — unverified from this sandbox.
- **B2B PIVOT + distinctive identity — Phase 1 DONE (on `main`); the new north-star direction**: the product is repositioning from "consumer maker for everyone" to **B2B — onboard companies by empowering distinct corporate functions** ("not just another wrapper for all"). User feedback: the launchpad looked like every other AI app (dark canvas, centered hero, glowing prompt box, emoji cards). Differentiator = the product is **organized around departments** and speaks their language; ArksAI already produces every artifact a team needs (apps, decks/PDFs, dashboards, `generate_spreadsheet`, `generate_doc`), so it's curation + language + a distinct look, not new capability. **Approved 3-phase plan** (in `/root/.claude/plans/i-want-to-create-floofy-lantern.md`): (1) department-aware in-app experience [DONE], (2) B2B acquisition landing + lead capture [DONE — `client/src/components/Landing.tsx` shown pre-login via `App.tsx`; public `POST /api/leads` (rate-limited, allowlisted in `auth.ts`) + admin-gated `GET`; dual-driver `leads` table; editorial hero + by-dept value grid + how-it-works + lead form + FAQ; Playwright-verified funnel], (3) org/team multi-seat platform — orgs/users/memberships/invites, per-user auth replacing the single `APP_PASSWORD`, org-scoped data, departments→per-org templates [AFTER, big + security-sensitive]. Lead departments: **Marketing, Sales, Finance/Strategy, HR·People & Ops**. Positioning: "Give every team in your company a builder."
  - **Phase 1 shipped**: new `client/src/lib/departments.ts` (typed catalog: each dept has accent + line-icon + ~4 ready-to-run "plays" → real prompts in the right `mode`). Rebuilt `Launchpad.tsx` = two-step (pick function → that team's plays + free-form box), remembers choice in `localStorage`, reuses the one-step create+send. **NEW VISUAL IDENTITY (token-driven `theme.css` restyle — the app is now LIGHT/warm editorial, not dark)**: warm ivory/paper canvas, **Source Serif 4 (display) + Inter (body) + Space Grotesk (labels)** self-hosted at `client/public/fonts/` (the SAME fonts the reports use → one identity; Google-Sans CDN removed from `index.html`); publication masthead, numbered tiles, hairline rules, per-department accent coding. The whole shell flipped to light via tokens (only ~13 hardcoded dark spots needed fixing). Playwright-verified desktop+mobile. NOTE: chat/canvas-with-a-running-session in the light theme is unverified without a model key — eyeball on the Droplet. Visual "soul" is the one taste call still open (editorial chosen; tactile "Instrument" / warm "Studio" were the alternatives).
- **Smart agent: image recognition + auto mode/engine switching — DONE (on `main`)**: (A) **Uploaded images are now seen** — `runner.noteUploadedImages()` injects a context note at run start for recent `uploads/` images telling the text-only agent to `see_image` the path (it previously had no idea a photo existed); chat+code prompts + the `see_image` description call out uploaded photos; if `MINIMAX_API_KEY` is unset the note says image viewing is unavailable instead of guessing. (B) **The agent switches mode itself** (the 4 modes stay; agent drives between them) — new `switch_mode` tool (`tools/mode.ts`, all modes, sets `ctx.requestModeSwitch`); the runner applies a pending switch between tool batches: reloads `getToolsForMode` + rebuilds `buildSystemPrompt` + re-routes the engine (refactored into `routeModel`/`floorModel`), emits an inline "Switched to Build mode" system line + `session_meta_updated` so the toggle follows. Chat prompt instructs it to switch AUTOMATICALLY (no confirm) when the user wants something built (→code) or a polished PDF/deck (→report). (C) Added an **Engineering** department (internal tool / API / prototype / tech doc) to the catalog — 5 depts now. 47 tests (added `mode.test.ts`). NOTE: live vision + mid-run switch need a real DeepSeek/MiniMax key (Droplet) to exercise end-to-end; MiniMax-VL request shape still unvalidated from sandbox.
- **Day-to-day platform arc — DONE (on `main`)**: made ArksAI useful for each department's *everyday* work. (1) **Comprehensive task catalog**: `client/src/lib/departments.ts` expanded from ~4 to ~8–11 researched tasks/department (5 depts, ~50 plays), each tagged `category: create|analyze|operate` (the `CATEGORIES` const). `Launchpad.tsx` renders plays grouped under Create/Analyze/Operate headers; `Landing.tsx` shows top-5/dept + "+N more". (2) **Recurring tasks — durable server scheduler**: `server/src/schedule/scheduler.ts` (pure `computeNextRun` for daily/weekly/interval; 60s `tick` advances `next_run_at` first then fires by spawning a fresh dated session + `manager.startRun`), `schedules` table, `routes/schedules.ts` CRUD, `startScheduler()` at boot, client `SchedulesDialog` via the sidebar "Scheduled" button. Replaces the ephemeral client `useAutomation` loop. (3) **Data IN (credential-free)**: `tools/data.ts` `fetch_data` pulls a PUBLIC CSV/JSON/published-Sheet URL (SSRF-guarded via `lib/web.fetchPublic`, dep-free CSV parser) → dashboards/reports off real numbers. (4) **Deliver OUT (credential-free)**: `tools/outbound.ts` `send_webhook` POSTs to a user-provided Slack/Zapier/Discord hook. **No new agent modes** (the 4 cover all making). (5) **Per-task EXPERT STANDARDS (server-side domain rigor)**: `server/src/agent/expertise.ts` — a per-department persona (FP&A rigor / inclusive HR / RevOps / brand-growth / senior-eng) + ~50 research-backed per-task standard blocks (FAST financial-model structure, deck craft, dashboard F-pattern + 5–7 KPIs, landing CTA eye-path, inclusive JD, specific offer letters, battlecard comparison + "why we win", problem-first PRD, runbooks…). Each play carries a stable `key` (`finance.cashflow`); `createSession({task})` → `sessions.task` column → `SessionMeta.task` → `buildSystemPrompt` injects `expertiseFor(task)` (all modes, survives `switch_mode`). All 52 catalog keys resolve; DB round-trip verified. Tests: 65 (added `datatools`/`schedule`/`expertise` suites); scheduler fire-path + Schedules-dialog Playwright-verified. **STAGED (needs credentials on the Droplet; NOT committed to the public repo)**: OAuth connectors for private Google Sheets/Drive + CRM, a Slack app, Drive push, and SMTP `send_email` — framework + config gates + docs are the follow-on. Scheduled runs / live data / webhook delivery need a model key + open egress → validate on the Droplet.
- **Match/beat Claude on reports arc — DONE (on `main`)**: benchmarked Report mode vs a Claude Code run on the GIC Dhaka brief; Claude's edge was real charts (matplotlib dual-axis + month×year heatmaps), a designed dark KPI cover, reconciled analysis, and methodology/sources. Closed it WITHOUT blowing the latency budget: (1) **`render_chart` tool** (`server/src/agent/tools/chart.ts`) — Vega-Lite → SVG server-side (no browser/Python/`canvas`; vega+vega-lite are ESM-only, loaded via the same `Function('s','return import(s)')` native-import trick mupdf uses). Types: line/multi_line/**dual_axis**/bar/bar_h/stacked_bar/area/donut/**heatmap**; editorial defaults baked in (flat, transparent bg, muted base + accent on the key series, value labels, faint warm gridlines, Inter). Registered in `tools/index.ts` + `REPORT_TOOLS`; pure `buildVlSpec` unit-tested (9 tests). The protocol now routes data-viz through it (prefer dual_axis for two-metric time-series, heatmap for x×y matrices). (2) **Designed cover** (prompts.ts Report block) — image-free but composed: masthead, accent title line, one-line thesis, a KPI band ON the cover, a metadata footer, fill-the-page, + a first-class dark full-bleed variant (negative `@page`-margin bleed) for finance/BI/markets briefs. (3) **Deterministic structural pre-check** in `deliverableCheck.ts` — `rasterizePdf` now also returns per-page ink coverage; `detectEmptyPages()` (pure, unit-tested) flags lonely near-empty interior pages with NO model call, seeded into defects so it gates even without vision egress. (4) **Analysis + insight + methodology rigor** added to the Report block — systematic analysis pass first (profile, full cross-tabs, RECONCILE conflicting fields), insight-led narrative (counter-intuitive reframe → ranked recs tied to specific data points), a Methodology/Notes section (proxies + limits, gaps, source attribution). (5) **Quality-first revise cap** — report gate raised 1→2 rounds (`REPORT_MAX_DESIGN_ROUNDS` in `runner.ts`), affordable because the deterministic check finds cheap defects. 96 tests green; typecheck+build clean; charts visually verified (rasterized — flat, on-brand). NOTE: full e2e (agent calling `render_chart` + the dark cover on the real GIC brief, vision QC) needs a model key + MiniMax egress → run in a fresh session on the Droplet.
- **Operator isolation + onboarding-logo fixes — DONE (on `main`, 2026-06-16)**, from operator feedback ("I can still see other orgs' chats from operator"; "the logo was uploaded but the agent didn't understand it"). (1) **The operator is now scoped to its OWN workspace, like every tenant** — the `isSuperadmin` see-all bypass was the leak. Changed `scoped()` (sessions/store.ts) + `isTenant()` (scheduler.ts) to filter on `orgId != null` (member OR operator; only `undefined`/internal callers stay unscoped), and removed the `scope.isSuperadmin` bypass in the `/api/sessions/:id` + `/api/projects/:id` preHandlers and the `/api/sessions-events` global stream. The operator sits on `DEFAULT_ORG_ID` (there's no switch UI; the `/switch` endpoint is unused by the client) so it sees only Default's data; `isSuperadmin` still unlocks the admin endpoints (`/api/admin/*`, provisioning, leads, any-org member mgmt) + WITHIN its own org it still bypasses per-user private-project visibility (getProject/listProjects keep that). Red-team test extended: the operator opens its own chat but 404s on another org's session/events/projects/schedules/deployments while keeping `/api/admin/orgs`. (2) **Onboarding reads an uploaded logo** — the org.onboarding toolset now also injects `extractPaletteTool` (deterministic brand colours, NO vision needed), the onboarding prompt has a logo step ("call extract_palette … NEVER say you can't view it"), and `buildUploadNote` gained a `paletteAvailable` arg so an uploaded image in onboarding/code/report steers to `extract_palette` (+ see_image when keyed) instead of the "image viewing unavailable" line. NOTE: the earlier "MINIMAX_API_KEY not set" theory was WRONG — vision/MiniMax work on the Droplet (`see_image` is `available: !!minimaxApiKey`, modes chat+code); the real gap was that onboarding never steered the model to read the logo + lacked `extract_palette`. 144 tests green.
- **Vision-everywhere + addictive onboarding — DONE (on `main`, 2026-06-16)**, from operator feedback ("vision works, stop saying it doesn't"; "few users finish onboarding — make it addictive, research dopamine"). (1) **Vision in ALL modes + zero defeatist messaging**: `see_image` modes now `['chat','plan','code']` (+ report via REPORT_TOOLS) = all 4; removed every "image unavailable / you can't view it / MINIMAX_API_KEY not set" branch from the chat+code prompts, the onboarding prompt (my own stale "you don't see pictures" line), and `buildUploadNote` (now always steers to see_image; +extract_palette for logos). NOTE the gotcha that bit me twice: putting the literal phrase "can't view" in steering text trips the `doesNotMatch(/can't view/i)` test — phrase positively ("treat every image as fully readable"). (2) **Onboarding rebuilt for completion via dopamine/anticipation** (research: dopamine = reward-prediction-error/seeking; what drives the next step is a meaningful expectation, and reward that BEATS expectation): new `prompts.ts` org.onboarding block = creative-director persona, hook+one-ask → live REVEAL → momentum → peak-end ("build you something now"); `OrgOnboarding.tsx` rebuilt with an anticipation hero, an **endowed 3-step tracker** (Brand→Profile→First build, step 1 lit from t0), and a **live brand-reveal panel** (`.onb-stage`/`.onb-reveal` in theme.css, staggered swatch animation) that polls `api.getOrgProfile` after each turn. KEY wiring: the prompt makes the agent call `save_org_profile` EARLY (complete:false) right after extract_palette/crawl_site so the panel lights up DURING onboarding (a late-only save would flip onboardingComplete→onDone and leave before the reveal shows). Playwright-verified empty(anticipation)+lit(reveal) × desktop/mobile × light/dark. WhatsNew bumped to 2026-06-16.5.
- **Dopamine/UX pass — DONE (on `main`, 2026-06-16)**, from a hands-on audit (drove the real flows locally with the live keys; egress reaches deepseek+minimax from THIS session, so I booted the built app and Playwright-drove it). Two background subagents produced a component-level + a journey/dopamine audit (~48 findings). Shipped batch (all verified live): (1) **optimistic running** — `beginRun(sessionId)` in the store fires on send so the progress bar+footer appear instantly (no silent gap); Composer rolls it back + restores the typed text on failure. (2) **Launchpad hand-off** — per-play "Starting your workspace…" + sibling dimming (`startingId`). (3) **Session-switch flash FIXED** — App renders the chat shell whenever `activeMeta` exists using `emptyLive()` fallback (exported from the store), so switching never flashes the Launchpad. (4) **Flat completion FIXED at the root** — a Bash-heredoc build never set `this.mutated`, so no `open_canvas`/completion card fired; now bash commands matching write-patterns set mutated. The CompletionCard was rebuilt: staged reveal (ccReveal/ccCheck anims), a **live iframe thumbnail** of the result (from `canvasTarget` port/file), Open + Share, "Copied ✓", and a one-tap **"Fix & republish"** recovery. (5) **Visible competence** — friendly tool labels (`see_image`→"Looking at your image", `render_report`→"Designing the PDF", …) + a live **ticker** of the current action, auto-expanded while running. (6) **Warm failures** — plain-language, non-scary copy for the output-budget/stall/verify-fail terminal errors; static-site verify now says "No build step needed — it's a static site" not "No recognizable project to verify". (7) **Composer** — autofocus on session open, `/`-menu only opens for slash text. (8) **Onboarding** reveal now polls mid-turn (1.8s) so swatches pop at discovery. (9) Esc closes any dialog (global handler in App + `useEscClose` hook for DeploymentsDialog); chat autoscroll only when near-bottom; ProgressBar stops its rAF at the ceiling; CostBar tooltip is brand-neutral ("Estimated cost for this session" — operator: no DeepSeek/MiniMax in pricing UI). **Validated live**: a fresh build showed the ticker ("Designing an image · …"), inherited the org brand teal, and ended on the staged completion card WITH a working thumbnail. **NOTE/FOLLOW-UP**: the auto-opened Canvas panel showed "Error" for the static site while the completion-card thumbnail (same preview, exact port) rendered fine → Canvas's `pickPreviewPort` should prefer `canvasTarget.port`. **Deferred next batch** (told the user): replace native `confirm()` with an in-app modal, a landing live-demo loop, a "Delivered" surface for scheduled runs, streamed publish-phase events, dialog focus-trap, a "Jump back in" recents row, finer report-phase beats.
- **Dopamine/UX pass — batch 2 (the deferred items) — DONE (on `main`, 2026-06-16)**, all verified live: (1) **Canvas port FIX** (the "Error" I saw): `pickPreviewPort(ports, prefer)` + a sticky `targetPortRef` set from the `open_canvas` port — the exact port always wins over noise ports (2024/2025/…) and the dead-port check never drops it. (2) **In-app confirm modal** replaces native `confirm()` — `state/confirmStore.ts` (promise-based `confirmDialog()`) + `components/ConfirmModal.tsx` (mounted once in App, zIndex 50, Esc=cancel); wired into Sidebar delete, ProjectDialog delete, AdminDialog remove-member + out-of-domain invite; App's global Esc defers to an open confirm. (3) **"Jump back in" recents row** on the Launchpad team-picker (last 4 non-onboarding sessions → setActive). (4) **Finer build/report beats** — `TOOL_BEAT` map in runner.ts emits a friendly phase label per tool while `progressPhase==='building'` ("Drawing your charts…", "Setting the typography…") so the long report wait visibly moves. (5) **Streamed publish phases** (pragmatic) — the completion-card + DeploymentsDialog publish buttons walk "Snapshotting → Installing → Booting → Checking the live URL" on a timer while busy. (6) **Scheduled "Delivered" surface** — scheduler stamps spawned sessions `task:'scheduled'`; Sidebar shows a ⏱/✓ badge (green when done) so automated deliveries are visible (unseen-notification refinement still open). (7) **Landing "watch it build" demo** — `LandingDemo` in the hero, a pure-CSS 11s loop (brief → building ticker → finished live card; collapses to the finished card under reduced-motion). **Validated live**: in-app confirm ("Delete this session?"), the recents row (2 chips), the landing demo, and the completion card + Canvas all render correctly. **STILL DEFERRED**: dialog focus-trap (Esc is done), and the scheduled-run UNSEEN notification/badge-count. 144 tests; typecheck+build green.
- **UAE Legal department — BUILT (on `main`, 2026-06-16)**, per `LEGAL_DEPARTMENT_PLAN.md`. New `legal` department (8th) in `client/src/lib/departments.ts` (accent `#7a2e3b`, new `scale` icon) with **15 plays** grouped create/analyze/operate — incl. the operator's specific asks: **police report / criminal complaint** (`legal.policereport`, cites Penal Code 31/2021 + Cybercrime 34/2021), **legal forensic audit** (`legal.forensic`), **legal opinion/memo** (`legal.opinion`), plus contracts/NDA/employment/POA/MOA+SHA/resolutions/notice-letter-before-action/contract-review/compliance-audit/dispute-brief/licensing-advisor/legal-calendar. Standards/persona live in **`server/src/agent/legal/uae.ts`** (`LEGAL_PERSONA` + `LEGAL_TASKS`), wired into `expertise.ts` (`DEPARTMENT.legal` + `...LEGAL_TASKS`). The persona enforces: **formal British English + legal register/jargon**, **jurisdiction-first** (mainland vs free zone vs DIFC/ADGM), **bilingual DELIVERABLE — the produced DOCUMENT is bilingual in eloquent posh Modern Standard Arabic that is NOT a literal translation** (native legal idiom + diacritics), while the **CONVERSATION mirrors the user's own language (never bilingual chat)** — the operator flagged that chat was replying in Arabic; fixed by splitting communication-language from the bilingual-document rule in `LEGAL_PERSONA`, then refined so bilingual applies BY DESTINATION (only government/court/notary submissions — police report, notarised POA/MOA, onshore MOHRE contract, notice served via Notary/court; internal docs — opinion/review/forensic/compliance/dispute/licensing — default to English in the user's language) (2 locking tests), **execution-ready** (full clauses/boilerplate/schedules/signature blocks + bracketed placeholders) so counsel reviews+signs not rewrites, **cite law+article, never fabricate**, and a **mandatory bilingual lawyer-sign-off footer** (not legal advice). **MODEL BAKE-OFF (live, this session): MiniMax M3 (`arksai-max`) decisively beat DeepSeek v4-pro on legal drafting** — M3 produced a complete 14.5k-char bilingual letter-before-action with excellent British-English legal register + native posh Arabic in a few min; DeepSeek v4-pro (thinking mode) produced **0 chars in 5+ min** (unusable). So **all legal plays default to `model: 'arksai-max'`** (M3 = `M3` const in departments.ts). LATENCY NOTE: M3 is slow on long single outputs — the real plays mitigate (report-mode plays route MiniMax→fast model via `useFast`; code-mode `generate_doc` is a HEAVY_GENERATOR with the 30s stall→fallback). My CHAT-mode validation run hit full-M3 latency (0 chars/5min, no play uses chat) so a clean persona-driven sample wasn't captured live, BUT the bake-off proved the exact quality + typecheck/144 tests/build pass + the wiring resolves. FOLLOW-UPS: bilingual Arabic/RTL polish in `generate_doc`; report-mode legal docs now also use M3 (legal is excluded from the report-mode `useFast` in createMinimaxStream) — a live M3-vs-M2.7 bake-off (a non-compete legal opinion) was DECISIVE: M3 returned a 12k-char law-firm-grade opinion citing Art. 10 of Federal Law 33/2021 in ~66s, while M2.7-highspeed (an "always-think" coding model) spent its whole budget on a thinking block and returned 0 usable chars; Tier-1/2/3 filing automation (UAE Pass e-notary, MOEC, courts) per the plan.
- **Admin Analytics (operator console + per-org dashboards) — DONE (on `main`, 2026-06-17)**, from "we need to see more granular data/usage… understand retention and how people use it." Operator chose: BOTH operator console AND per-org dashboards now; ADD an event-tracking layer now; named per-org/per-user drill-down with **METADATA ONLY (never chat/document content)**. Plan in `/root/.claude/plans/hidden-orbiting-bachman.md`. **Two data sources feed one service**: a new **`analytics_events`** table (+ non-blocking `track()` in `server/src/analytics/track.ts`, fire-and-forget, with a **write-side scalar-only `props` allowlist** so content can never be persisted) wired at `app_open` (auth.ts `/api/auth/me`) + `run_finished` (runner.ts), AND **derived-from-existing** aggregates over `sessions`/`deployments`/`orgs`/`users`/`org_profiles`/`leads` (so metrics work from day one, richer as events accrue). `day` is a **precomputed epoch-day bucket** (floor(ts/86400000)) → ZERO date-SQL (SQLite+PG portable, the repo's standing rule). Pure math in `server/src/analytics/compute.ts` (`engagement` dau/wau/mau+stickiness, `dailyActiveSeries`, `dailySumSeries`, `cohortRetention`, `funnel`); aggregation in `server/src/analytics/queries.ts` (`Scope={orgId:string|null}`, null=platform; `activePoints` = events UNION session activity). Routes in `server/src/routes/analytics.ts`: operator `/api/admin/analytics/{overview,timeseries,usage,quality,cost,retention,funnel,orgs,users}` (`isSuperadmin`-gated) + per-org `/api/orgs/:id/analytics/{overview,timeseries,usage,quality,retention,funnel,members}` (`canSeeOrg` = superadmin OR org admin). Client: full-screen **`AnalyticsConsole.tsx`** (sidebar 📊, superadmin-only) + per-org **`OrgAnalytics.tsx`** inside `AdminDialog.tsx` (a "Usage & engagement" tab; dialog widens to 940px), sharing hand-rolled SVG primitives in **`analyticsCharts.tsx`** (no chart dep). **Privacy enforced at BOTH boundaries**: write-side props allowlist + a grep test that `analytics/*` never reads `timeline`/content; **gating red-teamed** in `redteam-isolation.test.ts` (org admin → 403 on operator routes + 403 on another org's per-org routes; operator drills any org). **Validated LIVE this session**: booted the built server, operator-logged-in via Playwright → both views render real data (operator: 111 sessions, modes/models/plays/funnel/retention/orgs; per-org KKLM: 8 sessions, $1.64) — screenshots eyeballed. **GOTCHA fixed live**: the per-org `usersTable` query reused `$1` four times → SQLite's `q()` rewrites every `$N`→positional `?` so it 500'd ("Too few parameter values"); fixed to distinct `$1..$4` + 4 values, and added **`analyticsQueries.test.ts`** (DB-backed, runs every query in both scopes against temp SQLite — pure-fn tests can't catch SQL-portability bugs). 157 tests green; typecheck+build clean. **Step "Later" follow-ups — NOW DONE (on `main`, 2026-06-17, second commit), all live-validated**: (1) **per-user drill-down** — `userDetail(scope,userId)` in queries.ts + shared `UserDetailPanel.tsx` (sessions/success/cost/TTFB + 30d activity + plays/modes/models + orgs), clickable rows in the operator Users table AND per-org Members table (`/api/admin/analytics/users/:uid` + `/api/orgs/:id/analytics/members/:uid`, the per-org one **membership-gated** → 404 on a non-member so an admin can't probe arbitrary users). (2) **time-to-first-build** — pure `timeToFirstBuild()` (median signup→first session), a KPI in both consoles + per-user in the drill. (3) **alerts (no setup, derived)** — pure `costSpike()` (last-7d vs trailing-weekly baseline, ignores no-history → no noise) + `churnRisk()` (onboarded+once-active orgs gone quiet 14d+); `alerts(scope)` → operator: churn orgs + per-org cost spikes; per-org: own cost spike + inactive members; shared `AnalyticsAlerts.tsx` "all clear" when quiet. (4) **scheduled digest** — `analytics_digests` table + `server/src/analytics/digest.ts` (`generateDigest` computes a platform snapshot w/ deltas vs the prior, stores it, and POSTs to `ANALYTICS_DIGEST_WEBHOOK` if set via the SSRF-guarded `fetchPublic`; `startAnalyticsDigest()` hourly self-check, generates once per `ANALYTICS_DIGEST_HOURS` (default 24), booted in index.ts), shown in a console "Scheduled digests" table; webhook added to `secretValues()`. (5) **CSV export** — client `toCsv`/`downloadCsv` in `analyticsCharts.tsx`, ↓CSV buttons on the orgs/users/members tables (pure client). 164 tests green (added compute tests for median/TTFB/costSpike/churnRisk + DB-backed userDetail/alerts in both scopes + redteam gating for the new endpoints). LIVE-validated via Playwright: TTFB KPI, all-clear alerts card, clickable users→drill panel (admin@kklm.ae: 8 sessions/$1.64/100%/TTFB), auto-generated digest row, CSV buttons, + per-org Usage tab drill — all render. NOTE: email digest delivery still needs the deferred delivery layer (webhook works now); real cost-spike/churn FIRING needs live data that crosses the thresholds (logic is unit-tested with synthetic data that does).
- **Analytics redesign + Launchpad uploads + marketing image-gen + waitlist auto-cleanup — DONE (on `main`, 2026-06-17, third commit), all live-validated**, from operator feedback ("dashboard isn't aesthetically pleasing… denser, quicker data ingestion, more editorial, better fonts; QC it"; "describe box has no upload/paste like other inputs"; "marketing needs MiniMax M3 image generation wired in"; "remove people from the waitlist once given access"). (1) **Dashboard = a dense editorial "data desk"** — new shared primitives in `analyticsCharts.tsx` (`Stat`, `Delta` WoW-change chip with `invert` for cost, `Sparkline` stretch-to-fill, area `LineChart` w/ gridlines); `overview()` now returns prior-7d (`activeUsersPrev7d`/`sessionsPrev7d`/`costPrev7d`) for the deltas; `AnalyticsConsole`+`OrgAnalytics` rebuilt: serif masthead + 7/30/90 **pill tabs** (replaced the `<select>`), a 6-up **hero stat strip** (tabular Inter numbers + delta + sparkline), a secondary strip, a 3-col charts row (active/sessions/cost), denser 2-col grid + tabular right-aligned tables. **Type system = 3 tiers**: Source Serif (masthead) / Space Grotesk (eyebrow labels + card headers) / **Inter `tabular-nums` for ALL numbers** (the "better fonts" fix). Theme `.an-*` block fully restyled (tighter padding/radii, hairlines). Playwright-eyeballed — looks far better. (2) **Launchpad free-form box now uploads** — `Launchpad.tsx` stages `File[]` client-side (attach button + drag/drop + clipboard paste, mirroring the Composer) and, on Make-it, creates the session → POSTs them to `/api/sessions/:id/upload` → THEN sends the brief (so the runner's upload-note sees them on the first run); chips with remove; `.lp-box` went column-layout + `.lp-attach`/`.lp-attached` styles. (3) **Marketing image generation wired in** — `generate_image` (MiniMax `image-01`, **verified LIVE this session: a real call returned an image, `status_msg:success`, on the `sk-cp` token plan** — so the "image-01 likely stale" note was WRONG for image; it's current) is now actively used by Marketing: a new **`marketing.creative` "Ad & social creative"** play + the `DEPARTMENT.marketing` persona + `marketing.emailkit`/`landing` standards all steer to GENERATE real on-brand hero/ad/social imagery (right aspect ratios, no text-in-image) instead of placeholders, with a CSS/SVG fallback when unkeyed. Locking test in `expertise.test.ts`. (4) **Waitlist auto-cleanup** — `removeLeadByEmail()` in orgs/store.ts (case-insensitive) called inside `createInvite` (the chokepoint for ALL "given access" paths — admin-org-create + member invite) AND `acceptInvite` (defensive); test in `orgs.test.ts`. 166 tests green; typecheck+build clean. NOTE: marketing image-gen END-TO-END through a live agent build (the model actually calling generate_image mid-build + laying images out) wasn't run — the ENGINE + model are verified live, the steering is tested, but a full agent run is a Droplet follow-up.
- **Marketing `generate_creative` — finished image+text+logo creatives — DONE (on `main`, 2026-06-17, fourth commit), live-validated end-to-end**, from operator: "M3 can't place text — workaround: generate image with negative space, then composite the text" + "users can upload a logo too" + "output always PNG/JPEG." **The workaround, prototyped here first then wired**: `server/src/agent/creative.ts` `composeCreative()` — (1) `genBackground` makes a TEXT-FREE bg with reserved negative space (image-01, `aspect→gen-ratio` map in `CREATIVE_SIZES`), (2) M3 **vision** (`analyzeImage`) returns JSON `{zone,textColor,hasText,clutter}` → picks the calmest zone + the readable colour (regenerates ONCE if `hasText`/`clutter≥7`), (3) **pure `buildCreativeHtml()`** composites kicker/headline/subhead/CTA as **pixel-crisp type** (self-hosted report woff2 embedded as base64) + a contrast scrim + the **optional uploaded logo** (workspace path → data URL, fail-soft), (4) headless Chromium (`--no-sandbox`, deviceScaleFactor 2, `page.setContent` + string-form `page.evaluate('document.fonts…')` since server tsconfig has no DOM lib) rasterises to **PNG/JPEG** in `images/`, (5) vision QC → one corrective text-colour flip on a legibility complaint. Tool `tools/creative.ts` `generate_creative` (params: prompt/headline/subhead/cta/kicker/aspect_ratio[1:1,4:5,9:16,16:9,1.91:1]/accent/**logo**/text_color/format), gated on the MiniMax key, modes chat+code, **in ALL_TOOLS + REPORT_TOOLS**. Marketing persona + `marketing.creative`/`emailkit` standards + the play prompts steer to it (use generate_image only for wordless illustration). Cost ≈ $0.026/creative (1 img + 2–3 vision). **Validated LIVE this session** (prototype: a skincare 1:1 + a SaaS 16:9 sent to the operator; wired tool: a 16:9 hero WITH an uploaded logo, vision→bottom/light, screenshot eyeballed). Pure builder unit-tested (`creative.test.ts`) + the marketing-wiring test updated. **Then extended (same commit-arc) with `bullets` (a check-marked feature list, accent-circled ✓) + a top-left BRAND CORNER (real logo OR a dashed "LOGO" placeholder via `logo_placeholder`, with its own radial scrim; text auto-avoids the `top` zone when a corner mark is present)** — both driven by a real operator brief (a UK Tourist Visa 1:1 ad: headline + sub + 3 bullets + logo placeholder) which was **run LIVE through the pipeline and looks professional** (Big Ben/London Eye/red bus, vision→bottom/dark+light-scrim, crisp text, red ✓ bullets; sent to the operator). Then (per operator) the agent now **asks the user for their logo + brand accent BEFORE building a creative** (persona "BRAND FIRST" + tool desc + play prompt; uses logo_placeholder if they have none), social creatives carry a research-backed **high-conversion `SOCIAL` block** (benefit hook in ~3 words, ONE CTA always, truthful proof/specifics, eye-path logo→hook→bullets→CTA, thumbnail-legible, 9:16 safe-area), and — operator correction — **email vs social are optimized SEPARATELY** (the `SOCIAL` block lives on `marketing.creative` + the generate_creative tool, NOT on the always-on persona and NOT on `marketing.emailkit`, which is inbox-optimized: subject/preheader/one-CTA; a locking test asserts the email standard contains no social-scroll tactics). 175 tests green. **FOLLOW-UP/known limits**: M3 (a) sometimes drifts a foreground element into the "empty" zone (QC + regenerate-on-clutter mitigate; a multi-candidate "pick the cleanest" pass is the next robustness step), (b) is imperfect on SPECIFIC ethnicity/landmark fidelity (the visa couple read less distinctly "South Asian"; Tower Bridge was ambiguous vs Westminster) — tighten the imagery prompt, but it's never guaranteed; (c) a logo that doesn't contrast with the corner scrim could read poorly (most ad logos are light/transparent — fine).
- **Seamless chat overhaul — DONE (on `main`, 2026-06-17, fifth commit), UI-validated**, from operator ("go chat, no pills; ask for context when vague; coding → plan → approve/revise; department selection should READY the agent not fire a canned prompt"). (1) **No mode pills** — removed the Chat/Plan/Code/Report toggle from `Composer.tsx`; the box is one seamless input (skill-agnostic placeholder; `/mode` still works for power users). A read-only **current-skill chip** in `TopBar.tsx` (`.mode-chip`, maps chat→Chat/plan→Planning/code→Building/report→Report) shows what it's doing. (2) **Default mode = chat** (`routes/sessions.ts` `'code'`→`'chat'`; Launchpad free-form + NewSessionDialog default chat) so the agent routes from a plain message. (3) **Chat auto-route rewritten** (`prompts.ts` CHAT block): build→plan-gate, report→report, one-off sheet/doc→code, **IMAGE→generate_creative/generate_image right in chat** (fixes the deployed "I don't have an image tool" lie the operator screenshotted). (4) **Vague → ask for context FIRST** (new prompt section: a thin ask like "make me an image" → short clarifying round, never refuse, never blind-guess). (5) **Plan-gate**: a coding ask → `switch_mode('plan')` → present the exact plan → END turn asking APPROVE (→ autonomous build) or REVISE; on approval → `switch_mode('code')` + build. PLAN-mode prompt rewritten as the approval gate. (6) **Department readiness** (`Launchpad.tsx`): picking a dept no longer auto-sends a play's canned prompt — plays are now EXAMPLES that **pre-fill the editable chat box** (`fillFromPlay`, `.play-card.picked`), and the session is created `task:<deptId>` (chat) so `expertiseFor(deptId)` loads that department's PERSONA (verified all 8 dept ids have a DEPARTMENT persona; a clicked play still passes its `dept.play` key for full per-task rigor). Playwright-verified: 0 composer pills, TopBar chip "Chat", dept→ready heading + 11 example plays that pre-fill (no nav/send). 175 tests; typecheck+build clean. NOTE: the live AGENT behaviors (auto-route, vague-ask, plan-gate firing) are prompt-driven and best felt on the Droplet with a model key — not exercised through a live run here. **Personalized greeting + input-first Launchpad — DONE (sixth commit)**: new `client/src/lib/greeting.ts` (`greeting(name)` = local-time slot morning/afternoon/evening/night × a pool of ~5 varied lines, rotates by hour so it never feels canned; `displayName` = localStorage `arksai.name` → else first word of `me.user.name`, excluding generic "Operator"/"Admin"). The Launchpad opens with a personalized line ("Coffee and ArksAI time, Kamran?") + an inline ✎ to set/change the name (localStorage). **UI reflow** (CSS flex `order`, not JSX moves): on a department screen the **chat input (`.lp-free`) sits at the TOP** under the greeting, examples (`.lp-cats`) denser below (3-col `auto-fill minmax(212px)`, tighter cards) — "input at top, rest below, selectable when wanted". Playwright-verified (greeting personalizes, input above examples). NOTE: the "input-at-top" reflow was applied to the LAUNCHPAD (the landing). Whether the operator also wants the in-conversation CHAT view changed (input pinned to top there) is UNCONFIRMED — left as-is (inverting a live chat is unusual); ask before doing. The greeting is client-only (no server test runner) so it's QC'd visually, not unit-tested.
- **Launchpad density + shareable chat links — DONE (seventh commit), QC'd**: (1) **density pass** on the Launchpad (theme.css) — wider container (760→880px), smaller hero (38→30px), tighter masthead/sub/margins, denser example grid (`auto-fill minmax(204px)`, gap 8, card padding 10×12) + denser dept tiles (`auto-fill minmax(270px)`, padding 13×15). (2) **Deep-linkable chats**: each session has a URL `/s/<id>` (the SPA `setNotFoundHandler` already serves index.html for non-`/api/` paths). `App.tsx` syncs it with TWO effects — URL→state (mount + `popstate` only; deps `[authed, sessions, setActive]`, reads `activeId` via `useStore.getState()` to avoid a render loop) and state→URL (deps `[activeId, authed]`, `pushState` guarded by `p !== target`). **GOTCHA (hit + fixed): React error #185 infinite loop** when the URL→state effect depended on `activeId` — it ping-ponged activeId↔null against the state→URL push; fixed by dropping `activeId` from its deps + reading it fresh via getState. New **🔗 Link** button in `TopBar.tsx` copies `${origin}/s/<id>`. Playwright-verified: open→URL updates, reload reopens the session, no #185, Link button present. The address bar also gives every chat a visible link. 175 tests; typecheck+build clean.
- **generate_creative robustness fix — DONE (on `main`, 2026-06-17)**, from operator ("generating creative didn't work, why?" + a screenshot). ROOT CAUSE: the model called the tool with `{"imagery_prompt": "...the whole brief..."}` — the wrong field name (the param is `prompt`) AND no `headline`, so the `required:['prompt','headline']` validation rejected it in 0.0s; the agent then MISREAD that as "image tools aren't available" and fell back to a plain HTML/CSS graphic in code mode. NOT egress/key (image-01 + the sk-cp key return 200 from here, re-verified live; a transient 403 mid-debug was just a momentary rate blip). FIX (`tools/creative.ts`): accept aliases (`prompt` ← `imagery_prompt`/`image_prompt`/`scene`; `headline` ← `title`/`heading`), make `headline` OPTIONAL (`required:['prompt']` only; `buildCreativeHtml` omits the `.h` when empty so a missing headline never blocks output), and lead the description with "SPLIT THE BRIEF" + a concrete example call so the model puts imagery in `prompt` and copy in `headline`/`bullets`/`cta` (cramming copy into the prompt is what garbles image text). The no-prompt error is now an actionable retry instruction with the example. `expertise.ts` marketing.creative reinforces the split. Re-validated LIVE end-to-end: a call with `imagery_prompt`+split copy produced the full UK-visa creative (sent to operator). 176 tests green.
- **Pending / next steps**: (1) MiniMax request shapes (vision/tts/video LLM) are UNVALIDATED from the sandbox — validate on the Droplet (or a fresh session with egress) and tune model ids/costs. **(image-01 IS validated live — works.)** (2) Egress is a new-session allowlist: **a fresh session CAN reach `api.minimax.io`** (this/old sessions can't) → that's where vision-QC for reports actually runs. (3) Re-generate the GIC report in Report mode in a fresh session to get MiniMax + mandatory vision QC. (4) PWA dormant (needs HTTPS/domain), TLS parked.

## System audit (2026-06-15) — full report in `AUDIT.md` (on `main`)
A hands-on audit ran ALL 52 plays end-to-end (real API + Playwright/mupdf capture). Verdict: **core quality is high** — product UI, reports/decks, and web apps (microanimations + responsive + self-hosted Google-font typefaces) meet the bar; 13/14 apps published live. Ranked flaws to fix one-by-one (don't re-discover):
- **P0 agent/host isolation:** `AGENT_UNRESTRICTED=true` runs the agent shell as the SAME user IN the server process — a web-app build's `pkill node`/`npm install` **killed the server (×2)** and mutated the host repo. Fix = spawn agent children with a dedicated low-priv `uid/gid` (`exec.ts`/`processes.ts`/`registry.ts` + a `Dockerfile` user + chown `/data`) + an always-on `pkill/kill/fuser` denylist in `bash.ts`. (Testing mitigation = an auto-respawn server supervisor.)
- **P1 spreadsheet models aren't formula-driven — RESOLVED (2026-06-15).** Two-part fix landed. (1) The "no formula support" claim was STALE: commit `8c76445` already added formula+cached-result plumbing — `toCell`/`toRow` in `excel.ts` convert `"=B2*C2"` and `{f,v}` into ExcelJS `{formula,result}` cells, and it works (a real run produced 191 live cross-sheet formulas). (2) The REAL gap was **enforcement**: the model used formulas only ~1 in 6 times, and the gate couldn't catch a formula-less model — `checkDeliverable` only *counted* formulas and the vision review renders computed VALUES (so a `=SUM()` and a typed-in literal look identical). Fixed by `auditFormulaModel(wb)` in `deliverableCheck.ts` (pure; reads `cell.f` directly): a workbook with **0 formulas** that shows derived numbers (a Total/Net/Ending-Balance/Growth ROW carrying literals, or a multi-sheet model with an Assumptions/Drivers tab + ≥30 numeric cells) is flagged → seeded into `designDefects`/`designVerdict='revise'` BEFORE the vision early-returns (so it fires even with no MiniMax egress) → the existing 1-round bounded revise (`reviewDeliverables`→`runVerifyGate`/`runReportGate`) sends it back with a targeted "use live formulas" instruction. Plain data tables (no derived rows, no assumptions sheet) are never flagged — calibrated against all 6 sweep files + the test fixtures. Steering also tightened (excel.ts description + `FIN_SHEET` in `expertise.ts`: hard-coded totals "WILL BE REJECTED", prefer `{f,v}`).
- **P1 MiniMax design-gate fails silently** when vision errors (`uiCheck.ts`) → make it loud. **P2:** dup errored deployments on re-publish (no GC in `publish.ts`); faux-logo ⚠ icon on some report covers (`prompts.ts`); `.docx` uses Calibri not the editorial font (embedding needs TTF assets, `docx.ts`).
- Re-validate any fix by re-running the affected plays via the API harness (create session w/ `task`+`model` → send brief + an autonomous suffix → poll → screenshot via `/apps/<slug>/` or mupdf).

## Stack & layout
- **Projects** (persistent workspaces grouping sessions): `projects` + `project_files` tables, `sessions.project_id`. A project holds custom instructions, default repo/branch/mode/model, branding (accent/logo), and a knowledge base (files on the volume at `data/projects/<id>/knowledge/`, copied into each session's `knowledge/` at setup with doc-extraction). Sessions created in a project INHERIT its defaults; the runner injects a "## Project" block (instructions + knowledge index + branding) + `project:<id>` memory scope. API `routes/projects.ts`; client `ProjectDialog.tsx` + sidebar Projects→Sessions grouping. Deleting a project detaches (keeps) its sessions.
- npm workspaces: `server/` (Fastify + TS), `client/` (React + Vite), `shared/types.ts` (the contract).
- Storage: dual driver in `server/src/db` — PostgreSQL when `DATABASE_URL` is set, else SQLite on the volume. The store (`server/src/sessions/store.ts`) is async.
- Agent loop: `server/src/agent/runner.ts`. Tools in `server/src/agent/tools/`. Prompts in `server/src/agent/prompts.ts`.
- Engines/orchestration: `server/src/engines/` (registry + suno). Add an engine = registry entry + a gated tool.
- Modes: chat / plan / code / **report**. Report mode (`render_report` + `add_fonts` tools, report block in `prompts.ts`) turns pasted text/CSV/uploads into a designed PDF or 16:9 deck via bespoke HTML/CSS → headless-Chromium `page.pdf()`; curated toolset via `REPORT_TOOLS` in `tools/index.ts` (no git/verify). **No fixed templates** — each report is designed bespoke but MUST follow the protocol (minimal/modern/typography-first, light by default, charts+tables, centered cover, page-break-safe). `add_fonts` installs self-hosted Inter/Source Serif 4/Space Grotesk (`server/assets/report-fonts/`) so fonts are always embedded. Agent ALWAYS asks branding (colors/accent or upload a logo). Both doc+deck per request; narrative+cited benchmarks allowed, never fabricate figures.
- **Report PAGE MECHANICS (verified, baked into the prompt)**: margins go on `@page{margin:18mm 16mm}` so they repeat on EVERY physical page (NOT on a fixed-width padded container — that was the continuation-page bleed bug). Cover is centered with `min-height:calc(100vh - 36mm)` (36mm = 2× the vertical margin; `@page:first{margin:0}` is NOT honored by Chromium) AND `page-break-after:always` so it's isolated. Tables: compact (`padding:1.1mm 2.6mm`, 9pt, lh 1.3), centered, zebra rows + faint column hairlines, `<thead>` repeats. **Verify PDFs by rasterizing with the `mupdf` npm pkg** (WASM, no native deps) → PNG → Read it; headless Chromium can't screenshot a PDF (it downloads it).
- **Report quality lessons**: reports default to **MiniMax** when keyed (else Pro), NEVER Flash — Flash produced cover bleed, lonely pages, dark boxes, invisible text. Protocol mandates: content FLOW (no forced per-section breaks → lonely pages); CONTRAST safety (no text == bg/accent; callouts LIGHT); ICONS inlined (external `<use href>` doesn't render in Chromium PDF — `add_fonts` ships `icons.svg` as an inline-source). **Design quality (research-backed)**: RESTRAINED palette — ink/charcoal numbers, accent used only ~5–10% (don't colour every number); COMPOSITION — fill each page ~85–100%, balance whitespace, NO orphan KPI tiles (even grid), group a chart with its insight callout (wrap, break-inside:avoid), 2-col grid for dense text; CHARTS — flat 2D, no 3D/gradients/shadows/gridlines, direct value labels, muted base + accent on the key series only. **VISUAL QC via see_image/MiniMax-VL is MANDATORY** — critique every page like a design director and iterate (needs MiniMax egress; blocked in old sessions). Core needs no MiniMax; vision QC (`see_image`) lights up when keyed. **Editorial/newspaper upgrade (2026-06, from user feedback on the Hormuz report)**: (1) HEADING-BLEED was still happening (`break-after:avoid` alone is insufficient) → the protocol now mandates a MECHANICAL anti-orphan: wrap each section's heading + its opening paragraph (its "lede", kicker too) in `<div class="lede">` with `break-inside:avoid` so the pair always moves as ONE unit and a heading can never strand at the page bottom. (2) Side margins widened to **`@page{margin:20mm 26mm}`** (24–28mm guidance, cover calc 2×20mm) for generous newspaper gutters — text never edge-to-edge. (3) New EDITORIAL STRUCTURE toolkit (hairlines that separate focus points): kickers/eyebrows, ruled section headings, full-measure `.rule` section dividers, 2-col body with a `column-rule`, a running `.masthead`. ALL of these verified to render in the real Chromium `page.pdf()` pipeline (rasterized via `mupdf` npm — install with `npm i mupdf --no-save` in a fresh session; rasterize at ~170–200 DPI and Read the PNG).
- Models are ArksAI-branded labels (`deepseek-v4-flash` = "ArksAI Flash", `-pro` = "ArksAI Pro"); model list is fetched live from DeepSeek's /models.
- Sandbox egress is a **curated allowlist proxy** (set per environment, applies to NEW sessions only). `api.deepseek.com` is allowed; `api.minimax.io` was added but a running session keeps its old policy — MiniMax validation needs a fresh session. Generated-media download URLs sit on storage/CDN hosts that also need allowlisting.
- Canvas (in-app dev preview): `client/src/components/Canvas.tsx` (Preview/Files tabs). Preview proxies through `/api/sessions/:id/preview/:port/*` (`server/src/routes/preview.ts`), which rewrites root-absolute `src/href="/..."` and injects `<base href>`. Listening ports come from `/ports` → `server/src/lib/ports.ts` (`listeningPorts` parses /proc/net/tcp{,6}).

## How to work here
- **Don't rely on training memory — VERIFY first (operator's standing rule).** The operator runs models newer than the assistant's knowledge cutoff (e.g. **MiniMax-M3**, June 2026); answer any model/API/pricing question only after checking it LIVE (web search / a real API call / live `/models`), and any code question only after reading the actual files in-repo. Never guess. **Re-read the COMPLETE chat before responding** so earlier decisions / keys / running state aren't lost.
- **Before telling the operator "it works", VERIFY IT ON THE DEPLOYMENT SERVER, not just locally (operator's standing rule, 2026-06-20).** Local typecheck/tests/build passing is necessary but NOT sufficient — a fix only counts when it's confirmed working on the LIVE site (**https://arksai.studio**, login `APP_PASSWORD` from `/opt/arksai/.env`). This session CAN reach arksai.studio (Playwright is installed) — so after pushing, WAIT for auto-deploy (~2 min) and drive the real live flow (log in, create a session, exercise the actual feature end-to-end through the live agent) before claiming success. Never report a behavioural/UX/agent fix as working off local checks alone.
- Before claiming done: `npm run typecheck && npm test && npm run build` (65 tests). All must pass.
- **Keep `FEATURES.md` current**: every commit that adds/removes/meaningfully changes a feature must update `FEATURES.md` (the living, user-facing feature list) in the SAME commit. The user asked for this explicitly.
- **Keep the "what's new" modal current** (user asked explicitly): with EVERY shipped iteration/update, update `client/src/components/WhatsNewModal.tsx` — bump `WHATS_NEW_VERSION` (so it re-shows once per user) and **REPLACE** `ITEMS` with ONLY the latest build's changes (1–3 entries — do NOT accumulate a running changelog; the cumulative list overflowed the screen on mobile, which the user flagged) — in the SAME commit. It's how we keep people in the loop.
- After changes: commit to `main` and push. **Auto-deploy** (systemd timer on the Droplet) pulls + rebuilds `main` every ~2 min — no manual deploy needed. Don't hand-edit files in `/opt/arksai` (overwritten); `.env` is safe.
- The user wants work landed on `main`. The web/task harness sometimes assigns a feature branch (e.g. `claude/...`); auto-deploy only watches `main`, so commit/merge to `main` — the user has said "commit to main" explicitly and repeatedly.
- Local manual run for testing: `APP_PASSWORD=testpass PORT=3000 DEEPSEEK_API_KEY=... nohup node server/dist/server/src/index.js &` — start it in an ISOLATED bash command (a `pkill`/`kill` in the same command kills the new start; that's burned me repeatedly).
- **UI work is always VISUALLY QA'd, not just gate-checked (operator directive, 2026-07-03):** any change to a skeleton/template/client surface gets Playwright screenshots at 390px AND 1280px of every affected page, eyeballed for micro-animations, responsiveness, palette and overall aesthetic before shipping. The screenshots have caught what the detectors can't (a builder-note leaking into a delivered dashboard).
- To reproduce UI bugs, drive a real headless browser (Playwright is installed) and click the actual element — curl tests miss frontend-path bugs (the DELETE-400 bug was only visible through the browser).

## Deployment (DigitalOcean)
- **⚠️ PROTECTED MASTER INFRA — what the AI must NEVER touch (operator's explicit rule, 2026-07-02):** the master production droplet **`arksai` (id `577088981`, IP `159.89.172.210`)** and the baked Android snapshot (`233935491`) are OFF-LIMITS to any automation — never destroy, resize, rebuild, power-off, or reconfigure them. This is ENFORCED in code: `server/src/build/do.ts` `isProtectedResource`/`assertDestroyable` hard-refuse a `destroyDroplet` on the master id (layer 1) and defensively on its name/tag (layer 2), so a runaway build job or a wrong id can't reap the live host — only disposable build droplets (a distinct `arksai-build` tag) are ever destroyed. Config: `config.masterDropletId`/`masterDropletName` (env `MASTER_DROPLET_ID`/`MASTER_DROPLET_NAME`). Any DO-API use from the sandbox is READ-ONLY (list/inspect); the pasted token is EXPOSED → rotate it. Rotate the DO token + the Swift ark key from **Admin → Keys** (`POST /api/admin/providers/do` · `/byteplus`, encrypted at rest, no SSH/redeploy).
- Droplet **arksai**, 4 GB, region **blr1 (Bangalore)**, in DO project **"ARKS AI Platform"**. Public IP **159.89.172.210**.
- App URL: **http://159.89.172.210** (plain HTTP; TLS via `docker-compose.tls.yml` + Caddy is parked — needs a domain to avoid self-signed warnings).
- Managed Postgres **arksai-db** exists (PG16, blr1) but is only used if `DATABASE_URL` is set; currently on SQLite + the `arksai-data` volume.
- Redeploy manually if ever needed: `cd /opt/arksai && ./deploy.sh` (plain) or `./deploy.sh tls`.
- I (the assistant) CANNOT reach the Droplet directly — sandbox egress blocks its IP and SSH; the DO API token only manages infrastructure, not shell. Deploy happens via push→auto-deploy.

## Secrets (NEVER commit real values here)
- All live secrets are in `/opt/arksai/.env` on the Droplet: `APP_PASSWORD`, `DEEPSEEK_API_KEY`, `GITHUB_TOKEN`, `SERPER_API_KEY`, `SUNO_API_KEY`, `MINIMAX_API_KEY`. Read the login password with `grep APP_PASSWORD /opt/arksai/.env`.
- MiniMax: `MINIMAX_API_KEY` MUST be a **Subscription Key (`sk-cp-…`)** to draw on the Token Plan — a pay-as-you-go `sk-api-…` key returns `insufficient_balance (1008)`. Value is NOT in any tracked file — dev: gitignored repo-root `.env` (`MINIMAX_API_KEY=`, `MINIMAX_BASE_URL=https://api.minimax.io/v1`); prod: `/opt/arksai/.env` on the Droplet (**update it to the `sk-cp` key** to make M3 work live). Optional `MINIMAX_GROUP_ID=` for T2A/voice.
- **The GitHub repo `chordsnstrings/arksai` is PUBLIC.** Never commit any real secret (CLAUDE.md included) — it would be scraped/auto-revoked in minutes. Secrets live only in gitignored `.env` (local) and `/opt/arksai/.env` (Droplet).
- `AGENT_UNRESTRICTED=true` is set (full host access for the agent) — single-operator trusted mode.
- Keys pasted in chat are considered exposed and should be rotated.

## Engine notes
- Engine roster lives in `server/src/engines/registry.ts` (`listEngines()`), surfaced at `GET /api/engines`. Each engine = registry entry (gated on its key) + a tool. Keys are read in `server/src/config.ts`.
- Suno (sunoapi.org/apibox): `generate_music` tool, gated on `SUNO_API_KEY`. Cost per track added to the session cost bar (`SUNO_COST_PER_TRACK`, default $0.08). The real API call was never validated from the sandbox (egress) — validate on the Droplet; adjust request shape in `server/src/engines/suno.ts` if it errors.
- MiniMax (`server/src/engines/minimax.ts`): the "anything DeepSeek can't do" provider. **`arksai-max` = MiniMax M3, NOT Anthropic** — MiniMax merely offers OpenAI- *and* Anthropic-*protocol*-compatible surfaces; the provider/model is MiniMax. Don't frame it as Anthropic. Gated tools (`tools/minimax.ts`): `see_image` (vision — also the verify-gate visual check), `generate_image`, `text_to_speech` (needs `MINIMAX_GROUP_ID`), `generate_video`. LLM is OpenAI-compatible (`minimaxBaseUrl=…/v1` + `minimaxModel`, default **`MiniMax-M3`**; **M3 is natively multimodal so vision runs on M3 too** — `minimaxVlModel` also defaults to M3; old `MiniMax-VL-01` is an INVALID id).
  - **BILLING IS BY KEY TYPE, not endpoint (the big gotcha):** a **Subscription Key (`sk-cp-…`)** draws on the Token Plan quota; a pay-as-you-go **`sk-api-…`** key bills the (often empty) credit wallet → `insufficient_balance (1008)` *even when the dashboard shows 100% plan quota*. Same error on BOTH `/v1` and `/anthropic` — it's the key, not the route. Check plan quota: `GET https://api.minimax.io/v1/token_plan/remains` (covers `general`+`video`).
  - **M3 MUST be called via the ANTHROPIC endpoint, not OpenAI (THE big 2026-06-15 finding, verified live).** On the OpenAI surface (`/v1/chat/completions`) M3's thinking is **forced ON and unbounded** — on a real agentic prompt it reasoned 20k+ chars and **never emitted a tool call in 150s** (then our `max_tokens` truncated it into a silent empty turn = the "spreadsheet hang"). Every OpenAI thinking-control knob (`reasoning_effort`, `thinking`, `enable_thinking`, `chat_template_kwargs`) is **IGNORED**; `reasoning_split:true` only relocates reasoning into `reasoning_content` (it does NOT bound it). On the **Anthropic surface (`https://api.minimax.io/anthropic/v1/messages`) M3 thinking is OFF by default** → it called `generate_spreadsheet` in **~2.8s**. So the runner now routes MiniMax through an Anthropic-format adapter (`createMinimaxStream` + `toAnthropicMessages`/`anthropicSseToOpenAI` in `runner.ts`) that translates our OpenAI-shaped loop to/from the Anthropic wire format; DeepSeek still uses OpenAI. Thinking is left **off** (fast, decisive tool use; `thinking:{type:'adaptive'}` = ON = the slow/hang path — MiniMax's docs recommend adaptive for agentic quality, but it's minutes/turn, so off is our default for now). **Gotchas:** (1) `fetch` has no timeout → the adapter idle-aborts at 120s (M3 buffers tool-arg generation server-side with ~30s silent gaps that are normal). (2) M3 **stalls under concurrency** on the Anthropic endpoint (concurrent streams hang) — keep concurrent M3 runs low. (3) Multi-turn with thinking ON requires echoing the model's `thinking` blocks back **unchanged** (incl. `signature`); we don't preserve them yet, so only enable adaptive after adding that. (4) Models on `/anthropic`: `MiniMax-M3` (1M ctx, multimodal) + `M2.7`/`M2.5`/`M2.1`(+`-highspeed`, ~100 tps) coding models — M2.x always think. Token plan healthy (sk-cp); M3 rate limit 200 RPM / 10M TPM.
  - Capability ids `image-01`/`speech-02-hd`/`MiniMax-Hailuo-02` are likely **stale** (current gens: Hailuo 2.3, Speech 2.6) — confirm before use; tune `minimax*Cost`. Music stays on Suno.
- Orchestrator model **ArksAI Auto** (`arksai-auto`) + **ArksAI Max** (`arksai-max` = MiniMax LLM, shown/selectable when keyed). Routing in `server/src/agent/router.ts` (pure heuristic: task text + mode → Flash/Pro/MiniMax tiers, escalates to a stronger model when verification fails, MiniMax→Pro fallback on hard error). Cost is **server-authoritative**: summed per concrete model and streamed as `usage_update.costUsd`, so the footer blends models in Auto mode. Two routing layers: model-tier (text) here + capability tools (modalities) above.

## Gotchas learned the hard way
- Empty JSON body → Fastify 400. The client must not send `Content-Type: application/json` without a body; server has a tolerant JSON parser. (Caused the DELETE-not-working bug.)
- Agent apps default to `PORT=4000` (childEnv) so they can't collide with / kill ArksAI on 3000.
- DeepSeek v4 models default to THINKING mode — for tiny calls (titles) use the non-thinking `deepseek-chat` alias or the token budget is eaten by reasoning.
- The sandbox/container has noise listening ports that are NOT apps (saw 2024, 2025, plus ephemeral 34xxx). Canvas must never auto-load the *lowest* port (`ports[0]`). `pickPreviewPort` in Canvas.tsx prefers known dev ports (agent default 4000 first, then 5173/3000/8080/…) and only auto-picks a single plausible 3000–9999 port otherwise; `orderPorts` lists recognised dev ports first. (This was the "canvas won't load apps" bug — it had been loading port 2024 and showing 502/401.)

## Behavior defaults baked into the agent
- **Design philosophy (global, durable): minimal, classy, beautiful, typography-first — ALWAYS, for reports AND any UI/code build, unless the user explicitly opts out.** Every iteration should look designed, not default: embed high-quality fonts (self-host via @font-face / the `add_fonts` tool: Inter, Source Serif 4, Space Grotesk — or a quality Google Font), use a clear modular type scale + ~1.5 line-height + real hierarchy, light/restrained palette with one accent, generous whitespace. Baked into the code-mode UI defaults + the report protocol in `prompts.ts`.
- UI builds: modern/minimal/responsive, generous padding, micro-animations; ASK the user to choose a color palette (offer named complementary palettes) before building.
- Code mode: mandatory verification gate (static checks + runtime boot/flow for apps) before completion.
- Auto-export + auto-canvas: on a successful code run that touched a real project, the runner zips a complete export (`<name>-export.zip`, excludes node_modules/.git; download chip) and, for renderable output (web app or static HTML), boots a persistent preview server (`startPreviewServer`, PORT 4000 / python static server) and emits `open_canvas` so the client opens the Canvas. Code lives in `server/src/agent/canvasExport.ts`. The agent shouldn't hand-zip or start its own preview for this.
- UI verification: the runtime probe (`probeApp`) loads HTML apps in **headless Chromium** (Playwright, `server/src/agent/uiCheck.ts`) and hard-fails the verify gate on a blank page, uncaught JS errors, or failed same-origin asset/API requests — because DeepSeek is text-only and can't "see" the UI; these are signals it CAN read and fix. True visual/layout judgment would need a vision model via the orchestrator (not built). `uiCheck` degrades gracefully if Chromium is missing. The Dockerfile installs Chromium (`npx playwright install --with-deps chromium` → `/ms-playwright`) and `zip`.
- Music: act as a Suno expert; confirm the brief before the first (paid) generation.
- Global/project memory feature exists in-app (`/memory`, ARKS.md) — separate from this file.
